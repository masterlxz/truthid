// Copie este arquivo para `secrets.dart` (mesma pasta) e preencha com suas
// proprias chaves para desenvolvimento local. `secrets.dart` e ignorado pelo
// git — nunca commitar chaves reais.
//
// Nota (debito #27): usuarios finais nao precisam criar este arquivo. A
// configuracao do bundler pode ser feita via SettingsScreen (gear icon no
// AppBar), que salva no secure storage do dispositivo em tempo de execucao.
// O `secrets.dart` serve apenas como fallback durante o desenvolvimento.
//
// Exemplo: crie uma conta em dashboard.pimlico.io e gere uma API key gratuita.

const pimlicoApiKey = "";