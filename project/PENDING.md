# Pendências do Projeto

> Arquivo central de pendências — **resolvidas e não resolvidas**.
> Toda pendência encontrada em qualquer arquivo do projeto deve ser registrada aqui com um ID único.
> Ao resolver uma, marcar como `✅ Resolvida` com a sessão em que foi corrigida.
> 
> Última atualização: 2026-08-27 (Sessão 228: **P86 FECHADO por completo** — `coinbase_wallet_sdk`
> vendorizado em `mobile/third_party/` com 3 patches (`compileSdk` 36 + 2 `.pro` vazios que faltavam
> no tarball publicado, bug real do upstream achado só ao reproduzir o build de verdade) via
> `dependency_overrides`; validado localmente via Docker (`assembleRelease` limpo, `flutter analyze`
> limpo, `flutter test` 702/702) e depois no CI real: tag `v2.2.1` cortada (só bump do build number do
> Mobile, Desktop ficou em 2.2.0 de propósito) — `Build Mobile` e `Build` passaram (`success`) num
> runner limpo do GitHub Actions, release publicado de verdade, `site/frontend/lib/releases.ts`
> atualizado (`android.apk` → `v2.2.1`, confirmado ao vivo via `curl`). Sessão 227, 5ª parte: **tag `v2.2.0` cortada de verdade** pra validar
> o P85 — `build.yml` (Desktop 3 SOs + extensão) passou 100%, release draft com todos os assets
> assinados + `latest.json`; achado real no processo: input `uploadUpdaterJson` do `build.yml` estava
> com nome errado (`includeUpdaterJson` é o certo, corrigido — o default já cobria, então não afetou
> o resultado). **`build-mobile.yml` falhou** — `coinbase_wallet_sdk` (dependência abandonada do
> `reown_appkit`) travado em `compileSdk 31`, sem fix rápido disponível — registrado como **P86**,
> novo item. Decisão do dono do projeto: publicar `v2.2.0` só com Desktop, Mobile fica de fora até
> P86 ser corrigido numa sessão dedicada. Sessão 227, 4ª parte: **P85 registrada e implementada** —
> auto-update real no Desktop (Windows/macOS/Linux via AppImage) via `tauri-plugin-updater`, chave de
> assinatura gerada e guardada nos secrets do GitHub + backup local fora do repo; `tsc`/`vitest`/`cargo
> check`/`cargo test`/`clippy` limpos. Sessão 227, 3ª parte: **P82 validado de verdade via Docker**
> (`docker compose run --rm flutter ...` — dono do projeto lembrou que os testes rodam assim, sem
> precisar Flutter local) — `flutter analyze` limpo, `flutter test --concurrency=1 test/`: **702/702
> testes passando, 2 pulados (tag `arlocal`), 0 falhas** nos 76 arquivos, incluindo todos os 8 tocados
> nesta sessão. Sessão 227, 2ª parte: **P82 FECHADO por completo no código** — os 7 achados de
> limpeza/duplicação (4)-(10) também corrigidos: mixin `WalletConnectFlowMixin` deduplica
> `connectWallet`/`_waitForReceipt` nos 4 flows do Mobile; `abi_encoding.dart` compartilhado entre
> `identity_consent_hash.dart`/`BlockchainService`; `eth_amount.dart` ganhou `weiToDecimalString` e
> `wallet_screen.dart` passou a delegar o parsing decimal↔wei em vez de duplicá-lo;
> `random_bytes.dart` virou a única fonte do salt Arweave (2 call-sites migrados); 2 lugares com
> `await` sequencial viraram `Future.wait`; 1 comentário desatualizado corrigido em
> `blockchain_service.dart`. Sessão 227, 1ª parte: P82 itens (1)/(2)/(3) corrigidos —
> reatividade do campo de username em `act_as_guardian_screen.dart`, catch faltando em
> `checkAvailability()` de `create_identity_screen.dart`, timeout defensivo em
> `WalletConnectService.openConnectModal()`). Sessão 226, 6ª parte: os 2 achados de reuso restantes do P84 (itens
> 9-10) **corrigidos** — **(9)** novo `config::get_keyring_or_file`/`set_keyring_or_file` (Rust)
> deduplica o padrão "keyring do SO → fallback em arquivo → trata string vazia como ausente" (P71),
> antes copiado em `get_device_key_hex`/`get_arweave_wallet`/`set_vault_key` (`lib.rs`),
> `local_wallet.rs` e `app_lock.rs`; achado colateral real: unificar `get_device_key_hex` também ganhou
> o cuidado do P71 que só o Arweave tinha, e isso **corrigiu de vez a falha ambiental pré-existente**
> em `local_signer_server::sign_message_endpoint_...` nesta máquina (keyring com entrada vazia pra
> device key) — `cargo test --lib` foi de 232/233 pra **233/233**. **(10)** novo módulo
> `connectors/evmProviderShared.ts` (TS) deduplica `parseLedgerSignature`/`parseTrezorSignature`/
> `parseLocalWalletSignature` (agora um só `parseRsvSignature`) e o dispatcher inteiro do provider
> EIP-1193 (eth_chainId/eth_accounts/eth_sendTransaction/personal_sign + loop de fallback de RPC),
> antes ~90 linhas quase idênticas em `ledger.ts`/`trezor.ts`/`localWallet.ts`; mensagens de erro
> específicas de cada wallet (incluindo a inconsistência de capitalização já existente,
> "Local wallet not connected." vs "Ledger/Trezor not connected.") preservadas ao pé da letra —
> nenhum teste existente precisou mudar. **P84 fechado por completo** (10/10 achados corrigidos).
> `cargo test --lib` (233/233)/`cargo clippy --lib`/`npx vitest run` (179/179)/`tsc --noEmit` limpos.
> Sessão 226, 5ª parte: os 5 bugs de correção restantes do P84 (itens
> 4-8) **corrigidos** — guarda de "ocupado" impedindo fechar Configurações em pleno migração; espera
> o saldo carregar antes de mover (evita saldo órfão); mutex fechando a corrida de duplo-clique em
> `local_wallet_generate` + botão desabilitado no frontend; `SigningKey::from_slice` em vez de
> `.into()` (panic→`Err` limpo em chave de tamanho errado); `vault_import_backup` aborta o import
> inteiro se detectar uma chave local diferente da já configurada, em vez de manter a errada em
> silêncio. Sessão 226, 4ª parte: os 2
> travamentos totais do P84 (itens 1 e 2) **corrigidos** — reconectar wallet local existente sem
> passar por `local_wallet_generate`, e novo comando `app_lock_reset` + link "Esqueceu sua senha?" no
> `AppLockGate` pra recuperar o bloqueio sem senha (só remove o gate, não toca no Vault). Sessão 226,
> 3ª parte: P83 fechada — `/code-review high desktop/`
> rodado sobre P78 pedaço 1/2 + Configurações/senha, achados registrados em P84: **2 bugs de
> travamento total do app sem recovery** (reconectar wallet local depois de reiniciar o app; esquecer
> a senha do bloqueio ou o blob corromper) + 6 bugs reais de correção (gate de app-lock sem try/catch
> pode travar em "Checking..." pra sempre; `Settings.tsx` reusa `onClose` e derruba o `MigrateWallet`
> em pleno "confirming"; saldo pode ficar órfão numa migração com RPC lento; race de duplo-clique na
> wallet local; panic em vez de erro em chave malformada; import de backup pode manter chave local
> errada) + 2 achados de duplicação (padrão de custódia keyring→arquivo copiado 4x; parsing/fallback
> de connector copiado 3x). Nada corrigido ainda, decisão de prioridade fica pro dono do projeto.
> Sessão 226, 2ª parte: registrada P83 — falta rodar `/code-review`
> no Desktop, que acumulou P78 pedaço 1/2 e a tela de Configurações+senha sem revisão nenhuma; só o
> Mobile foi revisado nesta sessão. Sessão 226, 1ª parte: `/code-review high` rodado sobre `mobile/` (P68, as 3
> fatias) — 2 bugs reais de correção confirmados (P82: campo de username não reativo em
> `act_as_guardian_screen.dart`, quebra a busca de guardião por completo; exceção não tratada em
> `create_identity_screen.dart::checkAvailability`), 1 risco plausível não 100% confirmado (modal do
> WalletConnect pode travar `_busy` pra sempre), 7 achados de limpeza/duplicação. Nada corrigido
> ainda, só revisado e registrado — decisão de prioridade fica pro dono do projeto. Sessão 225, 3ª
> parte: tela de Configurações (⚙) nova no Desktop —
> reúne idioma (movido do topbar) + migrar wallet (movido do dashboard, P78 pedaço 2) + bloqueio do
> app por senha própria do TruthID (novo, pedido direto do dono do projeto, também pro Mobile mas
> confirmado que o Mobile já tem isso via `AppLockService`/biometria do celular). Pesquisado se dava
> pra integrar com auth nativa do SO (Windows Hello/Touch ID) — sem plugin Tauri maduro cobrindo os 3
> SOs, decisão do dono do projeto de não perseguir, só senha própria reaproveitando
> `backup::encrypt`/`decrypt` (PBKDF2+AES-GCM já testado no backup do Vault) sem escrever criptografia
> nova. 162/162 testes TS (11 novos), 4 testes Rust novos, tsc/clippy limpos; falta validação manual,
> registrada como P81. Sessão 225, 2ª parte: P78 pedaço 2 (migrar identidade pra outra wallet)
> IMPLEMENTADO no Desktop — resolveu as 2 perguntas em aberto da Sessão 224 lendo os contratos reais
> (guardiões e devices são indexados por `identityId`, sobrevivem à migração sozinhos; as 3 chamadas
> que a conta antiga precisa fazer cabem num único `executeBatch`, então não há "meio do caminho"
> sem device autorizado que dependa de mais de uma assinatura — o único passo que exige a chave nova
> é reautorizar devices, já coberto pelo banner existente de `ManageDevices.tsx`, P53); 151/151
> testes TS, tsc/clippy limpos; falta só validação manual real (2 wallets de verdade), registrada
> como P80. Sessão 224 (2ª parte): P78 pedaço 1 (wallet local embutida) IMPLEMENTADO
> no Desktop — 224/225 testes Rust + 145/145 TS, tsc/clippy limpos; falta só validação manual real
> (P79), tentativa abortada por segurança depois de um clique de coordenada absoluta acertar por
> engano uma aba do navegador do dono do projeto. Sessão 224 (1ª parte): P78 registrada — `/plan` de
> viabilidade da wallet local embutida (bootstrap sem wallet externa) + migração hot↔hardware wallet,
> pedido explícito do dono do projeto pra só pesquisar e registrar; conclusão: viável, sem bloqueio
> técnico, desenho em 2 pedaços com complexidade desigual (ver `ROADMAP.md`). Sessão 223 (2ª parte): P68 fatia 3 FECHADA — votar como guardião de outra identidade (propor/aprovar/executar recovery) implementado pelo Mobile, chamadas diretas ao `RecoveryManager` assinadas pela wallet do guardião, sem passar pela smart account; `cancelRecovery` continua fora, junto de revogar device (P75); falta validação real contra wallet física, registrada como P77; 688/688 testes (16 novos), sem achados de bug desta vez (calldata builders bateram no vetor `viem` de primeira). Sessão 223 (1ª parte): P68 fatia 2 FECHADA — parear novo device + configurar guardiões implementado pelo Mobile, reaproveitando a infra WalletConnect da fatia 1; revogar device e `cancelRecovery` ficam de fora, registrados como P75; falta validação real contra wallet física, registrada como P76; 2 achados reais no caminho (ordem dos slots do cabeçalho ABI de `registerDevice` tinha que seguir a ordem de declaração dos parâmetros, não "estáticos primeiro"; offset da tabela de `func[]` no `executeBatch` estava incluindo o slot de length a mais). Sessão 222: P68 fatia 1 FECHADA — WalletConnect + criar identidade 100% pelo Mobile implementado, espelhando o Desktop; pareamento/guardiões ficam pra fatia 2; Ledger BLE descartado; 3 achados reais no caminho (conflito reown_appkit↔web3dart, bug latente de parse do ABI de eventos, EtherAmount.fromBase10String com nome enganoso); falta validação real contra uma wallet física (P74); P71 — round-trip real do backup da wallet Arweave confirmado no Desktop; achado e corrigido bug real em `get_arweave_wallet()` (leitura vazia do keyring mascarava o fallback em arquivo válido); falta só o caso cross-plataforma com o Mobile; P24 FECHADO — suporte a Trezor implementado sobre a crate oficial `trezor-client`, espelhando a Ledger; YubiKey/FIDO2 descartado desta rodada por incompatibilidade real de curva (P-256/Ed25519 vs secp256k1) + libs Rust de CTAP2 imaturas; falta validação em hardware físico (P73), sem cobrança — dono do projeto não tem Trezor pra testar; Sessão 221: P72 — pesquisa de `/plan` completa sobre derivar a wallet Arweave da identidade, adiada por 2 barreiras reais (protocolo ECDSA do Arweave + Mobile sem wallet externa), nada implementado; P70 FECHADO — achado real de wallet Arweave órfã após reinstalar (perda real de ~R$10, sem recovery), corrigido embutindo a chave no backup do Vault nas 2 plataformas, falta validação manual (P71); P69 — Desktop validado ao vivo, toggle + geração de wallet Arweave confirmados, falta só Mobile e saldo real; Sessão 220: P66 FECHADO; P67 implementado e testado; P65 — achado real de validação do winget corrigido, CLA assinada, pipeline 10/10 verde, só falta review manual da comunidade)

---

## Nota de lançamento — Sessão 203 (2026-08-16)

Levantamento completo de pendências pedido pelo dono do projeto pra decidir sobre colocar o app em
produção hoje — **excluindo explicitamente a parte paga** (tier facilitado/billing). Conclusão: o
core do produto (protocolo de identidade, `IdentityRegistry`/`DeviceRegistry`/`RecoveryManager`/
`SessionRegistry`/`TruthIDAccountFactory`/`VaultRegistry`, Desktop, Mobile, extensão, Vault, 2FA,
passkeys, backup, social recovery, 4 SDKs) já está deployado na Base Mainnet (cascata da Sessão
197) e validado com hardware real (Ledger + celular físico) em sessões anteriores — sem débito de
segurança em aberto (S1-S7, C1-C9 todos fechados) e sem achado de `/code-review` pendente. O site
de documentação também já está no ar (GitHub Pages, v1 fechado nas Sessões 199-200).

**Decisão do dono do projeto**: as pendências de validação em hardware real desta seção (P3, P6,
P7, P30, P31, P32, P33, P34, P35, P37, P38 — autofill de SO em Android/iOS, leitura de documentos/
cartões do Vault, cliques reais no fluxo de Social Recovery, validação cross-network do dead-drop)
são **aceitas como risco conhecido** e não bloqueiam o lançamento de hoje — são todas validações de
recursos secundários (Fases 15/16), não do protocolo core de autenticação, que já roda em produção
real desde a Sessão 179 em diante. Ficam registradas como estavam, sem fechamento — continuam
valendo pra retomar quando houver hardware/ambiente disponível (Mac pro iOS, device Android físico,
2 redes de fato diferentes).

**Fora do escopo de hoje, por decisão já registrada em sessões anteriores**: P47 (billing/tier
facilitado), P15/P16 (monetização/session key com limite de gasto), P14 (polish de UI/UX), P49
(i18n, recém-registrado), P18-P24 (ideias de expansão, brainstorm puro).

---

## Não Resolvidas

### P86 — Build de release do Mobile (Android) quebrado: `coinbase_wallet_sdk` (dependência abandonada) trava em `compileSdk 31` — ✅ FECHADO por completo, validado no CI real e publicado (Sessão 228)

**Atualização (Sessão 228)**: corrigido via vendoring — `mobile/third_party/coinbase_wallet_sdk/` é uma
cópia local do pacote `1.0.10` (a mesma versão do pub.dev, código Dart/Kotlin/iOS intocado) com 3
patches no `android/build.gradle`: `compileSdkVersion` 31→36, e 2 arquivos `.pro` vazios criados
(`consumer-rules.pro`/`proguard-rules.pro`). `pubspec.yaml` ganhou `dependency_overrides` apontando
`coinbase_wallet_sdk` pro path local (`pubspec.lock` confirma `dependency: "direct overridden"`,
`source: path`). **Achado real no processo, não documentado na Sessão 227**: o bloqueio não era só
o `compileSdk` — reproduzindo `./gradlew :app:assembleRelease` de verdade via Docker (JDK+Android
SDK completos, mesma imagem que valida o Mobile desde o P82), apareceu um **segundo erro
independente**: `mergeReleaseConsumerProguardFiles` falhando porque `consumer-rules.pro` é
referenciado no `build.gradle` do pacote mas **não existe no tarball publicado no pub.dev** — bug de
publish real do upstream, não um problema de configuração do app. Forçar downgrade das ~20 libs
androidx conflitantes (a rota considerada mais rápida na Sessão 227) não teria resolvido esse
segundo bug de qualquer forma, e ainda carregaria o risco real de regressão no `mobile_scanner`
registrado antes — descartado. Vendoring evita os dois problemas de uma vez, sem tocar em nenhuma
API (Coinbase Wallet continua funcional no modal do WalletConnect, ao contrário de um stub) e sem
mexer no grafo de dependências do resto do app. **Achado de processo, não do código**: o primeiro
rebuild pós-patch falhou com `cannot find symbol: class CoinbaseWalletSdkFlutterPlugin` mesmo com o
`.kt` compilando sem erro — causa era cache do Gradle (`gradle_cache` persistido no volume Docker)
com estado inconsistente do módulo apontando pro path antigo (pub cache) vs. o novo (`third_party/`);
`./gradlew clean` + rebuild completo resolveu — não deve se repetir num runner de CI real, que
sempre começa limpo. **Validado nesta sessão**: `flutter build apk --release` via
`./gradlew :app:assembleRelease` limpo → `BUILD SUCCESSFUL`, APK real gerado (91MB) em
`build/app/outputs/flutter-apk/app-release.apk`; `flutter analyze` limpo (só os 13 infos
pré-existentes, nada novo); `flutter test --concurrency=1 test/` → **702/702 passando, 2 pulados
(`arlocal`), `All tests passed!`**. **Confirmado no CI real (mesma sessão)**: bump só do build number do Mobile (`2.2.0+3`→`2.2.0+4`,
Desktop ficou intocado em `2.2.0` de propósito, pra não disparar o auto-update do P85 à toa pra quem
já tem o Desktop instalado) + tag `v2.2.1` cortada e empurrada. `Build Mobile` (runner Ubuntu real,
sem o cache Docker local que causou o achado de processo) **passou** (`success`, 9m26s); `Build`
(Desktop) também passou (`success`, 12m29s, esperado — sem mudança de código lá). Release draft
`v2.2.1` no ar com `app-release.apk` de verdade entre os assets. **Dono do projeto decidiu publicar**
— release `v2.2.1` publicado de verdade (deixou de ser draft), `.apk` confirmado ao vivo via `curl`
(HTTP 200), `site/frontend/lib/releases.ts` atualizado apontando `android.apk` pra `v2.2.1` (URL
separada de `TAG`, que continua em `v2.2.0` — Desktop/extensão não mudaram, só o Mobile saiu num
release à parte desta vez). **P86 fechado por completo.**

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P86 | Cortando a tag `v2.2.0` (primeira tentativa de release do Mobile desde que o P68 — fluxo 100% mobile via WalletConnect/`reown_appkit` — entrou), `build-mobile.yml` **falhou** em `assembleRelease`: `checkReleaseAarMetadata` rejeita o build porque `coinbase_wallet_sdk` (dependência transitiva do `reown_appkit`, puxada porque ele oferece "Coinbase Wallet" como opção no modal do WalletConnect) foi publicado com `compileSdk = 31` e é incompatível com ~20 bibliotecas androidx mais novas presentes no grafo de dependências (`androidx.fragment:1.7.1`, `androidx.window:1.2.0`, etc. — todas exigindo `compileSdk >= 33/34`). **Confirmado que não é fix rápido**: (1) `coinbase_wallet_sdk` está parado em `1.0.10` há ~23 meses no pub.dev, sem correção disponível; (2) `reown_appkit` foi checado até a versão mais recente (1.9.0, changelog completo revisado) — a dependência nunca foi removida nem atualizada, e a 1.8.3 até **reforçou** a integração com Coinbase (exchange flow), então não vai sumir sozinho; (3) excluir o módulo só no Gradle não funciona de forma limpa — o Flutter gera `GeneratedPluginRegistrant` referenciando a classe Android do plugin porque o `reown_appkit` importa o pacote Dart de verdade, então um exclude Gradle puro troca "falha de metadata" por "símbolo não resolvido"; (4) `compileSdk = 36` no próprio app (já aplicado em `mobile/android/app/build.gradle.kts`, mantido — é o valor recomendado pela própria mensagem de erro e não tem custo) **não resolve sozinho**, porque o problema é o `compileSdk` do PRÓPRIO `coinbase_wallet_sdk` (imutável, travado no artefato já publicado), não do app consumidor. **Fix real identificado, não tentado**: forçar as ~20 dependências androidx conflitantes pra versões mais antigas compatíveis com `compileSdk 31` (`resolutionStrategy.force` no Gradle) — mas há risco concreto de regressão no `mobile_scanner` (scan de QR, feature real e usada), que provavelmente é quem está puxando pelo menos parte dessas versões mais novas pro grafo; sem hardware físico pra validar scan de QR depois do downgrade, o risco de quebrar em runtime sem detectar aqui é real. Alternativa mais segura mas de mais esforço: publicar um pacote Dart "stub" (mesma API pública do `coinbase_wallet_sdk`, sem implementação Android real) via `dependency_overrides`, pra satisfazer o import do `reown_appkit` sem carregar o módulo nativo quebrado — não tentado, precisa mapear a superfície de API que o `reown_appkit` realmente chama. **Decisão do dono do projeto**: publicar o release `v2.2.0` só com Desktop por enquanto (assets 100% prontos e assinados); Mobile fica de fora até este item ser corrigido com calma, numa sessão dedicada com tempo pra validar. | conversa direta, tentativa real de cortar release (Sessão 227) | 🟠 Média — bloqueia lançar Mobile em qualquer release futura até corrigir; Desktop não é afetado |

### P85 — Auto-update real no Desktop via `tauri-plugin-updater` — implementado, release `v2.2.0` cortado (Desktop-only), falta confirmar o fluxo completo numa instalação antiga (Sessão 227)

**Atualização**: tag `v2.2.0` cortada de verdade. Achado no processo: o input `uploadUpdaterJson` (citado abaixo) **estava com o nome errado** — o real é `includeUpdaterJson` (corrigido em `build.yml`); como o default já é `true`, o `latest.json` saiu certo mesmo assim (confirmado via `gh release view`: `latest.json` + `.sig` em todos os binários de update). `Build` (Desktop, 3 SOs + extensão) passou 100%; `Build Mobile` **falhou** por um problema real e não relacionado (`coinbase_wallet_sdk` travado em `compileSdk 31` — ver P86, novo item). Decisão do dono do projeto: publicar `v2.2.0` só com os assets do Desktop, Mobile fica de fora até P86 ser corrigido.

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P85 | Dono do projeto perguntou se dava pra automatizar a distribuição (apt + "atualiza sozinho quando builda no GitHub?"). Investigação confirmou: apt já é real (repo publicado, `apt install truth-id` funciona), mas **não havia auto-update nenhum** — só um checador manual (`useUpdateCheck.ts`) comparando com `releases/latest` e mostrando um link pra baixar na mão; o build do CI também só dispara em tag `v*` + release fica draft até publicação manual (isso continua igual, é o fluxo certo). Escopo escolhido pelo dono do projeto: Windows + macOS + **Linux via AppImage** (não `.deb` — pacote de sistema gerenciado pelo `apt` não pode ser autossubstituído por dentro do app; achado durante a implementação: o AppImage e o `.app.tar.gz` do macOS **já existiam** nos releases publicados, `bundle.targets: "all"` já builda os 2, sem mudança de packaging necessária). **Implementado**: `tauri-plugin-updater`+`tauri-plugin-process` (Rust, guard `#[cfg(desktop)]`/`cfg(any(macos, windows, linux))`, mobile nem builda hoje) e os pacotes JS equivalentes; `tauri.conf.json` ganhou `bundle.createUpdaterArtifacts: true` + `plugins.updater` (pubkey embutida, endpoint `https://github.com/masterlxz/truthid/releases/latest/download/latest.json`, `windows.installMode: "passive"`); capabilities ganharam `updater:default`/`process:allow-restart`; `useUpdateCheck.ts` reescrito pra usar `check()`/`downloadAndInstall()` de verdade (máquina de estados `idle→available→downloading→ready/error`, `restartNow()` separado de `installUpdate()` de propósito — reiniciar fecha trabalho em andamento, tipo assinar uma identidade, então quem usa decide o momento); banner em `App.tsx` e i18n (4 locales) atualizados pros novos estados. `build.yml` ganhou `TAURI_SIGNING_PRIVATE_KEY`/`_PASSWORD` (secrets novos no repo) + `includeUpdaterJson: true` explícito. **Par de chaves de assinatura gerado nesta sessão** (`tauri signer generate --ci`) — a pública foi embutida no `tauri.conf.json` (committada, é pra ser pública); a privada + senha foram direto pros secrets do GitHub (`gh secret set`) e a cópia local fica em `~/.truthid-secrets/tauri-updater-key/` (fora do repo git, nunca commitada) — **⚠️ fazer backup dessa pasta em algum lugar durável (gerenciador de senhas), perder a chave privada quebra a cadeia de confiança do updater pra sempre, mesmo problema já registrado pra chave GPG do apt em `ROADMAP.md`**. Validado nesta sessão: `npx tsc --noEmit` limpo, `npx vitest run` 185/185 (6 testes novos em `useUpdateCheck.test.ts`), `cargo check`/`cargo test --lib` (233/233, 6 ignorados, pré-existente) / `cargo clippy --lib` (só o warning pré-existente de `vault.rs`) limpos, e o build real do CI (`v2.2.0`) confirmou os artefatos assinados de verdade. **O que ainda falta**: confirmar que uma instalação de uma versão anterior detecta, baixa, instala e reinicia sozinha — só dá pra testar isso com o release publicado e uma instalação real rodando. | conversa direta (Sessão 227) | 🟡 Média — funcionalidade nova, sem usuários dependendo dela ainda; validar no próximo ciclo |

### P82 — `/code-review` do P68 (fluxo 100% mobile): ✅ FECHADO por completo — (1)-(10) corrigidos e suite completa validada via Docker (Sessão 227)

**Atualização 2 (Sessão 227, mesma sessão)**: os 7 achados de limpeza/duplicação restantes, (4)-(10),
também foram corrigidos, a pedido do dono do projeto.
- **(4) `connectWallet(BuildContext)`/`_waitForReceipt` deduplicados**: novo
  `mixin WalletConnectFlowMixin<Step>` (`lib/utils/wallet_connect_flow_mixin.dart`) com os 2 métodos
  compartilhados pelos 4 flows (`CreateIdentityFlow`, `PairDeviceFlow`, `ConfigureGuardiansFlow`,
  `ActAsGuardianFlow`) — cada classe passou a `with WalletConnectFlowMixin<XStep>`, expondo só o que a
  mixin precisa (getter/setter de `busy`, `connectingWalletStep`/`formStep`/`setStep`); `errorMessage`/
  `connectedAddress`/`walletConnect`/`blockchain` já eram campos públicos, satisfazem os
  getters/setters abstratos da mixin sem boilerplate extra. `_waitForReceipt` virou `waitForReceipt`
  (público, chamado de fora da classe que o originou). Comportamento idêntico nas 4 classes — mesma
  guarda síncrona de `busy`, mesmo timeout/poll de 5min/2s. Cobertura de regressão: os testes
  existentes das 4 telas já exercitam `connectWallet`/`waitForReceipt` de ponta a ponta contra a nova
  implementação (nenhum teste precisou mudar).
- **(5) `identity_consent_hash.dart` não reimplementa mais o encoding ABI de `BlockchainService`**:
  novo módulo `lib/utils/abi_encoding.dart` com `uint256Bytes`/`addressBytes` públicos —
  `identity_consent_hash.dart` importa direto; `BlockchainService._uint256Bytes`/
  `_uint256BytesFromBigInt`/`_addressBytes` viraram wrappers de 1 linha delegando pro módulo
  compartilhado, mantendo os ~30 call sites internos intactos (nomes/assinaturas privados
  preservados, só a implementação mudou). Teste novo dedicado (`abi_encoding_test.dart`) + os vetores
  de hash conhecidos de `identity_consent_hash_test.dart` continuam validando o mesmo algoritmo.
- **(6) `eth_amount.dart`/`wallet_screen.dart` não duplicam mais o parsing decimal↔wei**:
  `eth_amount.dart` ganhou `weiToDecimalString` (movida de `wallet_screen.dart`, código idêntico);
  `wallet_screen.dart._parseEtherToWei` manteve sua validação por regex (nullable, sem negativo,
  sem `.` sem dígito antes) mas delega o cálculo pra `eth_amount.parseEthToWei` — equivalência
  verificada campo a campo antes da troca (mesma aceitação, mesmo resultado). Testes novos em
  `eth_amount_test.dart` pra `weiToDecimalString` (incl. round-trip com `parseEthToWei`).
- **(7) `random_bytes.dart` agora é a única fonte do salt de 32 bytes**: os 2 call-sites que a
  motivaram (`arweave_transaction.dart:158`, `arweave_isolate.dart:39`) foram migrados pra chamar
  `randomBytes32()` (o comentário de `arweave_isolate.dart` sobre o salt precisar ser gerado *dentro*
  do isolate continua válido — a chamada ainda executa lá dentro, só a função mudou de arquivo); os 2
  `import 'dart:math'` ficaram sem uso e foram removidos. De brinde, `randomBytes32()` passou a
  instanciar `Random.secure()` 1x fora do loop, não 32x dentro do `List.generate`.
- **(8) `wallet_screen.dart._loadArweaveWallet` busca saldo e histórico em paralelo**: os 2 `await`
  sequenciais viraram `Future.wait([...])` — as 2 chamadas só dependem de `address`, cada uma já é
  best-effort com seu próprio try/catch interno, sem ordem que importe.
- **(9) `configure_guardians_screen.dart.submit` busca identidade e proposta em paralelo**: quando
  `controller` ainda não está em cache, `getIdentityByUsername`/`getProposal` agora disparam juntos
  (`Future.wait` implícito via 2 futures independentes) em vez de sequencial — preserva o cache de
  `controller` (só busca de novo se `null`) e o erro cedo se o controller não resolver.
- **(10) comentário desatualizado em `blockchain_service.dart` (perto de `RecoveryProposal`)
  corrigido**: não diz mais que ação de guardião em nome de outra identidade é "exclusivo do
  Desktop" — `configure_guardians_screen.dart`/`act_as_guardian_screen.dart` já fazem isso pelo
  Mobile (P68 fatias 2/3); só cancelar proposta e revogar device seguem fora de escopo (P75).
- Sem `flutter`/`dart` instalados nesta máquina pra rodar `flutter test`/`flutter analyze` (mesma
  limitação de P62, já registrada na atualização anterior) — mas o dono do projeto lembrou que os
  testes rodam via Docker (`docker compose run --rm flutter ...`, imagem já configurada em
  `mobile/Dockerfile`/`docker-compose.yml`), sem precisar instalar Flutter local. Rodado de verdade
  nesta sessão: `flutter analyze` limpo (13 infos pré-existentes, nada relacionado); `flutter test
  --concurrency=1 --reporter expanded test/` — **702/702 testes passando, 2 pulados (tag `arlocal`,
  precisam de nó Arweave local via `npx arlocal`, não disponível neste ambiente), 0 falhas**, nos 76
  arquivos de teste do projeto, incluindo os 8 tocados nesta sessão (os 4 flows da mixin, os 2 utils
  novos, `wallet_screen_test.dart`, `blockchain_service_calldata_test.dart` — este último confirma
  que os ~30 call sites de `_uint256Bytes`/`_addressBytes`/`_uint256BytesFromBigInt` continuam
  produzindo bytes idênticos aos vetores gerados via `viem`). Achado de processo: com concorrência
  padrão (workers em paralelo) o reporter do `package:test` perde/atropela linhas de log de arquivos
  rápidos no output — mesma contagem final (`702/702`), mas o log fica ilegível pra auditoria por
  arquivo; `--concurrency=1` resolve isso e mostra os 76 arquivos individualmente. **P82 está fechado
  e validado de verdade — 10/10 achados corrigidos, suite completa rodada e verde.**

**Atualização 1 (Sessão 227)**: os 2 bugs reais de correção e o risco de hang foram corrigidos, a
pedido do dono do projeto (achados (4)-(10), de limpeza/duplicação, ficaram de fora desta rodada
inicial — corrigidos na Atualização 2, mesma sessão).
- **(1) `act_as_guardian_screen.dart`** — `_usernameController` ganhou um listener
  (`addListener`/`removeListener` em `initState`/`dispose`) que chama `setState` a cada digitação,
  igual ao padrão já usado pra reagir a `flow.onChange`; o botão "Look up" agora habilita/desabilita
  em tempo real. Teste de regressão novo (`act_as_guardian_screen_test.dart`, primeiro `testWidgets`
  do arquivo — os demais só testavam `ActAsGuardianFlow` puro): pumpa a screen de verdade com
  `wrapForTest` (helper de l10n já usado noutras telas), digita no campo e confere `onPressed` do
  botão antes/depois.
- **(2) `create_identity_screen.dart`** — `checkAvailability()` ganhou `catch` (novo valor de enum
  `UsernameAvailability.error`, seta `errorMessage` e retorna) em vez de deixar a exceção escapar;
  `_handleCreate()` trata o caso `.error` (não precisa fazer nada — o `errorMessage` já aparece no
  bloco de erro existente do `build()`, e o widget já reconstruiu via `flow.onChange` antes do
  `_handleCreate` continuar). 2 testes novos: falha de RPC vira `.error` + `errorMessage` sem exceção
  escapando; guarda libera depois do erro e uma 2ª tentativa roda de verdade (mesmo padrão já usado
  no teste equivalente de `createIdentity`).
- **(3) `wallet_connect_service.dart`** — não foi possível confirmar 100% se `reown_appkit` sempre
  dispara `onModalConnect`/`onModalError` ao fechar a modal (pacote não baixado nesta checkout, sem
  `flutter pub get` rodado — mesma limitação já registrada em P62), então a correção é defensiva:
  `openConnectModal()` ganhou um `timeout` (padrão 5min, mesmo valor de `_waitForReceipt`) sobre o
  `Completer`, fecha a inscrição nos eventos e lança um erro claro se nenhum dos dois disparar a
  tempo — `connectWallet()` já captura qualquer exceção e libera `_busy`, então o timeout se propaga
  pro caminho de erro existente sem mudança adicional. Sem teste novo dedicado (pacote 3rd-party não
  é mockado em nenhum teste existente do projeto; adicionar cobertura exigiria simular
  `ReownAppKitModal` inteiro, fora de escopo desta correção pontual).
- Sem `flutter`/`dart` instalados nesta máquina pra rodar `flutter test`/`flutter analyze` (mesma
  limitação de P62) — mudanças revisadas manualmente linha a linha, chaves/parênteses conferidos por
  script; falta rodar o test suite numa máquina com Flutter antes de considerar 100% validado.

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P82 | `/code-review` de alto esforço sobre `git diff 4449c12 678fd5e -- mobile/` (as 3 fatias do P68). 8 ângulos rodados (7 aplicáveis — convenções pulado, sem CLAUDE.md cobrindo `mobile/`), todos os candidatos passados por verificação manual linha-a-linha contra o código real (não só o diff). **Achados de correção (CONFIRMADOS)**: **(1) `act_as_guardian_screen.dart:319`** — o `TextField` do username não tem `onChanged` nem nada mais escuta `_usernameController`; o botão "Look Up" (linha 330) lê `_usernameController.text.trim().isEmpty` direto no `build()`, mas só reconstrói quando `_flow.onChange()` dispara (eventos do fluxo, não digitação) — resultado: o botão fica preso no estado da última reconstrução (desabilitado, campo vazio) e digitar um username **não o habilita nunca**, quebrando a funcionalidade de buscar outra identidade pra agir como guardião. **(2) `create_identity_screen.dart:104`** — `checkAvailability()` tem `try/finally` sem `catch`; as 3 chamadas RPC internas (`predictSmartAccountAddress`/`getUsernameByController`/`isUsernameTaken`) também não capturam erro; o único chamador, `_handleCreate()` (linha 301), também não tem try/catch. Numa falha de RPC (rede instável, RPC fora do ar), a exceção escapa sem tratamento: o spinner some (`finally` reseta `_busy`) mas nenhuma mensagem de erro aparece — usuário fica sem saber que a tentativa falhou, inconsistente com `connectWallet`/`createIdentity` no mesmo arquivo que corretamente capturam e mostram `errorMessage`. Não coberto por teste (`create_identity_screen_test.dart` só testa os caminhos taken/alreadyHasIdentity/available, nunca uma exceção). **Risco plausível, confiança menor**: **(3) `wallet_connect_service.dart:60`** — `openConnectModal()` só resolve via `onModalConnect`/`onModalError`; sem timeout e sem handler pro caso da modal ser fechada (botão voltar/tap fora) sem nenhum dos dois disparar — se o pacote `reown_appkit` permitir esse caminho (não foi possível inspecionar o source do pacote nesta checkout pra confirmar 100%), `connectWallet()` trava pra sempre com `_busy` preso em `true` nas 4 telas novas, sem jeito de tentar de novo sem sair da tela. **Achados de limpeza (todos CONFIRMADOS)**: **(4)** `connectWallet(BuildContext)` e `_waitForReceipt` (polling de recibo) copiados byte-a-byte nas 4 telas novas (`create_identity_screen.dart:82/244`, `pair_device_screen.dart:108/233`, `configure_guardians_screen.dart:58/132`, `act_as_guardian_screen.dart:86/216`) — 3 ângulos independentes bateram na mesma duplicação; falta um mixin/base class compartilhado. **(5)** `identity_consent_hash.dart:44/53` reimplementa `_uint256Bytes`/`_addressBytes` que já existem como privados em `blockchain_service.dart` — risco concreto porque este mesmo PR já teve 2 bugs reais de offset ABI (ordem dos slots do `registerDevice`, offset do `executeBatch`, ambos da fatia 2). **(6)** `mobile/lib/utils/eth_amount.dart::parseEthToWei` reimplementa o parsing decimal→wei que já existe como `_parseEtherToWei`/`_weiToDecimalString` em `wallet_screen.dart:1110/1129` (confirmado, pré-existente). **(7)** `mobile/lib/utils/random_bytes.dart` foi criado citando 2 call-sites existentes (`arweave_transaction.dart:158`, `arweave_isolate.dart:39`) como motivo da extração, mas **nenhum dos 2 foi atualizado pra usar o helper novo** — confirmado, ainda inline nos dois — ficaram 3 cópias do mesmo gerador em vez de 1; o helper novo também instancia `Random.secure()` dentro do `List.generate` (32× por chamada em vez de 1×). **(8)** `wallet_screen.dart:142-153` (`_loadArweaveWallet`) busca balanço e histórico Arweave em 2 `await` sequenciais em vez de `Future.wait` (inconsistente com `act_as_guardian_screen.dart`, que já usa `Future.wait` pra 3 leituras no mesmo PR). **(9)** `configure_guardians_screen.dart:86-93` (`submit`) busca `getIdentityByUsername` e `getProposal` em sequência quando os dois só dependem do `username`, sem depender um do outro. **(10)** comentário em `blockchain_service.dart:1196-1200` (perto de `RecoveryProposal`) ficou desatualizado dizendo que ações de guardião em nome de outra identidade são "exclusivo do Desktop... fora de escopo desta rodada (ver P75)" — mas `act_as_guardian_screen.dart` (fatia 3, já implementada no mesmo intervalo revisado) faz exatamente isso pelo Mobile; risco real de alguém confiar no comentário em vez do código. **Sem achados no ângulo B** (comportamento removido) — a exclusão de `arweave_wallet_screen.dart` (170 linhas) foi confirmada como port 1:1 pra dentro de `wallet_screen.dart` sem perder guard/try-catch nenhum. Nada corrigido ainda — só revisão, registrando os achados pro dono do projeto decidir prioridade. | `/code-review` Sessão 226 | 🟠 Média — (1) e (2) são bugs reais em fluxos já fechados como "prontos" (P68 fatia 2/1); (3) é risco não confirmado; resto é dívida de duplicação/eficiência, sem urgência |

### P83 — ✅ Resolvida (Sessão 226): `/code-review high desktop/` rodado, achados registrados em P84

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P83 | Sessão 226 rodou `/code-review high mobile/` (achados em P82), mas **nenhum `/code-review` do Desktop chegou a rodar** — a recomendação original era rodar os dois juntos (`/code-review high desktop/ mobile/`), só o Mobile foi disparado de fato. Acumulou feature nova sem revisão desde a última passada de `/code-review` no Desktop: P78 pedaço 1 (wallet local embutida), P78 pedaço 2 (migrar identidade pra outra wallet — `executeBatch` com 3 chamadas + fluxo de reconectar 2 wallets diferentes), e a tela de Configurações + bloqueio do app por senha (`app_lock.rs` novo, reaproveitando `backup::encrypt`/`decrypt`). **Rodado nesta mesma sessão** (`/code-review high desktop/`) — achados em [[P84]]. | conversa direta (Sessão 226) | ✅ Resolvida |

### P84 — `/code-review` do Desktop (P78 pedaço 1/2 + Configurações/senha): ✅ FECHADO por completo — 2 travamentos totais + 6 bugs de correção + 2 achados de reuso, todos corrigidos (Sessão 226)

**Atualização 3 (Sessão 226, mesma sessão)**: os itens **(9) e (10)** (duplicação de código) também
foram corrigidos, a pedido do dono do projeto ("pode seguir com o 9 e 10").

- **(9) padrão keyring→arquivo→trata-vazio-como-ausente deduplicado**: novo par
  `config::get_keyring_or_file`/`config::set_keyring_or_file` (Rust) — o primeiro retorna `Ok(None)`
  quando não há segredo em nenhum dos dois lugares (erro de "não encontrado" fica a cargo de cada
  chamador, as mensagens variam), um erro de I/O real lendo o arquivo de fallback ainda propaga como
  `Err`. Usado agora por `get_device_key_hex`/`get_arweave_wallet`/`set_arweave_wallet`/`set_vault_key`
  (`lib.rs`), `local_wallet::get_local_wallet_key_hex`/`set_local_wallet_key_hex` e
  `app_lock::get_app_lock_blob_hex`/`set_app_lock_blob_hex`. `get_vault_key`/`get_current_vault_key_strict`
  ficaram de fora de propósito — decodificam pra `[u8; 32]` com validação de tamanho e têm um 3º nível
  de fallback legado, forma estrutural diferente o bastante pra não caber no helper genérico sem
  mudar comportamento. **Achado colateral real**: `get_device_key_hex` nunca tinha o cuidado do P71 de
  tratar uma entrada de keyring vazia como ausente (só `get_arweave_wallet` tinha); unificar via
  `get_keyring_or_file` deu esse cuidado de graça pra device key também — e isso **corrigiu de vez**
  a falha ambiental que já vinha sendo reproduzida (e confirmada pré-existente, sem relação com nenhum
  fix desta sessão) desde a Sessão 226 4ª parte: `local_signer_server::sign_message_endpoint_parks_until_resolved_and_returns_signature`
  panicava porque a device key nesta máquina tinha uma entrada de keyring vazia mascarando o fallback
  em arquivo. `cargo test --lib` foi de 232/233 pra **233/233**.
- **(10) duplicação entre os 3 conectores EVM deduplicada**: novo módulo `connectors/evmProviderShared.ts`
  com `parseRsvSignature` (substitui `parseLedgerSignature`/`parseTrezorSignature`/
  `parseLocalWalletSignature`, corpo idêntico), `toError`, `unsupportedMethod`, `rpcFallbackRequest`
  (o loop de fallback entre RPCs, sem nada específico de wallet) e `createEvmProviderRequest` (monta o
  `request` inteiro do provider EIP-1193 — eth_chainId/eth_accounts/eth_sendTransaction/personal_sign
  + fallback de RPC pro resto — parametrizado só no que cada conector faz diferente: como assina
  transação/mensagem, o nome de exibição da wallet e a mensagem exata de "não conectado", que os 3
  conectores têm cada um a sua, com uma inconsistência de capitalização pré-existente preservada de
  propósito ("Local wallet not connected." vs "Ledger not connected."/"Trezor not connected." — os
  testes de `localWallet.test.ts`/`trezor.test.ts` checam essas strings ao pé da letra). `ledger.ts`
  caiu de 224 pra 133 linhas, `trezor.ts` de 225 pra 140, `localWallet.ts` de 208 pra 122 — o novo
  compartilhado tem 141, líquido de -121 linhas nos 3 arquivos somados. Nenhum teste existente
  precisou mudar (os 3 arquivos de teste de conector já testavam só o comportamento público da
  instância, não os nomes das funções internas).
- `cargo test --lib` (233/233, 0 falhas — a falha ambiental de sempre sumiu como efeito colateral do
  #9)/`cargo clippy --lib` (só o warning pré-existente de `type_complexity` em `vault.rs`)/
  `npx vitest run` (179/179, nenhum teste novo — refatoração pura, comportamento idêntico)/
  `tsc --noEmit` limpos. Sem validação manual real — mesma limitação já registrada em P79/P80/P81
  (mas este item é refatoração interna, sem superfície de UI nova pra clicar).

**Atualização 2 (Sessão 226, mesma sessão)**: os itens **(4)-(8)** também foram corrigidos, a pedido
do dono do projeto ("recomenda tudo de uma vez?" → sim pros 5 juntos), depois de um `/plan` dedicado
com 3 explorações + 1 validação de plano em paralelo.

- **(4) Settings/MigrateWallet — fechar Configurações não pode mais derrubar uma migração em
  andamento**: `MigrateWallet.tsx` ganhou prop `onBusyChange?` que reporta `phase === "confirming"`;
  `Settings.tsx` repassa via `onMigrationBusyChange?`; `App.tsx` guarda o backdrop/✕ do modal de
  Configurações (`!settingsBusy &&`) e desabilita o ✕ enquanto uma migração está em voo. Sem toque em
  `Settings.test.tsx`/`MigrateWallet.test.tsx` existentes (props novas são opcionais).
- **(5) MigrateWallet — saldo não fica mais órfão com RPC lento**: efeito de envio do `executeBatch`
  agora espera `!isOldBalanceLoading` (novo campo destructurado de `useBalance`) antes de decidir
  `balanceToMove`, em vez de aceitar um `oldBalance` ainda não carregado como zero silencioso.
- **(6) `local_wallet_generate` — corrida de duplo-clique fechada nas duas pontas**: Rust ganhou
  `LOCAL_WALLET_MUTEX`/`lock_local_wallet()` (mesmo padrão de `VAULT_MUTEX`/`lock_vault` em
  `vault.rs`) guardando o check-then-act; `ConnectLocalWallet.tsx` ganhou `creating` desabilitando o
  botão "Create local wallet" durante a chamada (sem troca de texto/i18n — janela de corrida
  sub-perceptível, só cifra local, sem round-trip de rede).
- **(7) panic → `Err` em chave de tamanho errado**: `derive_address`/`sign_digest_recoverable`
  trocaram `SigningKey::from_bytes(bytes.into())` (que faz `assert_eq!`/panic em `GenericArray` pra
  tamanho ≠32) por `SigningKey::from_slice(bytes)`, que já existe no mesmo crate e retorna `Err`
  limpo pra qualquer tamanho fora do aceito (confirmado: 24-31 bytes viram zero-pad intencional da
  lib, não um bug — testes novos usam 16 e 40 bytes, fora desse intervalo, pra exercitar rejeição de
  verdade).
- **(8) `vault_import_backup` não mantém mais silenciosamente uma chave local errada**: nova função
  pura `local_wallet_keys_conflict` (deriva e compara endereços das duas chaves) roda **antes** de
  qualquer mutação (reordenado pra antes do bloco da wallet Arweave, senão um abort deixaria estado
  parcial); se o device já tem uma chave local diferente da importada, o import inteiro é abortado com
  erro claro em vez de sobrescrever o vault por baixo mantendo a chave errada. Também fecha de quebra
  parte do gap de validação do #7 — a chave importada agora é validada via `derive_address` mesmo
  quando não há conflito (chave malformada vira `Err` limpo na hora do import, não mais só na hora de
  assinar depois).
- `cargo test --lib` (232/233, só a mesma falha ambiental pré-existente de
  `local_signer_server::sign_message_endpoint_...`, sem relação)/`cargo clippy --lib` (só o warning
  pré-existente de `type_complexity` em `vault.rs`)/`npx vitest run` (179/179, 3 testes novos)/
  `tsc --noEmit` limpos. Sem validação manual real — mesma limitação já registrada em P79/P80/P81.

**Atualização 1 (Sessão 226, mesma sessão)**: os itens **(1) e (2)** — os dois travamentos totais do
app sem recovery — foram corrigidos, a pedido do dono do projeto ("pode começar pelos dois"), depois
de um `/plan` dedicado.

- **(1) reconectar wallet local**: `ConnectLocalWallet.tsx` agora checa `local_wallet_exists()` no
  mount (nova fase `"checking"`); se já existe chave, mostra uma tela nova (`"existing"`) com o
  endereço (via `local_wallet_address()`, já existente) e um botão "Conectar" que chama
  `connectAsync({connector: localWallet})` direto — nunca mais toca em `local_wallet_generate`. Se a
  checagem falhar, cai pro fluxo de criação de sempre (fail-open). Nenhuma mudança no Rust foi
  necessária (`local_wallet_exists`/`local_wallet_address` já existiam e já suportavam isso).
- **(2) recovery do bloqueio por senha**: novo comando Tauri `app_lock_reset` (wrapper público sobre
  `clear_app_lock_blob`, que já existia mas era privada) — remove o gate sem exigir senha (não há uma
  alternativa pra checar; só desliga o gate de abertura, não toca no Vault/identidade, confirmado lendo
  `create_blob`/`verify_blob`: cifram só um marcador fixo). `useAppLock.ts` ganhou `reset()`.
  `AppLockGate.tsx` ganhou: try/catch em volta de `onUnlock` (erro inesperado agora mostra mensagem
  distinta de "senha errada" e sempre reabilita o botão — fecha o bug real do "Verificando..." travado
  pra sempre); link "Esqueceu sua senha?" com confirmação de um clique antes de chamar `onReset`.
- i18n: novas keys em `appLockGate.json`/`connectLocalWallet.json` traduzidas nos 4 locales
  (`pt-BR`/`en`/`es`/`zh-CN`).
- Testes novos/atualizados: `ConnectLocalWallet.test.tsx` (+4 testes, 4 existentes ajustados pro mount
  check novo), `AppLockGate.test.tsx` (+4 testes), `useAppLock.test.ts` (arquivo novo, 6 testes).
  `cargo test --lib` (só 1 falha, `local_signer_server::sign_message_endpoint_...`, confirmada
  pré-existente e sem relação — reproduzida idêntica numa worktree limpa do commit anterior, causa
  ambiental desta máquina, não deste fix)/`cargo clippy --lib` (só o warning pré-existente de
  `type_complexity` em `vault.rs`, sem relação)/`npx vitest run` (176/176)/`tsc --noEmit` limpos.
  Sem validação manual real (clique de verdade) — mesma limitação já registrada em P79/P81.

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P84 | `/code-review high desktop/` sobre o diff das 3 features nunca revisadas (wallet local embutida, migrar identidade pra outra wallet, Configurações+bloqueio por senha — commits `a1bd2e5~1..9d195d5`). 4 ângulos rodados em paralelo (Rust, TS, limpeza/reuso/eficiência, altitude/convenções), todos os candidatos verificados linha-a-linha contra o código real. **Travamento total do app (as 2 mais graves)**: **(1) `ConnectLocalWallet.tsx:54`** — não existe caminho pra *reconectar* numa wallet local já criada, só criar uma nova; `local_wallet_generate()` (`local_wallet.rs:116-119`) erra com "já existe uma wallet local neste device" sempre que já há chave, e como `wagmi.ts` tem `storage: null` (nunca reconecta sozinho), qualquer reinício do app deixa quem criou identidade via wallet local sem jeito de voltar a assinar como aquela identidade — nunca mais. **(2) `app_lock.rs:113`** — o bloqueio por senha não tem NENHUM comando de reset/recovery, só enable/verify/disable, e disable exige a senha atual; se o usuário esquece a senha, ou o blob armazenado corrompe (escrita não-atômica, crash/disco cheio), `app_lock_verify` nunca mais aceita nada e, como o `AppLockGate` é a única porta de entrada quando o bloqueio está ativo (sem fechar/escape), o usuário fica trancado fora do app inteiro pra sempre — só resolve apagando arquivo/keyring manualmente por fora do app. **Bugs de correção (CONFIRMADOS)**: **(3) `useAppLock.ts:20`/`AppLockGate`** — `unlock()`/`handleSubmit` sem try/catch; se `app_lock_verify` rejeitar (erro transiente de IPC), o formulário trava em "Checking..." pra sempre, sem outra porta de entrada. **(4) `Settings.tsx:74`** — passa o próprio `onClose` pro `MigrateWallet` embutido; antes o `MigrateWallet` tinha modal próprio, agora Cancel nas telas de intro/review (ou o backdrop/✕ do Settings) fecha a tela de Configurações inteira e pode desmontar o `MigrateWallet` em pleno `phase: 'confirming'`, perdendo o estado local da migração/transação. **(5) `MigrateWallet.tsx:134`** — usa `oldBalance?.value ?? 0n` sem checar se a query de saldo já resolveu; com RPC lento, se o saldo ainda não carregou na hora de "confirming", `balanceToMove` vira 0 silenciosamente, a migração completa (dono+controller transferidos) mas o saldo antigo fica órfão na conta velha. **(6) `local_wallet.rs:116`** — trava anti-sobrescrita é check-then-act sem lock e o botão do frontend não desabilita durante a chamada; duplo-clique pode gerar 2 chaves diferentes concorrentes, a última escrita vence silenciosamente e quem recebeu o outro endereço fica com endereço que não bate com a chave persistida. **(7) `local_wallet.rs:67`** — conversão de bytes pra `GenericArray` via `.into()` dá panic (não `Err`) se o slice não tiver exatamente 32 bytes; escrita não-atômica da chave (crash/disco cheio) ou payload de import sem validação de tamanho/hex podem deixar chave de tamanho errado, e todo comando de wallet local subsequente panica dentro do comando Tauri. **(8) `lib.rs:961`** — `vault_import_backup` só restaura a chave de wallet local do backup importado se o device não tiver NENHUMA chave local ainda; um device com chave local não-relacionada (sobra de setup abortado) que importa backup de outra identidade continua assinando silenciosamente com a chave errada, aparecendo depois como mismatch confuso de dono/assinatura on-chain. **Achados de reuso (CONFIRMADOS)**: **(9)** padrão "keyring do SO → fallback arquivo → trata string vazia como ausente" (P71) copiado uma 3ª/4ª vez em `local_wallet.rs`/`app_lock.rs` em vez de helper compartilhado (já existia em `lib.rs` pro Arweave/device key). **(10)** `connectors/localWallet.ts` duplica quase palavra-por-palavra `parseLedgerSignature`/`parseTrezorSignature` e os loops de fallback de RPC de `ledger.ts`/`trezor.ts` (~90 linhas quase idênticas em 3 arquivos). Nada corrigido ainda — só revisão, registrando os achados pro dono do projeto decidir prioridade. | `/code-review` Sessão 226 | 🔴 Alta — (1) e (2) são travamento total do app sem recovery em fluxos já fechados como "prontos" (P78 pedaço 1, Configurações); resto são bugs reais de correção ou dívida de duplicação |

### P65 — PR do winget: achado real de validação corrigido, CLA assinada, pipeline 10/10 verde (Sessão 220)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P65 | Manifest (`packaging/winget/masterlxz.TruthID.*.yaml`) escrito e validado contra o schema JSON v1.28.0 do `microsoft/winget-cli` na Sessão 218 (`jsonschema`/Python, os 3 arquivos vieram `VALID`). Fork `masterlxz/winget-pkgs` criado, arquivos commitados via GitHub Contents API (repo tem ~820MB, clone completo seria inviável), PR aberto: https://github.com/microsoft/winget-pkgs/pull/422612. **Achado real ao checar o pipeline de verdade (Sessão 220)**: o campo `ManifestVersion: 1.28.0` dentro dos 3 arquivos confundia a versão do *winget-cli* (a ferramenta) com a versão do *schema do manifesto* (campo interno, escala própria e bem menor) — o validador real do backend do winget-pkgs rejeitou com `SchemaVersionInvalid: The manifest version '1.28.0' is not supported. Supported versions: 1.0.0, 1.1.0, 1.2.0, 1.4.0, 1.5.0, 1.6.0, 1.9.0, 1.10.0, 1.12.0` (check "02. Manifest Validation", `fail`). A validação de schema da Sessão 218 nunca pegou isso porque validava a *estrutura* dos campos contra o schema certo, não o valor específico do campo `ManifestVersion` contra a lista de versões que o backend aceita — são checagens diferentes. **Corrigido**: `ManifestVersion` trocado pra `1.12.0` (a mais recente aceita) nos 3 arquivos + `$schema` dos comentários `yaml-language-server` atualizado pra bater; revalidado contra o schema JSON real da 1.12.0 baixado ao vivo (`jsonschema`, os 3 `VALID`); commitado no branch do fork (`masterlxz-truthid-2.0.0`) via GitHub Contents API, mesmo mecanismo da Sessão 218. **Reprocessamento confirmado**: "02. Manifest Validation" voltou `success` no novo commit; os checks seguintes (03-10) ficaram `queued`, esperando a CLA. **CLA assinada pelo dono do projeto na mesma sessão** (comentário real `@microsoft-github-policy-service agree`, confirmado `license/cla: success`). **Pipeline inteiro reprocessado e verde**: os 10 checks (`01. Pull Request Validation` até `10. Validation Completed`, incluindo `08. Installation Validation` — instala o `.msi` de verdade num runner Windows) vieram `success`; PR ganhou os labels `Azure-Pipeline-Passed`/`Validation-Completed`. **Pendência real que fica, fora do controle do dono do projeto**: falta só o review manual da comunidade/mantenedores do `winget-pkgs` pra mergear — padrão pra 1ª submissão de um pacote novo, sem prazo previsível. | conversa direta, achado real + fix + CLA + pipeline verde, tudo Sessão 220 | 🟡 Baixa — só acompanhar o review manual |

### P64 — publicação no AUR bloqueada: cadastro de conta novo pausado pelo próprio AUR (Sessão 217); achado técnico do `hidapi`/linker isolado e corrigido (Sessão 218)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P64 | `PKGBUILD` (`packaging/aur/PKGBUILD`) escrito, build-from-source (`npm ci`+`tauri build --bundles deb`, reaproveitando o `data.tar` do próprio bundler do Tauri em vez de um `.desktop`/ícone mantido à mão). **Bloqueado só pelo motivo externo agora**: publicar exige conta no AUR (`aur.archlinux.org/register`), que **está com cadastro temporariamente pausado pelo próprio AUR** ("dealing with a wave of automated account creation", sem prazo definido — pedem explicitamente pra não ficar tentando de novo em loop; **não script/automatizar retry contra essa página**). ~~O achado técnico do `hidapi`/linker (Sessão 217) ficava registrado como não resolvido, com o build falhando de forma inconsistente entre execuções idênticas.~~ **Causa raiz isolada e corrigida na Sessão 218**: o `OPTIONS=(...lto...)` global do `/etc/makepkg.conf` do Arch injeta `-flto=auto` no `CFLAGS` de todo build feito via `makepkg` — e a crate `cc` (usada pelo build script do `hidapi` pra compilar o `hid.c` vendorizado do backend `linux-static-hidraw`) lê essa variável de ambiente. Isso faz o GCC gerar um objeto LTO no formato GIMPLE (próprio do GCC), que o `ld.lld` (linker padrão do rustc no Arch) **não sabe processar** — LLD só entende bitcode LLVM, não o formato de LTO do GCC — daí *todos* os símbolos `hid_*` saírem como "undefined" no link final. `RUSTFLAGS="-C link-arg=-fuse-ld=bfd"` "funcionava" só porque o `collect2` do GCC carrega o plugin de LTO do binutils (`liblto_plugin.so`) pra recompilar o IR na hora do link quando o linker é `ld.bfd`/gold — uma dança via plugin frágil o bastante pra explicar a inconsistência observada (mesmo comando falhando na 2ª tentativa idêntica). **Reproduzido de forma determinística e isolada** num crate mínimo (`hidapi = "2"` sozinho, sem o resto do projeto): com `CFLAGS` no estilo do `makepkg.conf` (incluindo `-flto=auto`) e linker padrão (`lld`), falha 100% das vezes (link em segundos, iteração rápida); removendo só o `-flto=auto` do `CFLAGS`, sucesso 3/3. **Corrigido no `PKGBUILD`**: `options=('!lto')` (mecanismo padrão do `makepkg` pra desativar a injeção de LTO por pacote) substitui o `RUSTFLAGS`/`-fuse-ld=bfd` — remove a causa raiz em vez de contornar via linker alternativo, então não depende mais da fragilidade do plugin de LTO. **Validado de ponta a ponta 2× consecutivas** via `makepkg -f` limpo (reextração completa do `src/`, sem cache): as 2 rodadas produziram `TruthID_2.0.0_amd64.deb` e o pacote `truthid-2.0.0-1-x86_64.pkg.tar.zst` final sem erro; `nm` no binário final confirma as 20 funções `hid_*` presentes como código real (`T`), não só "build passou". `.SRCINFO` regenerado (`makepkg --printsrcinfo`). **Segue bloqueado só pelo motivo 1** (cadastro no AUR pausado) — quando o AUR reabrir, o build já está confiável pra publicar. | conversa direta (Sessão 217), causa raiz isolada e corrigida (Sessão 218) | 🟡 Baixa — bloqueado por decisão externa do AUR; achado técnico do `hidapi`/linker **fechado** |

### P56 — rotação de DEK ao revogar device nunca funcionou de verdade; corrigida a causa e a ordem insegura (Sessão 206)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P56 | **Achado investigando o P54, sem relação direta com a causa raiz dele**: `rotateVaultKeyOnRevoke.ts::buildRotationBatch` (disparada automaticamente depois de revogar um device com outros restantes ativos) cifrava a chave nova via ECIES usando `d.pubKey` — o **endereço Ethereum de 20 bytes** que `DeviceRegistry.sol` guarda pra cada device (o próprio contrato documenta a decisão: guarda o address derivado, não a chave bruta, pra economizar gas). `encrypt_vault_key_for_device` (Rust) exige uma chave pública secp256k1 crua de 33 ou 65 bytes — **rejeita qualquer endereço de 20 bytes**. Ou seja: toda vez que um device era revogado com ≥1 device restante, a rotação sempre lançava erro ("device_pubkey must be 33-byte... or 65-byte") pro primeiro device da lista — a redistribuição nunca funcionou, nem uma vez, desde que a feature existe (`03c5828`). Pior: `rotate_vault_key()` (Rust) já rodava incondicionalmente ANTES desse loop — já tinha reescrito o keyring e re-cifrado o `vault.enc` local inteiro sob a chave nova antes do erro acontecer, deixando o Desktop local dessincronizado do que estava publicado on-chain (a transação `updateVault`/`updateDeviceVaultKey` nunca chegava a ser montada). Não disparou até hoje porque nenhuma revogação com devices restantes tinha acontecido de fato desde que a feature foi adicionada — mas era um problema latente esperando pra corromper o estado local na primeira vez que alguém revogasse um device de verdade. **Corrigido**: (1) `encrypt_key_for_device`/`generate_vault_key_hex` novos (Rust) permitem cifrar a chave nova pra cada device restante ANTES de commitar qualquer coisa local — `rotate_vault_key` agora recebe a chave já pronta (`new_key_hex`) em vez de gerar e commitar sozinha; (2) como a chave pública crua de um device já pareado nunca fica disponível em lugar nenhum (nem on-chain, nem local — só existe efemeramente na hora do pareamento), a rotação não dispara mais sozinha: `ManageDevices.tsx` agora pede a chave pública de cada device restante antes de confirmar (`pendingRemaining`/`rawPubkeys`), e só chama `buildRotationBatch` com todos os dados em mãos. `cargo test --lib` 184/184, `cargo clippy --lib` limpo, `npx vitest run` 101/101, `tsc --noEmit` limpo. Não validado em hardware — exigiria revogar um device de teste real com outro restante ativo. | achado investigando o P54 (Sessão 206) | 🟠 Média — não bloqueia nada hoje (nunca disparou em produção), mas corrompia o estado local na primeira revogação real com devices restantes |

### Validações em Hardware Real

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P81 | **Validação manual real — tela de Configurações + bloqueio por senha pelo Desktop (Sessão 225)** — clique real na UI: abrir ⚙ no topbar, confirmar que idioma e migrar wallet funcionam de dentro dela (não só nos lugares antigos, que devem ter sumido), habilitar o bloqueio com uma senha, fechar e reabrir o app de verdade (não só recarregar) e confirmar que o `AppLockGate` aparece e barra tudo (inclusive o gate de backup da wallet local, se aplicável) até a senha certa ser digitada, confirmar que senha errada mostra erro e não desbloqueia, desabilitar exigindo a senha atual e confirmar que senha errada nesse passo também é recusada. Implementado e testado só por `tsc --noEmit`/`npx vitest run` (162/162, 11 testes novos)/`cargo test --lib` (4 testes novos, só sobre o núcleo puro de cifra/decifra — o roundtrip real via keyring/arquivo nunca foi exercitado, mesmo padrão de `local_wallet.rs`) — nenhuma tentativa de clique automatizado foi feita, por segurança (ver P79). | conversa direta + `/plan`, implementado Sessão 225 | 🟡 Baixa — protocolo coberto por testes de unidade, mas nunca clicado de verdade |
| P80 | **Validação manual real — migrar identidade pra outra wallet pelo Desktop (P78, pedaço 2)** — clique real na UI: abrir "Migrar pra outra wallet" no dashboard, conectar uma wallet nova de verdade (ex: a wallet local do P78 pedaço 1, ou uma Ledger/Trezor física), confirmar o endereço previsto da conta nova, reconectar a wallet antiga e confirmar a transação (`executeBatch` com `createAccount`+`transferController`+transferência de saldo), depois reconectar com a wallet nova e confirmar que o banner de reautorização de devices em `ManageDevices.tsx` aparece e funciona. Também falta confirmar ao vivo que os guardiões continuam configurados sem nenhuma ação (esperado pela leitura do contrato, nunca clicado). Implementado e testado só por `tsc --noEmit`/`npx vitest run` (151/151, 6 testes novos) — nenhuma tentativa de clique automatizado foi feita nesta sessão, por segurança (ver P79/[[feedback_xdotool_live_desktop_safety]]: só o dono do projeto deve clicar de verdade num fluxo que move saldo real e transfere o controle de uma identidade). | conversa direta + `/plan`, implementado Sessão 225 | 🟡 Baixa — precisa de 2 wallets conectáveis de verdade e ETH real pra mover |
| P79 | **Validação manual real — wallet local embutida pelo Desktop (P78, pedaço 1)** — clique real na UI: abrir "Local Wallet" em `ConnectWallet.tsx`, gerar a chave, confirmar/exportar o backup no gate obrigatório, depositar ETH de teste no endereço mostrado, seguir o fluxo normal de `CreateIdentity.tsx` (consentimento → createIdentity → createAccount → financiamento) e confirmar que a identidade é criada com sucesso usando só a wallet local. Também falta fechar o app antes de confirmar o backup, uma vez, pra confirmar que o gate global (`App.tsx`) intercepta corretamente na reabertura. **Tentativa feita na Sessão 224** via `npm run tauri dev` nativo + `xdotool` — a janela abriu normalmente e o gate não bloqueou nada (correto, sem wallet local nesta máquina), mas os cliques automatizados não registraram de forma confiável (mesma flakiness já documentada no P46) e, numa tentativa com coordenada absoluta da tela em vez de relativa à janela, um clique acabou acertando por engano uma aba do navegador do dono do projeto (site financeiro pessoal, sem indício de ação destrutiva) — abortado por segurança a pedido do dono do projeto, sem tentar de novo nesta sessão. Implementado e testado só por `cargo test --lib`/`npx vitest run`/`tsc --noEmit`/`cargo clippy --lib`. | conversa direta + `/plan`, implementado Sessão 224 | 🟡 Baixa — protocolo já coberto por 224/225 testes Rust + 145/145 TS, mas nunca clicado de verdade |
| P77 | **Validação manual real — votar como guardião de outra identidade pelo Mobile (P68, fatia 3)** — sessão real de ponta a ponta: 2+ celulares/wallets diferentes, um configurado como guardião de uma identidade de teste (via Desktop ou `ConfigureGuardiansScreen`), o outro (Mobile, este item) buscando o username alvo e propondo/aprovando/executando a recovery de verdade contra o `RecoveryManager` na Base Mainnet. Sem celular físico, emulador ou wallet app disponíveis nesta sessão — implementado e testado só por `flutter analyze`/`flutter test` (688/688, incluindo 16 testes novos, calldata builders validados contra vetores reais do `viem`) e leitura direta do contrato (`RecoveryManager.sol`) e do fluxo já existente no Desktop. | conversa direta + `/plan`, implementado Sessão 223 | 🟡 Baixa — protocolo mapeado a partir do contrato real e do fluxo já validado no Desktop, mas nunca exercitado contra uma wallet de verdade |
| P76 | **Validação manual real — parear device + configurar guardiões pelo Mobile (P68, fatia 2)** — sessão real de ponta a ponta: escanear o QR de um device novo e confirmar o commit-reveal completo (`commitDevice` → espera → `registerDevice`+`addDevice` no mesmo `executeBatch`), e configurar guardiões da própria identidade (`configureGuardians` via `execute`), ambos assinados por uma wallet externa real conectada via WalletConnect. Sem celular físico, emulador ou wallet app disponíveis nesta sessão — implementado e testado só por `flutter analyze`/`flutter test` (672/672, incluindo 26 testes novos, com os calldata builders validados contra vetores reais do `viem`) e leitura direta do código-fonte real dos contratos (`DeviceRegistry.sol`/`RecoveryManager.sol`) e do Desktop (`PairDevice.tsx`/`GuardianManagement.tsx`). | conversa direta + `/plan`, implementado Sessão 223 | 🟡 Baixa — protocolo mapeado a partir do contrato real e do fluxo já validado no Desktop, mas nunca exercitado contra uma wallet de verdade |
| P74 | **Validação manual real — WalletConnect + criar identidade pelo Mobile (P68, fatia 1)** — sessão WalletConnect de ponta a ponta contra um app de wallet real (deep-link ou QR): conectar, assinar o consentimento, confirmar as 3 transações (`createIdentity`, `createAccount`, financiamento), opcionalmente derivar a vault key. Sem celular físico, emulador ou wallet app disponíveis nesta sessão — implementado e testado só por `flutter analyze`/`flutter test` (646/646, incluindo 42 testes novos) e leitura direta do código-fonte real do `reown_appkit` (não documentação). | conversa direta + `/plan`, implementado Sessão 222 | 🟡 Baixa — protocolo mapeado a partir da fonte real do pacote, mas nunca exercitado contra uma wallet de verdade |
| P73 | **Validação manual real — suporte a Trezor (P24, Sessão 222)** — `/plan` completo rodado, implementado em cima da crate oficial `trezor-client` (Rust) espelhando a integração da Ledger (`trezor.rs`, `connectors/trezor.ts`, `ConnectTrezor.tsx`, regra udev `99-trezor.rules`). Round-trip completo com hardware físico ainda falta: detecção via USB, fluxo de PIN on-device (Model T/Safe 3/Safe 5), assinatura de uma transação EIP-1559 real na Base, assinatura da mensagem pessoal do consentimento de `createIdentity`, e o caminho de rejeição no próprio device. Implementado e testado só por `cargo test --lib` (12 testes novos, cobrindo classificação de erro, derivação de path, reversão do quirk de codificação EIP-155 da crate)/`npx vitest run` (12 testes novos)/`tsc --noEmit`/`cargo clippy --lib` — sem Trezor físico disponível pra validar. | conversa direta + `/plan`, implementado Sessão 222 | 🟡 Baixa — protocolo mapeado a partir da fonte real do `trezor-client`, mas nunca exercitado contra hardware |
| P71 | **Validação manual real — backup da wallet Arweave (P70) — round-trip do Desktop CONFIRMADO na Sessão 222, falta só o caso cross-plataforma.** Rodado contra o ambiente real desta máquina (não mock): exportar backup com a wallet real embutida, apagar a wallet local (keyring + arquivo, simula máquina nova), confirmar `arweave_wallet_address` ausente, importar o backup, confirmar que o endereço restaurado bate com o original; repetido com uma wallet já restaurada antes de um 2º import (payload sintético de propósito inválido), confirmado que **não** foi sobrescrita. **Achado real no caminho**: `get_arweave_wallet()` (`lib.rs`) tinha uma leitura do keyring que retornava `Ok("")` — uma entrada vazia (achada de verdade nesta máquina, causa exata não identificada, provável sobra de sessão anterior) mascarava por completo o fallback em arquivo válido, porque só o `Err` do keyring era tratado como "não achou", não um `Ok` vazio. Corrigido filtrando string vazia/só-espaço antes de confiar no valor do keyring (mesmo padrão de validação que `get_vault_key()` já tinha, por consequência de checar o tamanho de bytes). **Achado colateral, sem relação com o código do projeto**: a sequência de operações do teste (delete + set + get em sucessão rápida) derrubou de verdade o daemon de secret-service desta máquina (`ksecretd`, KDE — segfault confirmado no journal, capturado pelo DrKonqi) — não causou perda de dado (o `arweave_wallet.json` local ficou íntegro via fallback em arquivo, confirmado por leitura de estrutura sem expor a chave), mas é bom saber que operações rápidas em sequência no keyring podem crashar esse daemon específico nesta máquina; relevante pro P56 (rotação de DEK), que também faz várias operações de keyring em sequência. `cargo test --lib` 212/212 (excluindo o 1 pré-existente sem relação)/`cargo clippy --lib` limpos depois do fix. **Ainda falta**: o caso cross-plataforma (backup exportado no Desktop importado no Mobile, e vice-versa) — segue só validado por leitura de código, nunca exercitado ao vivo (precisa do Mobile). | conversa direta, achado real + fix Sessão 222 | 🟡 Baixa — round-trip principal confirmado, falta só a perna Mobile |
| P69 | **Validação manual real — dashboard único ETH↔Arweave (P67) — Desktop confirmado na Sessão 221, Mobile ainda falta.** Rodado nativo (`GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 npm run tauri dev`, técnica já documentada nas Sessões 99/103/105). **Achado de contexto**: esta máquina é nova (`device.key` de 22/08, sem `arweave_wallet.json` — a premissa original do item, "ambiente já tem wallet com histórico", não vale mais aqui; nenhuma identidade tem AR de verdade pra mostrar histórico ainda). **Confirmado ao vivo**: toggle Ethereum↔Arweave alterna a view corretamente (clique real via `xdotool`, sucesso). **Clique em "Generate Arweave wallet" via `xdotool` não registrou — reproduz a mesma flakiness já documentada e aceita como risco no P46** (várias tentativas: coordenadas recalculadas contra screenshot, mousedown/mouseup separado, `--clearmodifiers`, navegação por Tab/Enter, nenhuma funcionou); dono do projeto clicou fisicamente na janela real (que estava aberta na tela dele) e funcionou — `arweave_wallet.json` criado de verdade em `~/.truthid/`. Tela pós-geração confirmada por screenshot: endereço gerado, `Balance: 0.000000 AR` com aviso "No balance yet — buy AR on an exchange...", `Transaction history` mostrando "No transactions yet" — nenhum erro, os 2 estados vazios (saldo zero, histórico vazio) renderizam certo. **Não confirmado**: saldo/histórico com AR de verdade (exigiria fundear a wallet nova) e o mesmo roteiro no Mobile (nenhum device físico conectado nesta sessão — `adb devices` vazio, sem emulador instalado no host). | conversa direta, P67 implementado Sessão 220; Desktop validado Sessão 221 | 🟡 Baixa — falta só Mobile + saldo real |
| P3 | **Validação E2E real — extensão (13.9)** — fluxo completo: extensão carregada unpacked + celular real na mesma Wi-Fi, scan → perfil → envio → confirmação das entradas na popup. LAN e dead-drop. | `PHASE.md` (Fase 13.9, pendências finais) | 🟠 Média |
| P6 | **Revalidar decifra da vault key de pareamento (ECIES)** em hardware real — corrigido na Sessão 92 (SHA-256 do shared secret) + 99 (Mac.empty no Dart), mas nunca confirmado ao vivo no celular. | `PHASE.md` (Fase 13.9, pendências finais) | 🟠 Média |
| P7 | **Diálogo de Local Network Privacy do iOS** — mitigação aplicada (timing), não validada em device real. | `PHASE.md` (Fase 13.9, pendências finais) | 🟡 Baixa |
| P30 | **Validação E2E real — autofill de endereço (15.4, fatia 1)** — extensão carregada unpacked num formulário real (`autocomplete="street-address"` etc) + celular físico na mesma Wi-Fi: detecção do campo, QR, LAN (ou IP manual), picker no Mobile, preenchimento de volta no formulário. **Achado real na Sessão 182 (validando o P33, mesmo gesto de permissão compartilhado)**: o clique no ícone in-page realmente não propagava gesto de usuário até `chrome.permissions.request()` — já corrigido (ver P33). Ainda falta o resto do roteiro específico deste item (QR real + celular físico), que não foi tocado nesta sessão. | `PHASE.md` (Fase 15.4, fatia 1) | 🟠 Média |
| P31 | **Validação E2E real — autofill de cartão de crédito (15.4, fatia 2)** — mesmo roteiro de P30, agora com `autocomplete="cc-number"`/`cc-exp`/`cc-csc` num formulário de checkout real. Mesma limitação de hardware (P30 nunca foi validada também). | `PHASE.md` (Fase 15.4, fatia 2) | 🟠 Média |
| P32 | **Validação E2E real — dead-drop do autofill (15.4, fatia 2)** — mesmo roteiro de P30/P31, mas forçando o caminho sem LAN compartilhada (celular em rede diferente do navegador): confirmar que o Mobile publica no nome IPNS certo, que a extensão resolve via `pullFromDeadDrop`/`background.ts` e decifra corretamente. Nunca testado contra um gateway IPFS real nem contra o Kubo do dono do projeto. | `PHASE.md` (Fase 15.4, fatia 2) | 🟠 Média |
| P33 | **Validação E2E real — autofill via Desktop loopback (15.4, fatia 2) — 2 bugs reais achados e corrigidos na Sessão 182, protocolo confirmado de ponta a ponta.** Testado ao vivo (Brave real + extensão carregada unpacked + TruthID Desktop nativo, tudo na mesma máquina, sem precisar de celular). Achados: **(1) gesto de usuário perdido no pedido de permissão** — `chrome.permissions.request({origins:['http://*/*']})` sempre falhava com `"This function must be called during a user gesture"` (confirmado rodando direto no console do service worker) quando disparado via `chrome.runtime.sendMessage` do clique no ícone in-page → background.ts (`AUTOFILL_ENSURE_HOST_PERMISSION_MESSAGE`) — o "fluxo de concessão que já funciona hoje" documentado no próprio `background.ts` (clicar "I scanned it — look for my phone" no popup) **também estava quebrado no Brave**: o handler desse botão (`popup/main.ts::findButton`) fazia `return` antecipado no check `!isNetworkDiscoverySupported()` (verdadeiro no Brave, que desativa `chrome.system.network`) **antes** de chegar em `ensureHostPermission()` — ou seja, não existia nenhum caminho real de concessão da permissão no Brave. Corrigido invertendo a ordem: `ensureHostPermission()` roda primeiro (aproveita o gesto real do clique), o check de LAN vem depois só pra decidir se vale a pena varrer. Confirmado com o prompt nativo do Chrome/Brave aparecendo pela primeira vez ("TruthID Vault has requested additional permissions... Allow"). **(2) CORS ausente no servidor loopback** — mesmo com a permissão concedida, o fetch de `/truthid/v1/autofill-address`/`-creditcard` (que roda no content script, sujeito a CORS da origem da página — diferente de fetches do background, que não são) sempre falhava com erro de CORS: `local_signer_server.rs` nunca setava nenhum header `Access-Control-Allow-Origin`. Corrigido com `tower-http::cors::CorsLayer::permissive()` no router (mesmo modelo de confiança da nota de segurança já existente — localhost já é confiável, CORS aberto pro navegador não amplia a superfície real de ataque). **Confirmado**: `curl -i` mostra `access-control-allow-origin: *`; a extensão real conseguiu achar a porta (`ping` 200), mandar o POST de autofill (parqueado, "Pending" no Network tab), e o `AutofillAddressApprovalModal` do Desktop apareceu com o candidato certo do vault local (`Casa Teste`) — confirma o protocolo inteiro funcionando de ponta a ponta pela primeira vez. **Não confirmado**: o clique em "Use this address" e o preenchimento visual de volta no formulário do navegador — bloqueado pela mesma flakiness de clique do mouse no WebView do Tauri já documentada no P46 (não um bug novo; o mecanismo por trás já foi provado funcionando no P46 quando o clique acerta). Achado colateral, não confirmado: depois de um reload do frontend do Desktop com uma requisição ainda parqueada no Rust (confirmado via 409 Conflict num pedido concorrente), o modal não reapareceu — pode ser um bug real em `get_pending_autofill_address_request`/`useIncomingRequest`, mas não investigado a fundo. `cargo test --lib` 135/135, `npx vitest run`/`tsc --noEmit` (extensão) limpos. **Efeito colateral da validação**: 2 entradas de teste (`Casa Teste` endereço, `Nubank Teste` cartão) ficaram no vault local, não publicadas on-chain — dono do projeto pode removê-las quando quiser. | `PHASE.md` (Fase 15.4, fatia 2); `SESSIONS.md` (Sessão 182) | 🟡 Baixa — protocolo confirmado, falta só o clique final de aprovação (mesma limitação do P46) |
| P34 | **Validação E2E real — Android Autofill Framework (15.5)** — habilitar "TruthID" em Configurações → Sistema → Idiomas e entrada → Serviço de preenchimento automático, num device físico (API 26+), e testar num app terceiro real: campo com hint reconhecido oferece "Fill with TruthID", `PendingIntent` abre a `MainActivity`, o picker mostra as entradas certas, `EXTRA_AUTHENTICATION_RESULT` é aceito pelo framework e o campo é preenchido de verdade. Nunca rodado fora de testes automatizados/build de APK — nenhuma parte do fluxo de autofill de SO (detecção de campo via `AssistStructure`, `PendingIntent`, `Dataset` de autenticação) foi exercitada contra o framework real do Android. | `PHASE.md` (Fase 15.5) | 🟠 Média |
| P38 | **Validação em hardware real — Social Recovery UI (Fase 16)** — `GuardianManagement.tsx` (Desktop) e `GuardianStatusScreen.dart` (Mobile) nunca foram clicados de verdade, só `tsc --noEmit`/`flutter analyze`/testes automatizados (Sessões 178). Falta: configurar guardiões de verdade no Desktop nativo contra o `RecoveryManager` da Mainnet, propor/aprovar/executar/cancelar um recovery real (ou pelo menos até o timelock, sem esperar 7 dias), e confirmar que o `GuardianStatusScreen` do Mobile reflete o estado on-chain via pull-to-refresh. | `SESSIONS.md` (Sessão 178) | 🟠 Média |
| P37 | **Validação E2E real — documentos separados do blob (15.7) — parcialmente fechada, Sessão 181.** Publicados 2 documentos reais no Desktop contra Kubo local de verdade (`ipfs.service`). **Confirmado**: o blob do documento pina como objeto IPFS totalmente separado do blob principal do vault — isolado via diff do `pin ls` do Kubo antes/depois de um publish isolado (exatamente 2 CIDs novos: o vault v11 e o documento, distintos); buscar o CID do documento num gateway público (`ipfs.io`) funciona de verdade (HTTP 200) e o hash bate byte a byte com o Kubo local — confirma resolução correta "num 2º device sem cache local". **Não confirmado**: "cache local funciona offline" — bloqueado pela fragilidade de clique no botão ⭐/"Baixar" documentada e fechada como risco aceito no P46 (ver "Resolvidas"): os botões de entradas tipo documento não respondem a clique de forma confiável neste ambiente, então não foi possível exercitar a leitura via UI (só a escrita/publish foi validada). | `PHASE.md` (Fase 15.7) | 🟠 Média — falta só a parte de leitura, mesma fragilidade de clique do P46 |
| P35 | **Build real — `AutofillExtension` (15.6, fatia 1 + fatia 2)** — target criado programaticamente (gem `xcodeproj`) e validado só por inspeção (dependency, embed phase, bundle id, entitlements, configs Debug/Release/Profile, frameworks linkados) — nunca compilado, este ambiente é Linux, sem Xcode/macOS/simulador. Checklist de validação num Mac real, fundindo fatia 1 e fatia 2 (ver P36, que foi fundido aqui): (1) abrir `Runner.xcodeproj`, confirmar que builda sem erro nos 2 targets; (2) habilitar "TruthID" em Ajustes → Senhas → Preencher senhas automaticamente; (3) confirmar que as identidades registradas via `ASCredentialIdentityStore` aparecem na lista/QuickType bar; (4) abrir o app, desbloquear o Vault (dispara `IosAutofillVaultSyncService.sync`) e confirmar que a chave/blob chegam no Keychain Access Group/App Group compartilhados; (5) num app ou site real com campo de senha, confirmar que o preenchimento de verdade funciona — Face ID/passcode do sistema, escolha na lista (`prepareCredentialList`) e auto-preenchimento direto (`prepareInterfaceToProvideCredential`); (6) forçar os caminhos de erro (Vault nunca sincronizado, entrada removida) e confirmar que cancela com mensagem sensata em vez de travar. | `PHASE.md` (Fase 15.6, fatias 1 e 2) | 🟠 Média |

### Funcionalidades Não Implementadas

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P9 | **Phase 15 — Autofill SO (Android/iOS)** — implementar `AutofillService` e `ASCredentialProviderViewController`. **Android (15.5) concluído na Sessão 174** — `TruthIdAutofillService.kt` detecta campos e delega a leitura/aprovação pra Activity Flutter existente (mesmo Vault/AppLockGate/picker da Fase 15.4), sem duplicar acesso ao vault em Kotlin. **iOS (15.6) concluído por completo na Sessão 176** — target `AutofillExtension` (fatia 1, S175) + decifra real dentro da extensão via Keychain Access Group/App Group compartilhados (fatia 2, S176, `CredentialProviderViewController.swift`/`SharedVaultAccess.swift`), só credencial (endereço/cartão não têm extension point público no iOS, achado real da fatia 1). Falta só validar em Mac real (P35). | `PHASE.md` (Fase 15, etapas 15.5/15.6) | 🟠 Média |
| P47 | **`site/` — próximas fatias, fora do escopo do v1** — billing (Stripe/Mercado Pago) + Entitlement Service, bootstrap de identidade TruthID sem Ledger (item 2.1 do épico de tier facilitado), GitHub e o próprio TruthID como provider adicional de login (modelo `Identity` já suporta). Migração de `docs/` (Docusaurus) pro site **fechada na Sessão 200** pro que já existia: Fumadocs em `/docs` no `site/frontend`, identidade visual reaplicada (teal/cyan + Inter/Space Grotesk), deploy estático no GitHub Pages (só a doc, não o app inteiro), conteúdo expandido com 8 páginas novas + correções reais (P48, fechado na mesma sessão — ver "Resolvidas"). Billing/Entitlement/bootstrap sem Ledger seguem em desenvolvimento, mas **decisão explícita do dono do projeto (Sessão 200): sem lançamento ao vivo por um bom tempo** — prioridade atual é continuar a doc. Nenhuma dessas fatias tem `/plan` rodado ainda. | `SESSIONS.md`/`ROADMAP.md` (Sessão 199, 200) | 🟡 Baixa |
| P28 | **SDK Dart: transporte deep link no `TruthIDRequester`** — só cross-device (QR) implementado. Deep link (mesmo aparelho) exigiria o app host registrar seu próprio esquema de URI, específico de plataforma — decidido deixar de fora de um pacote Dart puro por ora. Reavaliado na Sessão 165: hoje nem o Mobile aceita deep link pra `pin`/`vault-edit` (só `sign-message`/`sign-request`), e um pacote Dart puro não consegue automatizar o registro de URI scheme do app hospedeiro nem depende de `url_launcher` — decisão confirmada, segue de fora. | `SESSIONS.md` (Sessão 161, reavaliado 165) | 🟡 Baixa |

### P63 — Homebrew (macOS) — empacotamento adiado até decisão de comprar certificado de assinatura (Sessão 217)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P63 | Desenho já travado no `/plan` de instaladores nativos (`ROADMAP.md`, Sessão 215): tap próprio `masterlxz/homebrew-truthid` (`Casks/truthid.rb`), linkando o `TruthID_X_aarch64.dmg` já publicado no GitHub Release — só Apple Silicon (limitação já conhecida do P57), sem código novo no app em si. **Adiado de propósito pelo dono do projeto (Sessão 217)**: sem certificado de assinatura (Apple Developer ID, ~US$99/ano — achado do mesmo `/plan`), o Gatekeeper bloqueia o app instalado via `brew install --cask` ("desenvolvedor não pode ser verificado"), exigindo workaround manual do usuário — decidido não gastar/publicar agora, fica pro futuro quando fizer sentido pagar pelo certificado. Prioridade dada ao APT nesta sessão (ver `ROADMAP.md`, mesma seção). | `ROADMAP.md` (Sessão 215); decisão de adiar, conversa direta (Sessão 217) | 🟡 Baixa — bloqueado por decisão de custo, não por trabalho técnico |

### Pendências de Arquitetura / Decisões em Aberto

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P14 | **Interface e identidade visual (UI/UX)** — app e desktop funcionais mas sem polish de produto final. Identidade visual já aplicada (Fase 9), mas fluxos e polish de produto pendentes. | `ARCHITECTURE.md` (tabela de decisões) | 🟡 Baixa |
| P15 | **Session key com limite de gasto** — desenho para evitar gas por mensagem (IA). Em aberto: onde registrar consumo on-chain vs off-chain, revogação em cascata. | `ROADMAP.md` (Monetização) | 🟡 Baixa |
| P16 | **Monetização — definições finais** — precificação ETH/BRL, modelo de consentimento do /pin, session key spending limit, margem de cada fonte de receita. Nada implementado. | `ROADMAP.md` (Monetização) | 🟡 Baixa |

### Ideias de Expansão (Brainstorm — sem `/plan`)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| P18 | **Verifiable Credentials / Atestações ZK** — provar atributos sem revelar tudo (KYC descentralizado). | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P19 | **Delegação de acesso temporário** — sessões com escopo e prazo. | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P20 | **Reputação on-chain portátil** — histórico de confiança consultável. | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P21 | **Vault compartilhado (Family/Team)** — múltiplos Devices de pessoas diferentes. | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P22 | **Detecção de vazamento de senha** — k-anonymity (HIBP-like). | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P23 | **Modo panic/duress** — PIN secundário mostrando vault vazio. | `ROADMAP.md` (Expansão) | 💡 Ideia |
| P72 | **Derivar a wallet Arweave da identidade (em vez de depender só do backup do P70)** — `/plan` completo rodado (Sessão 221), 2 barreiras reais encontradas, nada implementado. (1) Determinar o tipo de wallet atual (RSA-4096) não é viável entre Rust/Dart — nenhuma solução da comunidade garante reprodutibilidade bit a bit entre linguagens diferentes. (2) Caminho melhor identificado (Arweave suporta wallets ECDSA/secp256k1 desde o hard fork 2.9, mesma curva da identidade) esbarra em: uma inconsistência não resolvida rastreada no código-fonte real do Arweave (deep hash de 48 bytes/SHA-384 entra direto num NIF que exige 32 bytes, sem passo de redução encontrado — PR original do ECDSA no Arweave nunca foi mergeado); e o Mobile não ter wallet Ethereum externa conectada (mesmo achado do P68) — a derivação funcionaria só no Desktop, o Mobile precisaria receber via pareamento ou ganhar WalletConnect próprio. Ver `ROADMAP.md` (Sessão 221) pro detalhe técnico completo. Retomar quando o suporte ECDSA do Arweave estiver verificado contra um nó real e o P68 avançar. | `ROADMAP.md` (Sessão 221) | 💡 Ideia |
| P75 | **Revogar device + `cancelRecovery` pelo Mobile** — deixados de fora da fatia 2 do P68 (Sessão 223) de propósito. Revogar device não é um `executeBatch` isolado — no Desktop (`ManageDevices.tsx`) dispara uma rotação de DEK opcional que exige a chave pública crua de cada device restante à mão (o `DeviceRegistry` só guarda o endereço, nunca a chave bruta), pedaço de trabalho comparável ao pareamento em si. `cancelRecovery` é owner-gated (mesmo padrão de `configureGuardians`) mas está fora do par "pareamento + configurar guardiões" pedido, e faz mais sentido junto de "votar como guardião" (propor/aprovar/executar), já adiado desde a fatia 1. | conversa direta, achado durante o `/plan` da fatia 2 (Sessão 223) | 💡 Ideia |

---

## Resolvidas

### Tela de Configurações (⚙) no Desktop + bloqueio do app por senha (Sessão 225)

| ID | Item | Resolvida em |
|---|---|---|
| ~~(sem ID próprio — pedido direto)~~ | ~~Dono do projeto pediu uma tela de Configurações (engrenagem) no Desktop reunindo o que estava espalhado (idioma no topbar, migrar wallet no dashboard) mais uma senha opcional pra abrir o app, ativável ali — pedido pra valer tanto no Desktop quanto no Mobile.~~ **`/plan` completo rodado antes de qualquer código.** Investigação do Mobile primeiro: **já tem praticamente tudo isso** — `settings_screen.dart` já é uma tela de Configurações completa (idioma em bottom sheet, atalhos pra recovery social e segurança), `security_screen.dart` já tem um toggle de bloqueio do app via biometria/PIN/padrão do próprio celular (`AppLockService`) — nunca uma senha própria do TruthID (comentário no próprio arquivo). **Decisão do dono do projeto**: manter o Mobile como está, replicar esse MESMO comportamento no Desktop — nada de senha específica do TruthID lá, o SO já resolve. Pesquisado se dava pra usar autenticação nativa do SO no Desktop (Windows Hello/Touch ID, análogo à biometria do celular): não existe hoje plugin Tauri maduro cobrindo Windows/macOS/Linux de forma uniforme, seria trabalho específico por SO e nem dava pra validar nesta máquina (Linux). **Decisão do dono do projeto**: sem integração com SO — só uma senha própria do TruthID no Desktop. **Implementado**: `desktop/src-tauri/src/app_lock.rs` (novo — reaproveita `backup::encrypt`/`backup::decrypt` já existentes em vez de escrever hash-and-compare novo: a senha cifra um marcador fixo conhecido, "verificar" é só tentar decifrar, o tag de autenticação do AES-GCM já rejeita senha errada sozinho; custódia keyring+fallback em arquivo espelhando `local_wallet.rs`; núcleo puro `create_blob`/`verify_blob` separado da parte que toca keyring, mesmo princípio de `backup::encrypt_with`, pra ser testável sem estado global compartilhado entre testes); 4 comandos Tauri novos (`app_lock_is_enabled/_enable/_verify/_disable` — desabilitar exige a senha atual, diferente do Mobile que não pede biometria pra desabilitar porque o SO já teria barrado a entrada antes). `desktop/src/hooks/useAppLock.ts` + `AppLockGate.tsx` (novos, espelham `useLocalWalletBackupGate.ts`/`LocalWalletBackupGate.tsx`) — gate global em `App.tsx`, checado ANTES até do gate de backup da wallet local, mesma prioridade "intercepta tudo". `desktop/src/components/Settings.tsx` (novo) — 3 seções: idioma (`LanguageSelector` movido do topbar, sem mudança nele), wallet (`MigrateWallet` movido do dashboard — **removido** o botão/modal correspondentes de `SmartAccountDashboard.tsx`), bloqueio do app (toggle habilitar/desabilitar com confirmação de senha). Botão ⚙ novo no topbar de `App.tsx`. i18n nos 4 idiomas (`settings.json`/`appLockGate.json` novos, chave `app.topbar.settings` nova). Testes: `npx tsc --noEmit` limpo, `npx vitest run` 162/162 (11 novos — `AppLockGate.test.tsx`, `Settings.test.tsx`), `cargo test --lib` 227/234 (4 novos em `app_lock::tests`, cobrindo só o núcleo puro de cifra/decifra — sem estado de keyring/arquivo compartilhado entre testes, mesmo padrão de `local_wallet.rs`/`backup.rs`), `cargo clippy --lib` limpo (1 aviso pré-existente sem relação); 1 falha pré-existente e já documentada como intermitente (`local_signer_server`), sem relação com esta mudança. **Não validado**: clique real na UI — nenhuma tentativa de automação foi feita nesta sessão, por segurança (ver P79); registrado como P81. | conversa direta + `/plan`, implementado Sessão 225 |

### P78 — pedaço 2: migrar identidade pra outra wallet implementado no Desktop (Sessão 225)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P78 (pedaço 2)~~ | ~~Migrar o `owner` de uma identidade de uma wallet pra outra (ex: hot wallet local↔hardware wallet), deixado só desenhado na Sessão 224 com 2 perguntas em aberto: (a) se um processo de vários passos interrompido no meio trava a conta nova sem device autorizado, valendo uma "função atômica nova no contrato"; (b) se os guardiões de Social Recovery ficam amarrados à conta antiga.~~ **`/plan` completo rodado antes de qualquer código** (2 agentes Explore em paralelo — convenções de UI/wallet do Desktop, e o precedente real da migração em cascata da Sessão 197/P52/P53 — mais leitura direta de `RecoveryManager.sol`/`IdentityRegistry.sol`/`TruthIDAccount.sol`/`TruthIDAccountFactory.sol`/`DeviceRegistry.sol`). **As 2 perguntas resolvidas direto no código real, não por suposição**: (a) impossível ser atômico entre as 2 chaves de qualquer jeito — `transferController` (via `execute()` da conta antiga) só o owner antigo assina, `addDevice` na conta nova só o owner novo assina, nunca cabem na mesma transação de uma EOA só; mas o risco é menor do que parecia, porque o owner novo (que por definição já assinou pra chegar até aqui) sempre pode chamar `addDevice` direto, sem depender de device nenhum — e o gap "device não reautorizado numa conta nova" já tem precedente real e testado em produção: P53 (Sessão 205), cujo fix (`ManageDevices.tsx`, banner "Reautorizar N devices") já cobre esse caso sem nenhuma mudança nova. (b) guardiões (`RecoveryManager._guardianConfigs`) e devices (`DeviceRegistry._devices`) são indexados por `identityId`, nunca pelo endereço da smart account — sobrevivem à migração automaticamente, confirmado lendo o contrato e `GuardianManagement.tsx`/`rotateVaultKeyOnRevoke.ts`. **Achado que simplificou o desenho**: como `execute()`/`executeBatch()` só fazem `dest.call{value}(func)` e `TruthIDAccountFactory.createAccount` é permissionless e idempotente, as 3 chamadas que a conta ANTIGA precisa fazer (implantar a conta nova, trocar o controller, mover o saldo) cabem inteiras num único `executeBatch`, uma única confirmação da wallet antiga — só o 4º passo (reautorizar devices) precisa da wallet nova, e esse já está resolvido. **Implementado**: `desktop/src/config/contracts.ts` ganhou a entrada `transferController` na `IDENTITY_REGISTRY_ABI` (faltava, `transferController` já existia no contrato desde sempre mas nunca tinha sido chamado pelo Desktop); `desktop/src/components/MigrateWallet.tsx` (novo — máquina de fases `intro → connectNew → confirmNew → connectOld → review → confirming → done`; como o app só sustenta uma conexão wagmi ativa por vez [`storage: null`], reconectar é sempre manual: a fase `connectNew` reaproveita `ConnectWallet.tsx` só pra capturar o endereço da wallet nova sem gastar gas, desconecta sozinho, depois `connectOld` pede a wallet atual de volta pra assinar; bloqueia seguir se a conta nova prevista bater com a atual — "migrar pra você mesmo"); botão novo em `SmartAccountDashboard.tsx`, ao lado de Depositar/Sacar; i18n nos 4 idiomas. Testes: `npx tsc --noEmit` limpo, `npx vitest run` 151/151 (6 testes novos, cobrindo captura+desconexão da wallet nova, bloqueio de "mesma wallet", montagem do `executeBatch` com e sem transferência de saldo) — precisou também ajustar `SmartAccountDashboard.test.tsx` pra mockar `MigrateWallet` (puxa `ConnectWallet`→`ConnectLedger`/`ConnectTrezor`, que chamam `createConnector` de verdade no topo do módulo). `cargo test --lib`/`cargo clippy --lib` rodados como checagem de regressão (nenhuma mudança Rust neste pedaço) — 1 falha pré-existente sem relação (`local_signer_server`, já documentada como intermitente desde antes desta sessão). **Não validado**: clique real na UI — nenhuma tentativa de automação foi feita nesta sessão, por segurança (ver P79, achado real da Sessão 224 de um clique de coordenada absoluta acertando por engano uma aba pessoal do dono do projeto); registrado como P80. | conversa direta + `/plan`, implementado Sessão 225 |

### P78 — pedaço 1: wallet local embutida (bootstrap sem wallet externa) implementada no Desktop (Sessão 224)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P78 (pedaço 1)~~ | ~~Permitir criar identidade sem Ledger/Trezor/WalletConnect, gerando e guardando uma chave secp256k1 localmente no Desktop, usada como `owner` da smart account.~~ **`/plan` completo rodado antes de qualquer código** (3 agentes Explore investigando conectores wagmi/custódia de chave/contratos+assinatura, 1 agente Plan desenhando a implementação) — achado-chave que simplificou o desenho: `CreateIdentity.tsx` e o cálculo do endereço da smart account já são 100% agnósticos de conector, nenhum dos dois precisou mudar. **Implementado**: `desktop/src-tauri/src/local_wallet.rs` (novo módulo — custódia keyring+fallback em arquivo espelhando `get_arweave_wallet`, SEM auto-geração implícita diferente da device key; assinatura ECDSA direto em Rust via `k256::sign_prehash_recoverable`, sem hardware — 7 comandos Tauri novos: `local_wallet_generate/_exists/_address`, `sign_local_wallet_transaction/_personal_message`, `local_wallet_backup_confirmed`, `confirm_local_wallet_backup`); `desktop/src/connectors/localWallet.ts` (novo conector wagmi, espelha `ledger.ts`); `desktop/src/components/ConnectLocalWallet.tsx` + `LocalWalletBackupGate.tsx` (novos — gate de backup OBRIGATÓRIO com bloqueio, decisão do dono do projeto: essa chave é o único controle da identidade até uma eventual migração, perder o device sem backup = perder a identidade pra sempre); `desktop/src/hooks/useLocalWalletBackupGate.ts` (novo) + gate global em `App.tsx` (intercepta antes de qualquer tela, inclusive login, caso o app feche entre gerar a chave e confirmar o backup); item novo em `ConnectWallet.tsx`; integração no backup exportável (`splice_local_wallet_into_export`/`extract_local_wallet_from_import` em `lib.rs`, mesmo padrão da wallet Arweave — campo `local_wallet_private_key` no envelope, nunca no struct `Vault` tipado); i18n nos 4 idiomas. Migração pra outra wallet (pedaço 2) explicitamente fora de escopo, só Desktop (Mobile fica pra depois). Testes: `cargo test --lib` 224/225 (11 testes novos, incluindo cross-check da assinatura contra `sign_personal_message_raw` já validada no projeto e vetor conhecido do Anvil; 1 falha pré-existente sem relação, `local_signer_server`, já documentada como intermitente), `cargo clippy --lib` limpo (1 aviso pré-existente sem relação), `npx vitest run` 145/145 (19 testes novos: conector + componente + hook), `tsc --noEmit` limpo. **Não validado**: clique real na UI — tentativa feita nesta sessão via `xdotool` na janela nativa (`npm run tauri dev`), abortada por segurança depois que um clique de coordenada absoluta acabou acertando por engano uma aba do navegador do dono do projeto (Investidor10, site financeiro pessoal dele, sem indício de ação destrutiva) devido a mudança de foco/empilhamento de janelas no desktop ao vivo — registrado como P79. | conversa direta + `/plan`, implementado Sessão 224 |

### P68 — WalletConnect + criar identidade 100% pelo Mobile implementado (fatia 1); pareamento/guardiões ficam pra fatia 2, Ledger BLE descartado (Sessão 222)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P68~~ | ~~Fluxo 100% mobile (sem depender do Desktop) — permitir criar identidade/parear device/publicar vault/gerenciar guardiões inteiramente pelo celular, usando uma wallet nativa do próprio celular (WalletConnect ou chave local) ou conectando a Ledger física ao celular (USB-C OTG ou BLE, só Nano X). Hoje o Mobile não tem nenhum código de wallet/hardware wallet.~~ **`/plan` completo rodado antes de qualquer código, 2 agentes Explore investigaram o Mobile a fundo primeiro**: achado real — existe um único gargalo que trava 3 das 4 sub-funcionalidades (criar identidade, parear device, configurar guardiões): o Mobile não tem como assinar como *owner* da smart account (`TruthIDAccount.owner` é literalmente o endereço da wallet externa que assinou; a chave de device do Mobile é sempre um signer de nível restrito, nunca owner). Publicar o vault **já funcionava 100% no Mobile antes desta sessão** (achado real, sem gargalo nenhum ali). Votar como guardião (propor/aprovar/executar recovery de outra pessoa) também não é travado por owner, mas foi deixado de fora por não avançar o objetivo central. **Decisão do dono do projeto**: implementar só a infra WalletConnect (`reown_appkit`) + o fluxo completo de criar identidade nesta rodada (fatia 1) — pareamento e configuração de guardiões ficam pra uma fatia 2, reaproveitando a mesma conexão WC sem infra nova. Ledger via Bluetooth (a outra opção do P68 original) descartado por completo — sem precedente no projeto, só o Nano X suporta BLE, sem hardware pra testar (mesmo tratamento do FIDO2/YubiKey no P24). **Implementado, espelhando `CreateIdentity.tsx` do Desktop**: `mobile/lib/services/wallet_connect_service.dart` (novo, wrapper sobre `ReownAppKitModal` — API real confirmada lendo o código-fonte instalado do pacote, não a documentação, incluindo o achado de que `session.getAddress('eip155')` é um método, não um getter simples); `mobile/lib/utils/identity_consent_hash.dart` (novo, replica `buildIdentityConsentHash.ts` byte a byte); `mobile/lib/utils/ecdsa_signature.dart` (novo, parser de assinatura 65 bytes); `mobile/lib/utils/eth_amount.dart` (novo — ver achado real abaixo); `mobile/lib/screens/create_identity_screen.dart` (novo, `CreateIdentityFlow` — máquina de estados com guarda síncrona de disparo duplo por chamada, mesmo princípio do bug documentado em `CreateIdentity.tsx:156-165` no Desktop, aqui ainda mais necessário já que um pedido WalletConnect é uma ida-e-volta assíncrona pra outro app); `blockchain_service.dart` ganhou `isUsernameTaken`/`getUsernameByController`/`predictSmartAccountAddress` (lê a view `getAddress` on-chain da factory — nunca replica CREATE2 localmente, mesma lição do bug real do P66) + `buildCreateIdentityCalldata`/`buildCreateAccountCalldata` (calldata codificado à mão, mesmo padrão já usado em `getIdentityByUsername` pro débito #32) + `isTransactionConfirmed` (polling de recibo sem lançar em tx pendente); `vault_key_service.dart` ganhou `deriveAndStoreFromWalletSignature` (persiste a chave derivada da wallet, reaproveitando o `hkdfSha256` já existente); `devices_screen.dart` ganhou o botão "Create new identity" ao lado do QR, só no estado não pareado; 2 novos esquemas de deep link (`truthidwc://`, Android+iOS) pra não colidir com o `truthid://` já usado pelo pareamento; i18n nos 4 idiomas. **3 achados reais no caminho**: (1) `reown_appkit >=1.7.7-beta02` exige `web3dart ^3.0.1`, incompatível com o `web3dart ^2.7.3` usado em todo o resto do app — pinado em `^1.7.6`, que ainda usa `web3dart ^2.x`. (2) `ContractAbi.fromJson` do `web3dart` lança `type 'Null' is not a subtype of type 'bool'` ao parsear `identityRegistryAbi` — o evento `IdentityCreated` (já existia antes desta sessão) nunca tinha o campo `"anonymous"`, e nunca tinha sido exercitado por esse parser porque `getIdentity` sempre foi codificado à mão (débito #32) — bug latente desde sempre, só descoberto porque os novos `isUsernameTaken`/`getUsernameByController` precisavam de um `DeployedContract` de verdade pela 1ª vez; corrigido adicionando `"anonymous": false`. (3) `EtherAmount.fromBase10String` do `web3dart` tem nome enganoso — faz `BigInt.parse` direto no texto inteiro, sem tratar ponto decimal nenhum; `fromBase10String(EtherUnit.ether, "0.001")` lançava `FormatException` na hora. `parseEthToWei` novo faz o parse decimal de verdade. Testes: `flutter analyze` limpo (13 issues, todas pré-existentes + 1 sugestão de estilo nova, sem erro/warning); `flutter test` 646/646 (42 testes novos, incluindo vetores cruzados com `viem` pro hash de consentimento e vetores cruzados com o HKDF real do Rust do Desktop pra derivação da vault key — interoperabilidade Desktop↔Mobile confirmada byte a byte nos dois). **Não validado**: sessão WalletConnect real contra um app de wallet físico — sem celular/emulador disponíveis nesta sessão, registrado como P74. | conversa direta + `/plan`, implementado Sessão 222 |

### P68 — fatia 2: parear device + configurar guardiões implementado pelo Mobile (Sessão 223)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P68 (fatia 2)~~ | ~~Reaproveitar a infra WalletConnect da fatia 1 pra fechar as 2 sub-funcionalidades que ficaram de fora: parear um novo device e configurar guardiões da própria identidade — ambas owner-gated, mesmo motivo da fatia 1.~~ **`/plan` completo rodado antes de qualquer código** (2 agentes: 1 de investigação do fluxo real do Desktop + estado atual do Mobile, 1 de design do plano de implementação). **Achado de investigação**: `DeviceRegistry.commitDevice`/`configureGuardians` do `RecoveryManager` exigem que `msg.sender` visto pelo contrato seja a smart account, nunca a wallet externa direto — toda transação desta fatia é roteada via `TruthIDAccount.execute`/`executeBatch` na própria smart account (`to = controller.hex`), nunca `to = DeviceRegistry/RecoveryManager` diretamente (confirmado lendo o contrato + `PairDevice.tsx`/`GuardianManagement.tsx` do Desktop, não por memória). **Decisão de escopo** (conversa direta antes do `/plan`): revogar device e `cancelRecovery` ficam de fora desta rodada (registrados como P75); votar como guardião de outra identidade (propor/aprovar/executar recovery) também fica de fora, mesma decisão da fatia 1. **Implementado**: `mobile/lib/utils/random_bytes.dart` (novo, `randomBytes32()` extraído do padrão já inline em `arweave_transaction.dart`/`arweave_isolate.dart`); `blockchain_service.dart` ganhou 7 calldata builders hand-rolled (mesmo padrão de débito #32 já usado por `buildCreateIdentityCalldata`) — `buildDeviceCommitment` (keccak256/`abi.encodePacked`), `buildCommitDeviceCalldata`, `buildRegisterDeviceCalldata`, `buildAddDeviceCalldata`, `buildExecuteCalldata`, `buildExecuteBatchCalldata`, `buildConfigureGuardiansCalldata` — todos validados contra vetores reais gerados com `viem` (mesmo pacote já usado no lado Desktop), mais `recoveryManagerAddress` exposto publicamente (mesmo padrão de `deviceRegistryAddress`); `mobile/lib/screens/pair_device_screen.dart` (novo, `PairDeviceFlow` — máquina de estados do commit-reveal: commit → espera 5s → reveal num único `executeBatch(registerDevice + addDevice)`, com 1 retentativa automática se o reveal reverter por `RevealTooEarly`; cifra a vault key local via ECIES pro device novo, best-effort — nunca aborta o pareamento se a chave não estiver disponível); `mobile/lib/screens/configure_guardians_screen.dart` (novo, `ConfigureGuardiansFlow` — valida proposta de recovery ativa antes de gastar gas numa tx que reverteria com `ActiveProposalExists()`, checagem que o próprio Desktop não faz); `devices_screen.dart` ganhou o botão "Pair new device" (reaproveita `ScanScreen<T>` genérico já existente pra ler o QR que `ShowDeviceQrScreen` já produz); `guardian_status_screen.dart` ganhou o botão "Configure/Change guardians", deixou de ser 100% leitura — footer e comentários que afirmavam "Mobile nunca escreve no RecoveryManager" corrigidos; i18n nos 6 arquivos ARB (4 idiomas). **2 achados reais no caminho, achados pelos próprios testes de vetor `viem` (não revisão manual)**: (1) `buildRegisterDeviceCalldata` — a ordem dos slots do cabeçalho ABI tem que seguir a ORDEM DE DECLARAÇÃO dos parâmetros (`address, string, bytes32, bytes`), não "estáticos primeiro": a 1ª versão colocava `devicePubKey, salt` (os 2 estáticos) antes dos 2 offsets dinâmicos, divergindo do vetor real porque `salt` (bytes32, 3º parâmetro) precisa ficar inline na posição 3, entre o offset do `label` (2º parâmetro) e o offset do `encryptedVaultKey` (4º parâmetro). (2) `buildExecuteBatchCalldata` — os offsets de `func[i]` são relativos ao INÍCIO da tabela de offsets (logo após o slot de `length` do array), não ao final dela; a 1ª versão começava a contagem em `32 + n*32` (bytes) em vez de `n*32`, divergindo do vetor real por exatamente 32 bytes (1 slot) em todo offset. Testes: `flutter analyze` limpo (13 issues pré-existentes, nenhuma nova); `flutter test` 672/672 (26 testes novos — 9 vetores de calldata cruzados com `viem`, 8 cobrindo `ConfigureGuardiansFlow` incluindo o bloqueio por proposta ativa, 9 cobrindo `PairDeviceFlow` incluindo o retry de `RevealTooEarly` e o fallback de vault key indisponível, todos usando `testWidgets`+`tester.pump(Duration)` pra avançar os delays reais do commit-reveal sem esperar de verdade — mesmo padrão já usado em `approval_screen_test.dart`). **Não validado**: sessão real contra uma wallet física escaneando um device de verdade — sem celular/emulador disponíveis nesta sessão, registrado como P76 (mesma limitação do P74 na fatia 1). | conversa direta + `/plan`, implementado Sessão 223 |

### P68 — fatia 3: votar como guardião de outra identidade implementado pelo Mobile (Sessão 223)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P68 (fatia 3)~~ | ~~Última peça do trio pareamento/guardiões/votar-como-guardião: agir como guardião de OUTRA identidade — propor/aprovar/executar recovery social — pelo Mobile.~~ **`/plan` direto (sem agentes extras — leitura direta do contrato + do Desktop já bastava, mesma qualidade de um Explore dedicado)**: confirmado lendo `RecoveryManager.sol` que `proposeRecovery`/`approveRecovery`/`executeRecovery` **não são owner-gated** — chamada direta ao `RecoveryManager` (`to = recoveryManagerAddress`), assinada pela wallet do guardião, nunca pela smart account da identidade alvo — mais simples que tudo já feito nas fatias 1/2 (sem commit-reveal, sem `execute()`). **Implementado, espelhando a seção "Act as guardian for another identity" de `GuardianManagement.tsx` no Desktop (linhas 324-474/612-746)**: `blockchain_service.dart` ganhou `hasGuardianApproved` (leitura) + `buildProposeRecoveryCalldata`/`buildApproveRecoveryCalldata`/`buildExecuteRecoveryCalldata` (escrita, calldata hand-rolled no mesmo padrão já estabelecido); `mobile/lib/screens/act_as_guardian_screen.dart` (novo, `ActAsGuardianFlow` — busca a config/proposta de um username alvo digitado pelo usuário, não o da própria identidade pareada; `isGuardian`/`proposalStatus`/`canExecute` replicam exatamente a lógica já validada no Desktop, incluindo `canExecute` **não** ser guardião-gated — qualquer um pode executar depois do threshold+timelock); `guardian_status_screen.dart` ganhou um ícone no AppBar (`Icons.shield_outlined`) que abre a tela nova, sempre visível independente de ter identidade própria pareada; comentários/footer atualizados de novo (só `cancelRecovery` continua exclusivo do Desktop agora, ver P75). i18n nos 6 ARBs, reaproveitando as chaves de `guardianStatusScreen*` já existentes pra proposta/timelock (mesmo texto, mesmo significado, não duplicado). Testes: `flutter analyze` limpo (13 issues pré-existentes, nenhuma nova); `flutter test` 688/688 (16 testes novos — 3 vetores `viem` pros calldata builders, batendo de primeira sem retrabalho desta vez; 13 cobrindo `ActAsGuardianFlow`, incluindo `isGuardian` case-insensitive, `hasGuardianApproved` só buscado quando é guardião com proposta ativa, `canExecute` nas 4 combinações relevantes de threshold/timelock/guardião, guarda de disparo duplo). **Não validado**: sessão real contra uma wallet física agindo como guardião — sem celular/emulador disponíveis nesta sessão, registrado como P77 (mesma limitação de P74/P76). | conversa direta + `/plan`, implementado Sessão 223 |

### P24 — suporte a Trezor implementado; YubiKey/FIDO2 descartado desta rodada por incompatibilidade real (Sessão 222)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P24~~ | ~~Suporte a hardware wallets alternativas — Trezor, YubiKey/FIDO2. Reafirmada pelo dono do projeto na Sessão 220, junto com P67/P68.~~ **`/plan` completo rodado antes de qualquer código**: investigação real (não memória) achou uma bifurcação técnica entre os dois hardwares citados. Trezor usa a mesma curva ECDSA/secp256k1 que a Ledger e a identidade já usam em toda a app — `derive_vault_key_from_wallet` (`lib.rs:248`) e `CreateIdentity.tsx` já consomem qualquer assinatura genérica no formato `"0x" + r + s + v(27/28)"`, então a Trezor se encaixa sem mudar nenhum código central. YubiKey/FIDO2 usa outra curva (P-256/Ed25519) — não produz esse formato sem reescrever a arquitetura de assinatura da app inteira, e as libs Rust de CTAP2/FIDO2 disponíveis hoje (`webauthn-authenticator-rs`, `authenticator-rs`) estão em estado alpha/branch instável, nenhuma opção madura. **Decisão do dono do projeto**: implementar só Trezor nesta rodada; YubiKey/FIDO2 fica de fora, sem pendência de "falta implementar" — só ideia de pesquisa se algum dia o cenário mudar (Rust CTAP2 amadurecer, ou a app ganhar um caminho de assinatura não-secp256k1 por outro motivo). **Trezor implementado**, espelhando a integração da Ledger na forma: `desktop/src-tauri/src/trezor.rs` (novo, 4 comandos Tauri — `is_trezor_connected`/`get_trezor_address`/`sign_trezor_transaction`/`sign_trezor_personal_message` — construídos sobre a crate oficial `trezor-client` em vez de reimplementar o protocolo na mão como a Ledger fez); `connectors/trezor.ts` (novo conector wagmi); `ConnectTrezor.tsx` (novo, wizard de 2 passos — sem o passo "abrir app" da Ledger, a Trezor não tem esse conceito); `ConnectWallet.tsx` ganhou a 3ª opção; `99-trezor.rules` (novo — diferente da Ledger, o transporte da Trezor é libusb/rusb, a permissão é sobre o nó USB bruto, não `/dev/hidraw*`); i18n nos 4 idiomas. **3 achados reais confirmados por pesquisa direta na crate (não memória)**: (1) `trezor-client` não depende de `hidapi` (usa `rusb`/`libusb1-sys`) — sem conflito de versão com a Ledger, confirmado via `cargo tree`; (2) a crate aplica a fórmula EIP-155 legada no `v` mesmo numa transação EIP-1559 (type 2) — revertido deterministicamente em `format_tx_signature` antes de devolver pro TS, nada rio abaixo precisa saber; (3) o transporte da Trezor não tem timeout de leitura nenhum (pior que a Ledger, que ao menos tem timeout na leitura) — o mitigador que `ConnectLedger.tsx` já tinha (`withTimeout`/`hidBusyRef`/`HID_TIMEOUT_MS`) foi portado 1:1 pro `ConnectTrezor.tsx`, mais necessário aqui, não menos. Testes: `cargo test --lib` 212/219 passam (12 novos em `trezor::tests`, cobrindo derivação de path, classificação de erro por `FailureType`, reversão do quirk EIP-155, normalização de `v`; 6 ignored pré-existentes; 1 teste pré-existente e sem relação — `local_signer_server`, servidor HTTP local — falha de forma intermitente mesmo no `main` sem nenhuma mudança desta sessão, confirmado rodando num worktree limpo), `cargo clippy --lib` limpo (1 aviso pré-existente, nenhum novo), `npx vitest run` 126/126 (12 novos), `tsc --noEmit` limpo, `cargo build --release --lib` confirma que `libusb1-sys` linka certo contra o `libusb-1.0` do sistema. **Não validado**: hardware real (sem Trezor físico disponível) — registrado como P73, decisão explícita do dono do projeto de não ser cobrado por isso. | conversa direta + `/plan`, implementado Sessão 222 |

### P70 — wallet Arweave órfã após reinstalar: backup passa a embutir a chave (Sessão 221)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P70~~ | ~~Achado real ao validar o P69: o dono do projeto reinstalou/trocou de máquina, reconectou a wallet Ethereum e recuperou a identidade `@masterlxz`/Vault normalmente — mas a wallet Arweave não veio junto, uma nova foi gerada do zero, órfã de ~R$10 em AR já enviados pra wallet antiga (perda real, sem recovery possível — máquina/backup antigos não existem mais).~~ **Causa raiz**: a wallet Arweave é uma chave RSA-4096 gerada aleatoriamente por instalação (`rand::thread_rng()` no Desktop, `generateJwkInIsolate` no Mobile), sem nenhum vínculo com a identidade/wallet Ethereum, guardada só localmente (keyring do SO com fallback em arquivo texto puro no Desktop; `flutter_secure_storage` no Mobile) — e nunca entrava no backup exportável do Vault (`.truthid-backup`), que só carregava `entries`/`profile_names`/`device_permissions`. Nenhuma tela de exportar/revelar/importar essa chave existia em nenhuma plataforma (o comando `arweave_import_wallet`/`ArweaveWalletService.import()` já existia no backend dos dois lados, mas era código morto). **Decisão do dono do projeto**: a wallet passa a viajar dentro do backup criptografado do Vault já existente (mesma senha de export), em vez de uma tela separada de revelar/exportar. **Restrição de segurança que guiou a implementação**: o tipo `Vault` (Rust) e o `_VaultData`/`_serializeVaultData`/`_parseVaultJson` (Dart) são os mesmos usados pro `vault.enc` local, pro snapshot publicado no Arweave e pro sync entre devices pareados — a chave nunca podia entrar nesses tipos (vazaria pro ledger público e pro canal de pareamento). **Corrigido**: `vault_export_backup`/`vault_import_backup` (`desktop/src-tauri/src/lib.rs`) passam por `serde_json::Value` no meio do caminho — `splice_arweave_wallet_into_export`/`extract_arweave_wallet_from_import` inserem/extraem um campo extra `arweave_wallet_jwk` só no envelope do backup, nunca no struct `Vault`; mesmo padrão no Mobile (`VaultRepository.exportBackup`/`importBackup`, `vault_repository.dart`, com `_vaultDataToJsonMap`/`_vaultDataFromJsonMap` extraídos pra isolar a estrutura core do campo extra). **Garantia central**: import só restaura a wallet do backup se o device de destino ainda não tem nenhuma local (`get_arweave_wallet().is_err()`/`!arweave.exists()`) — nunca sobrescreve uma wallet local já existente (mesma classe do bug original, no sentido import→overwrite). Backups antigos (sem o campo) continuam importando normalmente. **Achado colateral corrigido no mesmo lugar**: os 3 fallbacks de segredo em texto puro do Desktop (`device.key`, `vault.key`, `arweave_wallet.json`) nunca tinham permissão de arquivo restrita (saíam com o umask padrão do sistema, mundo-legível) — `config.rs` ganhou `write_secret_file` (grava com `0o600` no Unix), usado nos 3 lugares. **Aviso adicionado na UI** (Desktop `ArweaveDashboard.tsx`, Mobile `wallet_screen.dart`, sempre visível quando a wallet existe) lembrando que a chave só existe neste device e precisa de backup. Testes: Rust `cargo test --lib` 206/206 (10 novos — splice/extract puros + permissão 0600), `cargo clippy --lib` limpo (1 aviso pré-existente, nenhum novo); Desktop `vitest run` 114/114 (1 novo), `tsc --noEmit` limpo; Mobile `flutter test` 601/601 (8 novos: 2 em `arweave_wallet_service_test.dart`, 5 em `vault_repository_test.dart`, 1 em `wallet_screen_test.dart`), `flutter analyze` sem issues novos. **Não recuperado**: o saldo específico que motivou o achado — sem a máquina/backup antigos, fica perdido pra sempre; este fix garante que não aconteça de novo. **Não validado**: round-trip completo manual no Desktop (gerar wallet → exportar → apagar → importar → confirmar mesmo endereço) e o caso cross-plataforma (backup do Desktop importado no Mobile e vice-versa) — consistência conferida por leitura direta do código (mesmo nome de campo, mesmo envelope, mesma cifra), mas não exercitada ao vivo neste ambiente. | conversa direta + `/plan`, achado P69 Sessão 221, implementado e testado na mesma sessão |

### P67 — Dashboard único com toggle ETH↔Arweave (Desktop + Mobile), implementado e testado (Sessão 220)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P67~~ | ~~`ArweaveWalletSection` (Desktop, dentro de `VaultSettings.tsx`/view "settings" da aba Vault)/`arweave_wallet_screen.dart` (Mobile, atrás de um ícone na `VaultScreen`) só mostravam endereço+saldo da wallet Arweave, sem histórico de transações e desconectadas da visão ETH (`SmartAccountDashboard.tsx`/`WalletScreen`).~~ **Implementado nas 2 plataformas, `/plan` completo rodado antes**: Rust ganhou `arweave_wallet_transactions` (`desktop/src-tauri/src/arweave/mod.rs`) — `POST /graphql` contra `{ARWEAVE_DEFAULT_NODE}/graphql` (mesmo host já usado, sem config nova), consulta `transactions(owners: [$owner], first: $first)`, v1 sem paginação (25 itens mais recentes). Desktop: `DashboardScreen.tsx` novo (toggle `.tabs` reaproveitado, içando `@{username}`) alterna entre `SmartAccountDashboard` (inalterado) e `ArweaveDashboard.tsx` novo (funde a gestão de wallet portada de `ArweaveWalletSection` + histórico novo); `VaultSettings.tsx` deletado, botão/view "settings" removidos de `VaultManagement.tsx`, i18n migrado (`vaultSettings.json`→`arweaveDashboard.json` + `dashboardScreen.json` novo, chaves órfãs removidas de `vaultManagement.json`). Mobile: `wallet_screen.dart` ganhou o mesmo toggle (`SegmentedButton`, sem precedente no projeto até aqui) incorporando o conteúdo de `arweave_wallet_screen.dart` (deletado) sem `Scaffold`/`AppBar` próprio (a tela já não tinha), histórico via `getWalletTransactions` novo em `arweave_client.dart` (mirror exato do Rust); ícone removido de `vault_screen.dart`; ARBs dos 6 idiomas (`en`/`es`/`pt`/`pt_BR`/`zh`/`zh_CN`) migrados (`arweaveWalletScreen*`→`walletScreenArweave*` + chaves de histórico/toggle novas), `flutter gen-l10n` rodado. **Testes verdes nos 2 lados**: Rust `cargo test --lib arweave::` 53/53 (4 novos, mock via `axum` — parsing completo, `block: null` de tx pendente, `edges` vazio, não-2xx), Desktop `vitest` 113/113 (8 novos: `ArweaveDashboard.test.tsx`, `DashboardScreen.test.tsx`) + `tsc --noEmit` limpo, Mobile suíte completa 593/593 (`flutter test` via o container Docker `mobile-flutter`, incluindo 4 novos em `arweave_client_test.dart` e 4 novos em `wallet_screen_test.dart`) + `flutter analyze` sem issues novos. **Não validado**: clique real na UI (Desktop nativo/celular físico) — só automatizado; histórico real de uma wallet Arweave com tráfego de verdade nunca foi visto na tela (testado só contra mocks). | conversa direta + `/plan`, implementado Sessão 220 |

### P66 — os 4 SDKs republicados com o creation code corrigido (Sessão 220)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P66~~ | ~~Dono do projeto reportou ao vivo (Sessão 219) que logar no Desktop com a wallet certa não puxava a identidade `@masterlxz`, e criar uma identidade nova ("fabiojr") quebrava o fluxo.~~ Causa raiz confirmada contra a Mainnet real (`cast call`, fora do app): `TRUTHID_ACCOUNT_CREATION_CODE` (bytecode do `TruthIDAccount` usado no cálculo local do endereço CREATE2) estava desatualizado desde antes do fix C8 — o commit da cascata (débito #52, Sessão 197) trocou só os *endereços* em `FACTORY_IMMUTABLES`, esqueceu de regenerar esse blob. Todo caminho que calcula o endereço da smart account localmente (em vez de ler `identity.controller` on-chain, imune ao bug) ficou afetado: Desktop e os 4 SDKs, que já tinham comentário dizendo "copied verbatim... keep in sync" mas nunca foram sincronizados de fato. Mobile não foi afetado (lê `identity.controller` direto). Nenhuma identidade ficou corrompida on-chain (`fabiojr` nunca chegou a existir — `getIdentity` revertia `IdentityNotFound`, o fluxo quebrou antes de completar). **Corrigido (Sessão 219)**: bytecode recompilado de `contracts/out/TruthIDAccount.sol/TruthIDAccount.json` nos 5 lugares (Desktop + 4 SDKs), validado batendo com o controller real via script standalone; vetor de paridade cross-linguagem recalculado e validado contra a factory Sepolia on-chain. Testes verdes: Desktop `vitest` 105/105, SDK TS/Python/Ruby 7/7 cada (Dart não rodou por falta de `dart`/`flutter` no host). **Sessão 220**: versão bumpada nos 4 SDKs (`truthid-sdk` TS/Python/Ruby `0.2.0`→`0.2.1`, `truthid_sdk` Dart `0.1.0`→`0.1.1`, `CHANGELOG.md` do Dart atualizado); testes rerodados e verdes nos 4 sob a versão nova, Dart pela 1ª vez (69/69, via o container Docker `mobile-flutter` já usado no P60/P62); dry-run de empacotamento limpo nos 4 (`npm publish --dry-run`, `python -m build`+`twine check`, `gem build`, `dart pub publish --dry-run`). **Publicado de verdade pelo dono do projeto em cada registry** (mesmo padrão do P60 — Claude preparou build/testes/dry-run, não manuseou login/token). **Achado real no caminho**: o comando de `dart pub publish` via Docker dado ao dono do projeto tinha esquecido o `--network host` — o servidor de callback OAuth do login Google sobe dentro da rede isolada do container, então o link `localhost` que o Google devolve não alcançava nada no navegador do host ("offline"); corrigido apontando o comando certo (`docker run --rm -it --network host -v $(pwd):/repo -w /repo/sdk/dart mobile-flutter:latest dart pub publish`), publish concluído depois disso. **Confirmado ao vivo via `curl` nos 4 registries**: npm `truthid-sdk@0.2.1`, PyPI `truthid-sdk@0.2.1`, RubyGems `truthid-sdk@0.2.1`, pub.dev `truthid_sdk@0.1.1` — todos com `latest` já na versão nova. | achado + fix de código Sessão 219; versão bumpada, dry-run e publish real Sessão 220 |

### P60 — os 4 SDKs publicados de verdade nos registries públicos (Sessão 218)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P60~~ | ~~`truthid-sdk` preso em `0.1.0` no npm/PyPI/RubyGems enquanto o código local já estava em `0.2.0`; `sdk/dart` (`truthid_sdk`) nunca publicado no pub.dev.~~ **Publicado nos 4 ecossistemas, cada um pelo dono do projeto com sua própria credencial** (Claude preparou build/testes/dry-run, não manuseou login/token de nenhum): `npm publish` (TS, `truthid-sdk@0.2.0`), `twine upload` (Python, `truthid-sdk@0.2.0`), `gem push` (Ruby, `truthid-sdk@0.2.0`), `dart pub publish` (Dart, `truthid_sdk@0.1.0`, 1ª publicação da história do pacote). Confirmado ao vivo via `curl` nos 4 registries depois de cada publish. **Achados reais no processo**: (1) `dart`/`flutter` não instalados no host — rodado via o mesmo container Docker (`mobile-flutter:latest`) já usado pro Mobile, montando o repo inteiro (`docker-compose.yml` do `mobile/` só monta `mobile/`, não serve pro SDK) e com `--network host` pro callback OAuth do `dart pub publish` (login Google via browser) alcançar o servidor local que o comando sobe dentro do container. (2) `sdk/dart` não tinha `CHANGELOG.md` — `dart pub publish --dry-run` apontava isso como warning (pub.dev recomenda); adicionado, dry-run foi de 1 warning pra 0. (3) **2 tentativas reais de `gem signin` erradas antes de acertar**: a 1ª customizou scopes e respondeu "y" pro `show_dashboard` (que o próprio prompt avisa ser exclusivo — zera todos os outros scopes), gerando uma key só de leitura de dashboard; a 2ª aceitou o scope default sem customizar — mas o default do `gem signin` (confirmado lendo `gemcutter_utilities.rb` do próprio RubyGems instalado) é **só `index_rubygems: true`**, nunca inclui `push_rubygem` — ou seja, `gem push` sempre falharia com "This API key cannot perform the specified action" a menos que se customizasse os scopes explicitamente marcando `push_rubygem: y` e `show_dashboard: n`. Only depois disso o `gem push` funcionou. | conversa direta, publish real feito pelo dono do projeto (Sessão 218) |

### P59/P61 — versão duplicada no Mobile (`_kAppVersion`) e no Desktop (`Cargo.toml`) corrigidas de vez; Mobile compilado via Docker pela 1ª vez desde a mudança de máquina (Sessão 217)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P59~~ | ~~Mobile: `_kAppVersion` hardcoded em `main.dart` era fonte duplicada da versão real (`pubspec.yaml`), achado investigando o P58 (Sessão 213), bloqueado até aqui por falta de Flutter no host pra adicionar uma dependência nova com segurança.~~ **Corrigido**: `package_info_plus` adicionado (pinado em `9.0.1`, não a última `10.2.1` — de propósito: `10.x` sobe a dependência de `win32` pra `^6.0.1`, incompatível com o `win32 ^5.9.0` que o `file_picker 10.3.6` já exige — `9.0.1` usa `win32 ^5.5.3`, satisfaz os dois). `main.dart::_checkForUpdate` agora lê `PackageInfo.fromPlatform().version` em vez da constante — mesmo ponto único onde `_kAppVersion` era usado, sem sobrar nenhuma fonte duplicada. **Achado nesta mesma sessão que destrava o P59 de verdade**: `flutter` não está instalado no host, mas o Mobile builda via Docker desde sessões bem anteriores (`mobile/dev.sh`, imagem `mobile-flutter` já existente) — as sessões 213/215/216 registraram o bloqueio de Flutter sem checar essa rota. `flutter pub get`/`flutter analyze` (6 avisos pré-existentes, nenhum novo)/`flutter test` (586/586)/`flutter build apk --debug` (build completo, `app-debug.apk` gerado) rodados dentro do container, todos limpos — o build final também exercita o `LocaleService`/seletor de idioma do P62, nunca compilado até aqui (ver nota atualizada em P62). | **Sessão 217** |
| ~~P61~~ | ~~`desktop/src-tauri/Cargo.toml` com os valores do template padrão do Tauri desde sempre (`version = "0.1.0"`, `description = "A Tauri App"`, `authors = ["you"]`), fazendo `local_signer_server.rs` reportar `desktop_version: "0.1.0"` nos endpoints `/truthid/v1/ping`/`/handshake` (consumidos pela extensão e por apps terceiros) em vez de `"2.0.0"`.~~ **Corrigido**: `version` → `2.0.0`, `description` → `"TruthID Desktop"`, `authors` → `["masterlxz"]`. **Decisão de escopo**: o campo `name = "tauri-app"` (nome do crate, também citado no achado original) foi deixado como está — diferente dos outros 3 campos, é puramente cosmético renomear e teria efeito colateral real (muda o nome do binário gerado pelo `cargo build`, consumido pelo bundler do Tauri antes de aplicar o `productName` do `tauri.conf.json`), risco desproporcional a um achado 🟡 Baixa cujo efeito prático (versão errada reportada) já fica resolvido só com o bump de `version`. `cargo check`/`cargo clippy --lib` limpos (1 aviso pré-existente de `type_complexity`, nenhum novo), `Cargo.lock` atualizado junto. | **Sessão 217** |

### P62 — Seletor manual de idioma no Desktop, Mobile e extensão (Sessão 216)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P62~~ | ~~Pedido direto do dono do projeto: as 3 superfícies fechadas no P49 (Desktop, Mobile, extensão) tinham i18n completo mas **sem seletor manual** — só detecção automática do idioma do SO/navegador. Faltava a UI de troca.~~ **Desktop**: trivial — infra do `i18next` já cacheava em `localStorage["truthid:language"]` assim que `changeLanguage()` rodasse (`desktop/src/i18n/index.ts`), só faltava a UI. `LanguageSelector.tsx` novo, dropdown na topbar reaproveitando a classe `.topbar-btn`. **Mobile**: nenhuma peça de troca de locale existia — `TruthIDApp` nunca passava `locale:` pro `MaterialApp`. `LocaleService` novo (mesmo padrão do `AppLockService`: `flutter_secure_storage` + `ValueNotifier` estático), `TruthIDApp` convertida pra ouvir via `ValueListenableBuilder`, item novo em `SettingsScreen` abrindo um bottom sheet (padrão do `_DonationSheet`). Chaves novas nos 6 ARBs, `check_arb_parity.py` confirmou paridade (619 chaves). **Extensão — a mais trabalhosa das 3**: `chrome.i18n` nativo **não tem equivalente a `changeLanguage()`** — sempre resolve a partir do idioma do navegador, sem parâmetro pra forçar outro. Um seletor manual de verdade exigiu abandonar `browser.i18n.getMessage` no popup inteiro (não só o `localizePopup()` do markup estático) e trocar por um dicionário próprio bundlado em build time (`extension/src/i18n/`, import direto dos `_locales/*/messages.json`, sem `fetch`/rede — trocar de idioma é sempre síncrono), replicando o algoritmo de substituição de placeholders (`$VERSION$`→`placeholders.version.content`→`$1`) que o `chrome.i18n` fazia. Isso significou migrar ~50 call sites espalhados em `main.ts`, `entryDetail.ts`, `vaultList.ts` e `entryForm.ts` pra uma função `t()` nova — sem isso, só os rótulos estáticos (`data-i18n`) mudariam de idioma, e todo o texto dinâmico (status, lista de senhas, formulário) ficaria preso no idioma do navegador, quebrando a promessa do seletor pela metade. `chrome.i18n`/`_locales` continuam existindo só pro nome/descrição da extensão na loja (fora do controle do popup). **Verificado**: `tsc --noEmit` limpo e os 101 testes do Desktop passam; `tsc --noEmit`, os 158 testes e o `wxt build` de produção da extensão passam (dicionários confirmados bundlados no `.js` final via grep). ~~**Mobile não compilado nesta sessão** — mesmo bloqueio do P59, `flutter` não está instalado neste ambiente; os `.arb`/Dart estão prontos e com paridade de chave confirmada, mas falta rodar `flutter gen-l10n`/`flutter run`.~~ **Atualização (Sessão 217)**: compilado e validado — ver P59, mesma sessão descobriu que o Mobile builda via Docker (`mobile/dev.sh`, imagem `mobile-flutter` já existia de sessões anteriores), sem precisar de Flutter no host. `flutter gen-l10n` rodou dentro do container sem problema (escreve como root dentro do bind mount, igual o host — dockerd deste ambiente não é rootless); `flutter build apk --debug` completo confirmou o `LocaleService`/seletor de idioma do Mobile compilando de ponta a ponta pela 1ª vez. | **Sessão 216** |

### P58 — Versionamento unificado v2.0.0 (Desktop/Mobile/extensão) + update-check na extensão (Sessão 213)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P58~~ | ~~Dono do projeto pediu versionamento único a partir de agora: `v2.0.0` marca tudo que mudou desde o `v1.0.0` (P57, P49, fixes de tema/basePath), patch/minor pra bugfix/feature pequena dentro do v2.x daqui pra frente. Antes as 3 versões estavam soltas (Desktop `1.0.0`, Mobile `1.0.0+1`, extensão `0.1.0`).~~ ~~Investigação prévia: Desktop (`desktop/src/hooks/useUpdateCheck.ts`+banner em `App.tsx`) e Mobile (tudo em `main.dart`) já tinham update-checker caseiro desde a **Sessão 45** — busca `releases/latest` no GitHub, compara semver, banner dispensável. A extensão não tinha nada — confirmado, zero tentativa (correto: extensão carregada via "Load unpacked" nunca recebe auto-update de verdade do Chrome, só Web Store/política de empresa).~~ **Feito**: bump pra `2.0.0` em `desktop/package.json`+`tauri.conf.json`, `mobile/pubspec.yaml`, `extension/package.json`. Extensão ganhou `extension/src/updateCheck.ts` (mesmo padrão, mas lê a versão via `browser.runtime.getManifest().version` — mais robusto que constante hardcoded) + banner no popup (reaproveita a classe `.pending-banner` já existente, sem CSS novo) linkando pra seção de instruções da landing (`#extension`), não pro `.zip` direto, já que não tem auto-update de verdade — o usuário precisa baixar e recarregar manualmente. ~~**3 achados reais no caminho**: (1) `mobile/lib/main.dart` tinha `_kAppVersion` hardcoded, fonte duplicada da versão real — só bump manual desta vez, fix de verdade fica pro P59 (Flutter não instalado nesta máquina). (2) `build-mobile.yml` **nunca gerava `lib/config/secrets.dart`** (gitignorado de propósito, guarda a API key do Pimlico — ver Débito #27 em `ARCHITECTURE.md`) — o CI sempre quebrava em tempo de compilação; o `app-release.apk` do `v1.0.0` deve ter sido buildado local e subido manual, nunca por este workflow. Corrigido copiando `secrets.example.dart` (fallback vazio, documentado como comportamento correto pra usuário final) antes do build. (3) Os nomes dos instaladores do Desktop mudaram de `tauri-app_X` pra `TruthID_X`/`TruthID-X` — não é o campo `name` do `package.json` (como o P57 tinha assumido), é o `productName` do `tauri.conf.json`, trocado de "tauri-app" pra "TruthID" em algum momento depois do `v1.0.0` sem nenhum release novo cortado até agora pra refletir isso.~~ Tag `v2.0.0` cortada 2 vezes (a 1ª sem o fix do CI do Mobile, release/tag apagados e recriados limpos antes de qualquer asset público existir). `gh release view` confirmou os 8 assets esperados; `curl` confirmou as 8 URLs com 200 antes de atualizar `site/frontend/lib/releases.ts` pro `v2.0.0` — mesmo processo que pegou o achado do `.dmg` no P57. `RELEASES_URL`/`_kReleasesUrl` do Desktop/Mobile já apontavam pra `releases/latest` (não uma tag fixa) — pegam o `v2.0.0` sozinhos, sem mudança de código. `npx tsc --noEmit`/`npx vitest run` (158/158, 4 novos) na extensão, `cargo check` no Desktop, `npm run build`/`lint` no site, limpos. | **Sessão 213** |

### P57 — Landing pública com instaladores, sem Chrome Web Store, raiz do site nos 4 idiomas (Sessão 213)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P57~~ | ~~Registrada na Sessão 212 sem `/plan`. `/plan` rodado nesta sessão: instaladores do Desktop reaproveitando o release `v1.0.0` já existente (sem cortar tag nova), identidade visual da doc (teal/cyan `#0e7490`/`#4dd0e1`, Inter+Space Grotesk), landing traduzida nos 4 idiomas (mesmo padrão dual-root do P49), virando a **raiz do site** (antes, `index.html` era só o intro da doc copiado por um `cp` no `deploy-docs.yml`).~~ **Arquitetura**: a raiz sem prefixo (`app/page.tsx`+`app/layout.tsx`, inglês) segue o mesmo padrão já validado pra doc (`app/docs/`) — a ocultação de prefixo do locale default não roda sob `output:"export"`. `app/[locale]/page.tsx` (que era a tela de login OAuth do dashboard pago) virou a landing pros outros 3 idiomas; o login mudou pra `app/[locale]/signin/page.tsx` (conteúdo movido, `dashboard/page.tsx` atualizado pros 2 links que apontavam pra `/`). `deploy-docs.yml` passou a remover `/signin`+`/dashboard` do build estático (em vez de `/page.tsx`+`/dashboard`) e não precisa mais do `cp docs/intro.html index.html`. ~~**3 achados reais**: (1) o release `v1.0.0` tinha todos os instaladores do Desktop mas **nenhum `.zip` da extensão** — gerado via `wxt zip` e anexado ao release existente (`gh release upload`); `build.yml` ganhou um job novo (`build-extension`) pra isso não precisar ser manual em releases futuras. (2) O `.dmg` do macOS só existe pra `aarch64` (Apple Silicon) — o job `macos-latest` do `build.yml` não gera build Intel; a copy da landing deixa isso explícito. (3) Os links internos (`/`, `/pt-BR`, `/docs`) usados como `<a href>` cru não recebiam o `basePath` (`/truthid`) que o GitHub Pages precisa — só descoberto comparando com os links da doc (que passam por `next/link`/Link do fumadocs e saem corretos); corrigido trocando por `next/link` em `landing-header.tsx`/`landing-content.tsx`. Achado **colateral, corrigido antes de subir**: `tauri-app_aarch64.dmg` (sem a versão no nome) não batia com o asset real (`tauri-app_1.0.0_aarch64.dmg`) — pego rodando `curl -I` em todas as 8 URLs de download uma por uma, 1 delas voltou 404.~~ ~~`content/docs/quickstart.mdx` (+ pt-BR/es/zh-CN) tinham uma linha desatualizada ("no pre-built downloads yet... build from source") — trocada por link pra landing nova.~~ `npm run build` (dinâmico e export estático com `NEXT_BASE_PATH=/truthid`, dry-run local) limpos, `eslint` limpo, `python3 scripts/check_mdx_parity.py` OK (18 páginas, paridade mantida), as 4 traduções confirmadas renderizando de verdade via `curl` no dev server (não fallback), as 8 URLs de download confirmadas 200 contra o GitHub real depois da correção do achado colateral. **Sem clique real de browser** (mesma lacuna já registrada pro Desktop/Mobile/site no P49). **Redesign na mesma sessão**: primeira versão (coluna estreita centralizada, cards cinzas com link sublinhado, sem explicar o que é o TruthID) julgada "feia e sem conteúdo" pelo dono do projeto, validado com screenshot real (Playwright headless, claro/escuro/mobile — não só `curl`, dessa vez olhando o resultado renderizado de verdade). Adicionada seção "Why TruthID" (3 cards explicando o valor: chave no seu device, vault criptografado, login sem senha), hero com eyebrow+headline+gradiente de fundo, `components/icons.tsx` novo (ícones genéricos — evitado logo aproximado à mão de SO/marca, risco de sair errado/feio sem referência), botões de download reais (ícone+label, não mais `<a>` sublinhado) com formato primário por SO + "outros formatos" secundário. Achado no meio do redesign, corrigido antes de screenshot final: 2 seções (`#desktop`/`#extension`) tinham `max-w-*` sem `mx-auto` — SectionHeading usa texto alinhado à esquerda dentro do container, então mesmo com a centralização do container (confirmada certa via `getBoundingClientRect()` no Playwright, margens simétricas) a posição do heading muda com a largura do container, o que parecia inconsistente à primeira vista. **Achado real reportado pelo dono do projeto clicando de verdade**: `docsCta` apontava pra `/docs` (e `/{locale}/docs`) sozinho — a fonte do fumadocs não mapeia página nenhuma pro slug vazio, só rotas explícitas (`intro`, `quickstart`, etc.), então dava 404 sempre; nenhuma das verificações anteriores (build, `curl` no dev server, screenshot) tinha clicado nesse link especificamente. Corrigido pra `/docs/intro`, confirmado via `curl` (404→200) nos dois lados. **2 achados reais reportados pelo dono do projeto usando a página de verdade, também presentes na doc (pré-existentes desde o P49, não introduzidos pelo P57, mas na mesma superfície compartilhada)**: (1) trocar de idioma voltava o tema pro claro mesmo com o escuro ativo — reproduzido com Playwright: a troca cruza dois root layouts físicos diferentes (`app/docs/` ↔ `app/[locale]/docs/`, mesmo padrão em `app/` ↔ `app/[locale]/`), e o router do Next faz transição client-side em vez de recarregar a página de verdade (confirmado via `performance.getEntriesByType("navigation")` sem entrada nova) — isso quebra o script anti-flash do `next-themes`, que só roda num load real; o valor certo continua salvo no `localStorage`, mas nada reaplica a classe `dark` na árvore nova. Uma navegação de verdade preserva o tema (confirmado antes de mexer no código). Corrigido trocando `router.push` por `window.location.href` no `onLocaleChange` (`locale-aware-root-provider.tsx`). (2) `LandingHeader`/`SiteHeader` nunca tiveram o `ThemeSwitch` do fumadocs (só a doc tinha, de graça, via `DocsLayout`) — adicionado nos dois. **Regressão do fix do tema, achada pelo dono do projeto no site publicado**: `usePathname()` já vem sem o `basePath` (convenção do Next) — o `window.location.href` montado nunca incluía `/truthid`, só funcionava em dev local (sem basePath nenhum), dando 404 no site de verdade. Corrigido expondo `NEXT_BASE_PATH` pro cliente via `env` do `next.config.ts` (`NEXT_PUBLIC_BASE_PATH`) e prependendo na URL. Desta vez validado contra o export estático de verdade servido sob `/truthid` (symlink + `serve`, não só o dev server, que não reproduz esse bug por não ter basePath) — 6 cenários via Playwright (landing↔doc, 4 idiomas, ida e volta), todos com URL certa e tema preservado. | **Sessão 213** |

### P49 — Internacionalização (i18n), FECHADA nas 4 superfícies (Sessões 208-211)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| ~~P49~~ | ~~Pedido original na Sessão 202. Escopo travado na Sessão 208 via `/plan`: 4 superfícies (Desktop, Mobile, extensão, site incluindo doc pública), idiomas en (base) + pt-BR/es/zh-CN, um mecanismo de i18n por superfície (Mobile: `flutter_localizations`+`intl`+ARB; Desktop: `i18next`+`react-i18next`; extensão: `chrome.i18n` nativo; site: Fumadocs i18n nativo pra doc + `next-intl` pro resto).~~ ~~**Extensão** fechada na Sessão 208 (`_locales/{en,pt,es,zh_CN}/messages.json`, 68 chaves, sem seletor manual — segue o idioma do navegador). **Desktop** fechado na Sessão 209 (478 chaves × 4 idiomas; achado e corrigido bug real de chave externa redundante que quebrava `t()` em 9 arquivos, inclusive no `PinApprovalModal` e no `ErrorBoundary`). **Mobile** fechado nas Sessões 209-211 (617 chaves × 4 idiomas via 6 lotes de extração paralelos; achado e corrigido bug real de `context.l10n` chamado em `initState()`, que lança em runtime; achado e corrigido bug no próprio script de paridade de ARB, que dava falso-positivo em plural ICU e em CJK colado). Extensão/Desktop/Mobile seguem a mesma decisão: sem seletor manual, só detecção automática.~~ ~~**Site fechado na Sessão 211**, a última superfície: `next-intl@4` + Fumadocs i18n nativo, roteamento por `[locale]`. **3 decisões/achados arquiteturais reais**: (1) Next.js 16 renomeou `middleware.ts`→`proxy.ts` — só descoberto lendo `node_modules/next/dist/docs/` direto, o `AGENTS.md` do próprio `site/frontend` avisa que a versão instalada tem breaking changes vs. treino; (2) o mecanismo que escondia o prefixo do locale default (`hideLocale`/`localePrefix:"as-needed"`) depende de rewrite em runtime, que não roda sob `output:"export"` (o build da doc pro GitHub Pages) — resolvido com dois root layouts independentes lado a lado (`app/docs/` inglês sem prefixo, intocado; `app/[locale]/docs/` pt-BR/es/zh-CN); (3) o switcher default do Fumadocs montava URL assumindo que todo locale tem prefixo, quebrando a volta pro inglês (`/en/docs/...`, rota inexistente) — corrigido com `onLocaleChange` customizado usando hooks nativos do Next em vez dos do next-intl (que dependem de contexto ausente na raiz em inglês). **Diferente das outras 3 superfícies**: site ganhou seletor manual de idioma (decisão desta sessão, conteúdo público/marketing) — a doc ganha de graça via `DocsLayout` do Fumadocs, landing/dashboard ganharam `components/site-header.tsx` novo. As 18 páginas MDX (~17.600 palavras) traduzidas nos 3 idiomas via 3 sub-agentes paralelos (um por idioma). **2 achados reais no processo de tradução**: memória do projeto estava errada dizendo que "vault" genérico traduzia pra "vault de senhas" em pt-BR/es — checado contra o que de fato foi publicado no Desktop/extensão, "Vault" fica sem tradução nesses 2 idiomas (só traduz em zh-CN, "密码库"), memória corrigida; e título de heading traduzido muda o slug do anchor auto-gerado (github-slugger), quebrando link interno — 52 headings-alvo levantados por cross-referência real contra os links `](#slug)` das 18 páginas, contagem de heading confirmada idêntica entre idiomas antes de alinhar por posição, e os 27 headings de prosa genuinamente em risco corrigidos nos 3 idiomas via a sintaxe `## Texto [#slug-original-em-ingles]` do Fumadocs (pin do ID pro slug em inglês, independente do texto traduzido).~~ ~~`scripts/check_mdx_parity.py` novo (mesma ideia do `check_arb_parity.py` do Mobile, adaptado pra MDX/JSX). Verificado: build dinâmico + `tsc` + lint limpos; dry-run local do export estático (mesmo `rm`+`NEXT_BASE_PATH` do CI) rodado 3 vezes ao longo da sessão, sempre limpo; conteúdo traduzido confirmado renderizando de verdade via `curl` no dev server (não fallback) nos 4 idiomas; anchors pinados confirmados no HTML exportado; switcher confirmado presente (via `curl`, checando `aria-label`/texto do locale ativo) na doc, na landing e no dashboard — **não confirmado**: nenhum clique real (sem ferramenta de browser neste ambiente), mesma lacuna já registrada pro Desktop/Mobile. Commitado em 2 commits (`7e9c332` infra/roteamento/switcher, `d331561` traduções+achados).~~ Com o site fechado, **P49 está fechado nas 4 superfícies**. **Atualização (Sessão 216)**: a frase acima sobre "Extensão/Desktop/Mobile... sem seletor manual" deixou de valer — as 3 ganharam seletor manual de idioma, ver P62. | conversa direta (Sessão 202); extensão Sessão 208; Desktop Sessão 209; Mobile Sessões 209-211; site Sessão 211 | **Sessão 211** |

### P51 — aviso de "vault legado" não cobria o caso de falha total (sem cache, sem permissão de escrita) (Sessão 207)

| ID | Item | Onde se originou | Prioridade |
|---|---|---|---|
| ~~P51~~ | ~~`_buildLegacyStorageBanner()` em `vault_screen.dart` (Mobile) só renderiza atrás de `if (_canWrite && _legacyIpfsCid)`, escrito pra outro cenário (S201: device que **já tem** cache e só quer o nudge de republish). O caso real achado na Sessão 204 é diferente — um device sem cache local (read-only ou não) tentando resolver um vault ainda no cid IPFS legado cai direto em `VaultSyncStatus.syncFailedNoCache`, mostrando o erro técnico cru dos gateways IPFS sem nenhuma explicação. Causa raiz em `VaultSyncService.sync()`: a variável `ref` (que tem o cid on-chain) só existia dentro do escopo do `try`, invisível no `catch` que chama `_fallbackToCache()` — não tinha como saber ali se a falha era por causa do esquema legado ou de outra coisa qualquer.~~ **Corrigido**: ~~`ref` foi hoisted pra fora do `try` (`VaultRef? ref;` antes do bloco, atribuído sem `final` dentro dele) — o `catch` agora consegue checar `ref != null && !ref.cid.startsWith('ar://')` e propaga isso como `legacyIpfsCid` pro `_fallbackToCache()`, que passou a aceitar o parâmetro e repassá-lo pros dois outcomes que produz (`offlineUsingCache` e `syncFailedNoCache` — antes só os dois caminhos de sucesso setavam o campo). Em `vault_screen.dart`, o branch de `syncFailedNoCache` bifurca: com `legacyIpfsCid` verdadeiro, mostra "Vault on legacy storage" com instrução acionável (pedir pra outro device com cache sincronizado republicar pro Arweave) em vez do erro cru — independente de `_canWrite`, já que mesmo um device com permissão de escrita não consegue republicar sem ter cache nenhum pra ler.~~ **Fora de escopo, decidido por ser o fix mais estreito que cobre o cenário real relatado**: ~~não foi criada uma variante do banner informativo pra devices read-only com cache (a 2ª alternativa cogitada no achado original) — o caso descrito na S204 era falha total (sem cache), não um device read-only que já sincroniza normalmente.~~ ~~`flutter test` 586/586 (2 novos: um em `vault_sync_service_test.dart` provando que a detecção olha o prefixo do cid — não fica `true` incondicional só por `ref` existir — e um em `vault_screen_test.dart` confirmando a mensagem nova substitui a antiga), `flutter analyze` limpo (mesmos 6 avisos pré-existentes).~~ | conversa direta (Sessão 204), corrigido Sessão 207 | **Sessão 207** |

### P54 — mismatch real de vault key Desktop↔device pareado, fechado e validado em hardware (Sessões 205/206)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P54~~ | ~~Achado na Sessão 205 validando o P52 num celular físico (Z Flip, `SM-S731B`, device "S25 FE teste", `0x9f0E2AF37dcEA864F9F5DA7891AcdDC3d4A0A545`): a chave que o celular recuperava via `deviceVaultKeys` não batia com a que o Desktop usa pra publicar — `SecretBoxAuthenticationError` mesmo com o conteúdo certo (hash batendo contra o `contentHash` on-chain). Investigação da Sessão 206 (sem hardware disponível): `cast call deviceVaultKeys(...)` confirmou blob ECIES válido (93 bytes) on-chain, descartando "campo Encryption key em branco". Achado mais forte por forense de arquivos em `~/.truthid/`: `vault.key` (fallback em arquivo de `get_vault_key()`) tinha conteúdo idêntico ao keyring do SO mas foi escrito em `2026-08-12` (mesmo dia do pareamento deste device) e era **dono por `root`** — junto com `arweave_wallet.json` também de `2026-08-12`/dono `root` — sinal de uma execução com sessão/contexto diferente do usuário normal, sem acesso ao keyring da sessão do usuário, rodando naquele dia. Hipótese de causa raiz: se o keyring também falhasse pra LEITURA nesse mesmo contexto durante `encrypt_vault_key_for_device`, a função (que na época não tinha fallback estrito) caía silenciosamente pro tier 3 — a chave legada derivada da device key, nunca da wallet — entregando ao celular uma chave que nunca poderia bater com o vault real. **Corrigido independente da confirmação definitiva da causa**: `get_current_vault_key_strict()` novo (Rust) — `encrypt_vault_key_for_device` nunca mais cai silenciosamente pro fallback legado, erra explicitamente em vez disso. Novo caminho de recuperação sem revogar/re-parear nada: `RedistributeVaultKey.tsx` (Desktop) reenvia a vault key atual pra um device já registrado, só precisando colar a chave pública crua dele de novo (mesmo campo do pareamento) — mobile ganhou um botão "Show encryption key" novo (`devices_screen.dart`) pra reexibir esse valor num device já pareado, que antes só existia na tela de pareamento inicial. **Validado ao vivo na Sessão 206**: dono do projeto conectou a Ledger física, confirmou a transação `updateDeviceVaultKey`, `cast call` confirmou o blob on-chain mudou; no celular (depois de contornar 2 obstáculos de ambiente sem relação com o fix — tela travando sozinha entre comandos adb, exigindo desbloqueio manual repetido, e o bug conhecido do Impeller/tela preta da Sessão 123, resolvido com `am force-stop`+reabrir), a aba Vault carregou as entradas reais (`127.0.0.1`, `Casa Teste`, `Nubank Teste`) sem nenhum erro de decifra. Fecha o P54 de ponta a ponta. Achado colateral sem relação direta, registrado à parte: P56 (rotação de DEK ao revogar device nunca funcionou, corrigido no mesmo caminho).~~ | **Sessão 206** |

### P55 — crash real no Mobile: `canWriteVault()`/`pendingChanges()` sem proteção travavam a tela do Vault pra sempre (Sessão 205)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P55~~ | ~~Achado ao vivo validando o P52 num celular físico (Z Flip): `_VaultScreenState._load()` (`vault_screen.dart`) chama `_syncService.sync(...)` primeiro — que já trata decifra local falhando graciosamente (`syncFailedNoCache`) — mas logo depois chamava `_repository.canWriteVault(address)` e `_repository.pendingChanges()` **sem nenhum try/catch**, cada uma fazendo sua própria leitura+decifra do `vault.enc` local. Com o cache local desse celular ilegível (ver P54), essas duas chamadas lançavam `SecretBoxAuthenticationError` direto, derrubando `_load()` inteira ANTES do `setState` — a tela ficava girando o spinner pra sempre, mesmo quando `sync()` já tinha resolvido tudo certo. Corrigido envolvendo as duas chamadas num try/catch com fallback seguro (`canWrite = false`, `pending = 0`), mesma filosofia que `VaultSyncService._fallbackToCache` já usa. `flutter analyze` limpo, `flutter test` 584/584 (1 teste novo, simula as duas chamadas lançando e confirma que a tela mostra o resultado do sync em vez de travar). **Validado em hardware real**: rebuild do APK debug + `adb install -r` no celular físico — a tela passou a mostrar "Could not load your vault" com o erro real em vez de ficar carregando pra sempre.~~ | **Sessão 205** |

### P52/P53 — vault de `@masterlxz` destravado do CID IPFS morto; achado sistêmico de devices não reautorizados pós-cascata (Sessão 205)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P52~~ | ~~**Vault real de `@masterlxz` (identityId 1) preso no CID IPFS morto**~~ — causa raiz era dupla, as duas corrigidas e validadas ao vivo. **(1)** `IdentityContext.tsx` só expunha `smartAccountAddress` derivado de `useAccount().address` (CREATE2 síncrono, passado de `App.tsx`) — só existe se uma wallet estiver conectada *nesta sessão do app*. O botão "Publicar via device key (sem Ledger)" herdava esse mesmo valor; sem wallet conectada (justo o cenário que o botão promete cobrir), `smartAccountAddress` ficava `null` → "Nenhuma identidade carregada". Corrigido priorizando `identity.controller` — já devolvido pela leitura on-chain de `getIdentity(username)` que o contexto já fazia, é o mesmo endereço da smart account e só depende do username em cache local. **(2)** Corrigido esse ponto, apareceu um erro novo e mais fundo — `AA24 signature error` — ver P53 abaixo. Depois do fix do P53, republish confirmado on-chain de ponta a ponta, independente do app: `getVault(1)` no `VaultRegistry` (`0x0744...849b4`, Base Mainnet) foi de `QmeY4no...` (versão 1, IPFS morto) pra `ar://9_sUbYf4K9w9bOxqkzjIf_r8v4OvhbMTEcwDNHGq-TM` (versão 2, Arweave), `updatedAt` batendo com o horário real do teste (16/08/2026 19:03 UTC).~~ | **Sessão 205** |
| ~~P53~~ | ~~**Achado sistêmico: nenhum device consegue assinar UserOp pra `@masterlxz` desde a cascata da Sessão 197** — `DeviceRegistry.migrateDevices()` (ver [[project-cascade-migration-197]]) migrou os *registros* (label/addedAt/revoked/chave ECIES) pro `DeviceRegistry` novo, mas nunca chamou `TruthIDAccount.addDevice()` na smart account nova, que é uma mapping separada (`authorizedDevices`) dentro do próprio contrato da conta — é ela, não o `DeviceRegistry`, que `_validateSignature` checa de fato. Confirmado via `cast call` num RPC público: dos 7 devices listados em `getDevicesByIdentity(1)`, todos vinham `authorizedDevices() == false` na smart account atual (`0xAa45...eDa05`) antes do fix — só a Ledger (owner) conseguia assinar qualquer coisa; Mobile e Desktop via device key estavam quebrados pra essa identidade desde 13/08/2026, não só o publish do vault. Achado colateral: `DesktopDevice.tsx` só checava `DeviceRegistry.isDeviceActive()` (global) pra decidir se mostrava "✓ Registered" ou o botão de registro — como isso já vinha `true` da migração, o botão nem aparecia, sem nenhum caminho de UI pra corrigir. Corrigido em `ManageDevices.tsx`: nova leitura de `authorizedDevices()` por device na smart account atual (`useReadContracts`), filtrando os já revogados; se detectar a lacuna, mostra banner + botão "Reautorizar N devices" — um `executeBatch` só, uma confirmação na Ledger, sem re-parear nada (pubkeys já conhecidos on-chain). `tsc --noEmit` limpo, `npx vitest run` 101/101. **Validado ao vivo pelo dono do projeto**: dos 7 devices, os 4 ativos (3 já revogados antes da migração corretamente ignorados pelo lote) foram de `false` pra `true` em `authorizedDevices()`, confirmado via `cast call` independente do app.~~ | **Sessão 205** |

### P50 — bug real de scan de QR no Mobile, corrigido e validado em hardware (Sessão 204)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P50~~ | ~~Abrir a câmera de QR no Mobile (`mobile/lib/screens/scan_screen.dart`, usado pelo pareamento de leitura da extensão entre outros fluxos) sempre falhava com "Unexpected error occurred" em hardware real (Samsung Galaxy S25 FE, Android 16/API 36) — confirmado via `logcat` que não era permissão (já concedida): `NoSuchMethodException: com.google.mlkit.vision.common.internal.VisionCommonRegistrar.<init>`, causado por um bug conhecido do pacote `mobile_scanner` 6.x onde o R8 (modo full, padrão desde AGP 8.0) corrompe/obfusca classes do ML Kit acessadas via reflexão — corrigido oficialmente rio-acima na versão 7.2.1 (`CHANGELOG.md` do pacote, confirmado via fetch direto do GitHub). Corrigido atualizando `mobile_scanner: ^6.0.0` → `^7.4.0` (mais recente, sem breaking change relevante pro uso deste projeto — só `MobileScanner`/`BarcodeCapture`/`analyzeImage`, nenhuma API removida/alterada em uso). Aproveitado pra adicionar um `errorBuilder` de verdade em `ScanScreen` (não existia nenhum tratamento de erro antes, caía no fallback genérico do pacote sem nenhum diagnóstico). `flutter analyze` limpo, `flutter test` 582/582. **Validado em hardware real**: rebuild do APK debug + reinstalação via `adb install -r` no device físico, scan de QR real confirmado funcionando pela pessoa dona do projeto.~~ | **Sessão 204** |

### P46/P11 — decisões do dono do projeto: fecha investigação do bug do ⭐, define modelo de consentimento do /pin (Sessão 202)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P46~~ | ~~Botão ⭐ (favoritar)/"Baixar" não responde a clique de forma confiável — **fechado por decisão do dono do projeto, não por fix**: a investigação da Sessão 182 já tinha esgotado o que dava pra provar sem clique físico real ou devtools (mecanismo funciona quando o clique acerta, causa raiz é a fragilidade do alvo, não o handler). Sem hardware/ambiente novo pra investigar mais fundo, o dono do projeto decidiu não seguir tentando — risco aceito como conhecido, registrado, não escondido. Não desbloqueia o P37 (mesma fragilidade de clique continua impedindo a validação de leitura de documentos), só encerra a linha de investigação em si.~~ | **Sessão 202** |
| ~~P11~~ | ~~Modelo de consentimento do `/pin` decidido pelo dono do projeto: **aprovação por chamada**, mesmo padrão do `/sign-message`/`/sign-request` — sem autorização persistida por app nem cota diária. Achado no caminho: o modelo até então implementado no Desktop (`pin.rs`, autorização por app + `DEFAULT_DAILY_LIMIT=50`/dia, "fatia 3" da Sessão 106) nunca tinha sido registrado como resolvido aqui, apesar de já estar em produção desde 2026-07-17 — o Mobile (fluxo cross-device via QR) sempre foi por chamada, então os dois lados divergiam até esta sessão. `pin.rs` reescrito seguindo o padrão de `sign_message.rs` (state vira só `SingleSlotChannel`, sem arquivo `pin_authorizations.json`); comandos Tauri `pin_get_authorizations`/`pin_revoke_authorization`/`pin_set_daily_limit` e a seção `PinAuthorizationsSection` de `VaultSettings.tsx` removidos (ficaram órfãos); `PinApprovalModal.tsx` simplificado pro texto único (sem distinção "app novo" vs "cota estourada"), mirror do `SignMessageModal.tsx`. Doc pública (`concepts/cross-device-and-storage.mdx`, `apps/mobile.mdx`, escritas no P48) corrigida — descreviam o modelo antigo como fato atual. `cargo test --lib` 182/182, clippy limpo, `tsc --noEmit`/`npx vitest run` (101/101) limpos, `npm run build` do site limpo.~~ | **Sessão 202** |

### P48 — Expansão de conteúdo da doc (Sessão 200, continuação)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P48~~ | ~~8 páginas novas em `site/frontend/content/docs/` (`concepts/{vision,how-it-works,vault,recovery,cross-device-and-storage}`, `apps/{desktop,mobile,extension}`, `structure.mdx`), com conteúdo verificado direto no código-fonte (`contracts/`, `mobile/`, `desktop/`, `extension/`, `project/CONTEXT.md`) por 3 buscas de pesquisa em paralelo antes de escrever qualquer linha. Aproveitado pra corrigir erros reais já publicados nas 10 páginas antigas: contrato faltando (`VaultRegistry`, eram descritos "quatro contratos", são seis), claim errado de iOS Secure Enclave (não suporta secp256k1, a curva que os device keys realmente usam), link quebrado pro `PROJECT_STATE.md` (arquivo não existe mais), audit status incompleto (faltava a reentrância C1 do `RecoveryManager`), instalação do SDK Dart apontando pra pacote nunca publicado no pub.dev, "immutable" simplificado demais (sem proxy, mas com `updateRegistries` que já habilitou 4 redeploys em cascata). Cada tópico tem um dono canônico (`contracts.mdx` pra endereços/funções, `how-it-works.mdx` pro fluxo do protocolo) — as outras páginas linkam em vez de duplicar. Nada de monetização/pricing, conforme a decisão da mesma sessão. `npm run build`/`npm run lint` limpos; screenshots confirmaram sidebar, tabelas, callouts e navegação prev/next renderizando certo~~ | **Sessão 200** |

### P1/P2/P26 — Redeploy em cascata do débito #52, migração real de identidade na Mainnet

| ID | Item | Resolvida em |
|---|---|---|
| ~~P1~~ | ~~Deploy em cascata (`DeviceRegistry` débito #52) — 6 contratos redeployados em Sepolia (rehearsal) e Mainnet (`IdentityRegistry`, `DeviceRegistry`, `RecoveryManager`, `TruthIDAccountFactory`, `SessionRegistry`, `VaultRegistry`), todos com os fixes C1-C9. Novo `DeviceRegistry.migrateDevices()` (função de uso único, 9 testes novos) portou os 7 devices da identidade real (`masterlxz`, Sessão 116) numa única transação — status idêntico ao legado (3 revogados, 4 ativos), sem re-pareamento físico. Smart account mudou de endereço (`0x6689...` → `0xAa45...`, inevitável — CREATE2 depende do endereço da factory, e a conta antiga tinha os registries `immutable`, sem `updateRegistries`). Saldo ETH e ponteiro do vault (mesmo CID) migrados via `execute()` direto da Ledger. Endereços novos propagados em Desktop/Mobile/4 SDKs/docs/READMEs~~ | **Sessão 197** |
| ~~P2~~ | ~~Deploy do `RecoveryManager` corrigido (C1 — reentrância) — parte da mesma cascata~~ | **Sessão 197** |
| ~~P26~~ | ~~Assinatura de sessão desalinhada com o fix C4 (domain separation) — `SESSION_DOMAIN_SEPARATION_ENABLED`/`sessionDomainSeparationEnabled` ligadas em Desktop e Mobile agora que o `SessionRegistry` novo (com o fix C4) está live~~ | **Sessão 197** |

### Débitos Técnicos (1–53)

| ID | Arquivo(s) | Problema | Resolvida em |
|---|---|---|---|
| ~~D1~~ | ~~`desktop/src/components/ManageDevices.tsx`~~ | ~~347 linhas misturando 3 responsabilidades~~ | **Sessão 39** |
| ~~D2~~ | ~~`mobile/lib/services/blockchain_service.dart`~~ | ~~ABI inline como string JSON~~ | **Sessão 41** |
| ~~D3~~ | ~~`sdk/typescript/src/client.ts:22`~~ | ~~`private publicClient: any`~~ | **Sessão 41** |
| ~~D4~~ | ~~`desktop/src/components/ManageDevices.tsx:133`~~ | ~~`DeviceInfo` type local~~ | **Sessão 39** |
| ~~D5~~ | ~~Desktop (React geral)~~ | ~~Nenhum ErrorBoundary~~ | **Sessão 41** |
| ~~D6~~ | ~~Desktop (React geral)~~ | ~~Estado todo local sem compartilhamento~~ | **Sessão 41** |
| ~~D7~~ | ~~Desktop + Mobile~~ | ~~Zero testes de UI/frontend~~ | **Sessão 43** |
| ~~D8~~ | ~~Desktop (UX/layout)~~ | ~~Posição/organização das telas~~ | **Sessão 40** |
| ~~D9~~ | ~~`ConnectLedger.tsx`~~ | ~~Tela de espera sem hierarquia visual~~ | **Sessão 40** |
| ~~D10~~ | ~~`ConnectLedger.tsx`~~ | ~~Seletor de conta sem endereços~~ | **Sessão 40** |
| ~~D11~~ | ~~`sdk/typescript/`~~ | ~~Registro de sessão incompleto no SDK~~ | **Sessão 39** |
| ~~D12~~ | ~~wagmi auto-reconnect~~ | ~~Reconectava Ledger automaticamente~~ | **Sessão 41** |
| ~~D13~~ | ~~Site de docs (Fase 8)~~ | ~~Sem seção Session Registration~~ | **Sessão 42** |
| ~~D14~~ | ~~`devices_screen.dart`~~ | ~~Não detectava pareamento automaticamente~~ | **Sessão 46** |
| ~~D15~~ | ~~`show_device_qr_screen.dart`~~ | ~~Sem botão de retry no polling~~ | **Sessão 46** |
| ~~D16~~ | ~~Desktop + Mobile~~ | ~~Sem mecanismo de doação~~ | **Sessão 47** |
| ~~D17~~ | ~~`IdentityRegistry.sol:80`~~ | ~~`createIdentity` sem validação de controller~~ | **Sessão 62** |
| ~~D18~~ | ~~`TruthIDAccount.sol`~~ | ~~`abi.decode` revertendo em vez de `SIG_VALIDATION_FAILED`~~ | **Sessão 55** |
| ~~D19~~ | ~~`RecoveryManager.sol`~~ | ~~`emergencyWithdraw` inalcançável~~ | **Sessão 68** |
| ~~D20~~ | ~~`TruthIDAccount.sol:69`~~ | ~~Constante `_SECP256K1N_DIV_2` com dígito faltante~~ | **Sessão 55** |
| ~~D21~~ | ~~`TruthIDAccountFactory.sol`~~ | ~~`createAccount` recomputando hash desnecessariamente~~ | **Sessão 61** |
| ~~D22~~ | ~~`TruthIDAccountFactory.sol` + test~~ | ~~Assembly `extcodesize` duplicado~~ | **Sessão 61** |
| ~~D23~~ | ~~`Deploy.s.sol` + tests~~ | ~~`ENTRY_POINT_V07` hardcoded em múltiplos arquivos~~ | **Sessão 61** |
| ~~D24~~ | ~~`TruthIDAccountFactory.sol:40`~~ | ~~4 erros customizados separados~~ | **Sessão 61** |
| ~~D25~~ | ~~`TruthIDAccountFactory.sol:97`~~ | ~~`_salt` sem suporte a múltiplas contas por owner~~ | **Sessão 68/69** |
| ~~D26~~ | ~~`TruthIDAccountFactory.t.sol:40`~~ | ~~Helper não reusado~~ | **Sessão 61** |
| ~~D27~~ | ~~`pimlico_bundler_client.dart` + `secrets.dart`~~ | ~~API key do Pimlico embutida no build~~ | **Sessão 68** |
| ~~D28~~ | ~~`IdentityRegistry.sol` (deployado)~~ | ~~Factory chamada com seletor antigo de 1 argumento~~ | **Sessão 70** |
| ~~D29~~ | ~~`computeSmartAccountAddress.ts`~~ | ~~`encodeAbiParameters` em vez de `encodePacked` no salt~~ | **Sessão 70** |
| ~~D30~~ | ~~`blockchain_service.dart` + `devices_screen.dart`~~ | ~~`getUsernameForIdentity` sem `fromBlock`/`toBlock` e fire-and-forget~~ | **Sessão 70** |
| ~~D31~~ | ~~`mobile/docker-compose.yml`~~ | ~~Keystore de debug não persistida~~ | **Sessão 70** |
| ~~D32~~ | ~~`mobile/lib/services/blockchain_service.dart`~~ | ~~Struct com campo dinâmico no `web3dart`~~ | **Sessão 70** |
| ~~D33~~ | ~~`VaultManagement.tsx` + `VaultRegistry.sol:71`~~ | ~~`writeContract` direto em vez de via `TruthIDAccount.execute()`~~ | **Sessão 78** |
| ~~D34~~ | ~~`vault_key_service.dart:23`~~ | ~~Chave do vault derivada da device key em vez da wallet~~ | **Sessão 76** |
| ~~D35~~ | ~~`VaultManagement.tsx:386`~~ | ~~`invoke` com chaves snake_case em vez de camelCase~~ | **Sessão 79** |
| ~~D36~~ | ~~`VaultManagement.tsx:317`~~ | ~~Falha parcial de pin tratada como sucesso~~ | **Sessão 80** |
| ~~D37~~ | ~~`VaultSettings.tsx:70`~~ | ~~`healthStatus` indexado por posição, sem reindex~~ | **Sessão 81** |
| ~~D38~~ | ~~`vault_repository.dart:155`~~ | ~~`updateEntry` não verificava existência do ID~~ | **Sessão 82** |
| ~~D39~~ | ~~`VaultManagement.tsx:288`~~ | ~~`useEffect` sem `isConnected` nas dependências~~ | **Sessão 83** |
| ~~D40~~ | ~~`VaultSettings.tsx:90`~~ | ~~API key não validada no formulário PSA~~ | **Sessão 84** |
| ~~D41~~ | ~~`VaultRegistry.sol:71`~~ | ~~`updateVault` sem validação de `contentHash` zero~~ | **Sessão 85** |
| ~~D42~~ | ~~`VaultRegistry.sol:117`** + contracts~~ | ~~`_getCallerIdentityId()` duplicado em 3 contratos~~ | **Sessão 86/88** |
| ~~D43~~ | ~~`VaultManagement.tsx:199`~~ | ~~Orquestração de publish inline no componente~~ | **Sessão 87** |
| ~~D44~~ | ~~`CreateIdentity.tsx`~~ | ~~Fluxo travado se tx2/tx3 falha (nonce desatualizado)~~ | **Sessão 91** |
| ~~D45~~ | ~~`ConnectLedger.tsx`~~ | ~~Chamadas HID concorrentes + falta de timeout~~ | **Sessão 90** |
| ~~D46~~ | ~~`VaultSettings.tsx` (guia Kubo)~~ | ~~CORS ausente do guia de setup do Kubo~~ | **Sessão 91** |
| ~~D47~~ | ~~`mobile/lib/contracts/abis.dart`~~ | ~~`deviceVaultKeys` ausente do ABI~~ | **Sessão 92** |
| ~~D48~~ | ~~`desktop/src-tauri/src/lib.rs`~~ | ~~SHA-256 ausente antes da chave AES (ECIES)~~ | **Sessão 92** |
| ~~D49~~ | ~~`device_key_service.dart`~~ | ~~Race condition na criação da chave~~ | **Sessão 92** |
| ~~D50~~ | ~~`device_key_service.dart`~~ | ~~Prefixo SEC1 `0x04` ausente~~ | **Sessão 92** |
| ~~D51~~ | ~~`PairDevice.tsx`~~ | ~~Botão travado após erro on-chain~~ | **Sessão 92** |
| ~~D52~~ | ~~`DeviceRegistry.sol:103`~~ | ~~Impossibilidade de re-registro após revogação~~ | **Sessão 118** |
| ~~D53~~ | ~~`blockchain_service.dart`~~ | ~~Sem fallback entre RPCs~~ | **Sessão 93** |

### Correções de Segurança (Auditoria Fase 6)

| ID | Contrato | Achado | Resolvida em |
|---|---|---|---|
| ~~S1~~ | ~~IdentityRegistry~~ | ~~`setRecoveryManager` sem controle de acesso~~ | **Sessão 24** |
| ~~S2~~ | ~~SessionRegistry~~ | ~~`createSession` permissionless~~ | **Sessão 24** |
| ~~S3~~ | ~~RecoveryManager + IdentityRegistry~~ | ~~Falta validação de `address(0)` em `newController`~~ | **Sessão 24** |
| ~~S4~~ | ~~IdentityRegistry~~ | ~~`transferController`/`recoverController` sem validação~~ | **Sessão 24** |
| ~~S5~~ | ~~RecoveryManager~~ | ~~Guardians não zerados após recovery~~ | **Sessão 24** |
| ~~S6~~ | ~~RecoveryManager~~ | ~~Array de guardians sem limite~~ | **Sessão 24** |
| ~~S7~~ | ~~DeviceRegistry~~ | ~~Front-running do `devicePubKey`~~ | **Sessão 24** |

### Correções de Contratos (Review Sessão 140 — C1 a C9)

| ID | Achado | Resolvida em |
|---|---|---|
| ~~C1~~ | ~~Reentrância em `RecoveryManager.executeRecovery`~~ | **Sessão 150** |
| ~~C2~~ | ~~Revogar device não desautoriza na `TruthIDAccount`~~ | **Sessão 118** |
| ~~C3~~ | ~~`executeRecovery` não toca no `DeviceRegistry`~~ | **Sessão 118** |
| ~~C4~~ | ~~Replay cross-chain no `SessionRegistry.createSession`~~ | **Sessão 150** |
| ~~C5~~ | ~~`setRecoveryManager` sem checagem de endereço zero~~ | **Sessão 140** |
| ~~C6~~ | ~~3 arrays por identidade sem limite~~ | **Sessão 150** |
| ~~C7~~ | ~~`configureGuardians` aceita guardians duplicados~~ | **Sessão 140** |
| ~~C8~~ | ~~Cascata de endereços imutáveis não documentada~~ | **Sessão 140** |
| ~~C9~~ | ~~`try/catch` do `emergencyWithdraw` engole revert~~ | **Sessão 140** |

### Correções de Mobile (Review Sessão 151 — M1 a M10)

| ID | Achado | Resolvida em |
|---|---|---|
| ~~M1~~ | ~~Deep link/QR bypassava `AppLockService` — telas de aprovação empurradas por cima do bloqueio~~ | **Sessão 152** |
| ~~M2~~ | ~~Login sem checagem de `expiresAt` — `approval_screen.dart` não validava expiração do challenge~~ | **Sessão 153** |
| ~~M3~~ | ~~TOCTOU em `markPublished` — recarregava o vault atual do disco em vez do conteúdo de fato publicado; corrigido em Mobile e Desktop (mesmo padrão nos dois)~~ | **Sessão 154** |
| ~~M4~~ | ~~Future rejeitada cacheada pra sempre em `DeviceKeyService._getOrCreateKey` — falha transiente na 1ª leitura da secure storage quebrava toda assinatura até reiniciar o app~~ | **Sessão 155** |
| ~~M5~~ | ~~Sem guarda de reentrância — sign_request — duplo toque podia submeter 2 UserOperations~~ | **Sessão 156** |
| ~~M6~~ | ~~Sem guarda de reentrância — pin — duplo toque disparava 2 fluxos concorrentes de pin/entrega~~ | **Sessão 156** |
| ~~M7~~ | ~~Sem guarda de reentrância — vault edit — guarda `_entryPersisted` checada só após `await`, duplo toque podia criar entrada duplicada~~ | **Sessão 156** |
| ~~M8~~ | ~~Bug de username reintroduzido — `devices_screen.dart` reimplementava inline em vez de usar `resolvePairedUsername()`, só resolvia na auto-descoberta~~ | **Sessão 157** |
| ~~M9~~ | ~~`expiresAt` ignorado no canal de deep link — `DeepLinkDeliveryChannel.deliver()` recebia o parâmetro mas nunca checava, mascarado pelos chamadores~~ | **Sessão 158** |
| ~~M10~~ | ~~`IndexedStack` construía as 4 abas de uma vez no cold start — Devices/Sessions/Wallet/Vault cada uma chamando `getDevice` redundante pro mesmo endereço~~ | **Sessão 159** |

### Atualização dos SDKs (TypeScript, Python, Ruby — Sessão 160)

| ID | Item | Resolvida em |
|---|---|---|
| ~~SDK1~~ | ~~`registerSession`/`register_session` (relayer server-side) removido dos 3 SDKs — morto na prática desde que o mobile passou a criar a sessão on-chain sozinho via UserOp (14.9.5)~~ | **Sessão 160** |
| ~~SDK2~~ | ~~`AuthResponse` do TS sem `sessionSignature` — inconsistente com Python/Ruby e com o que o mobile de fato envia~~ | **Sessão 160** |
| ~~SDK3~~ | ~~`computeSmartAccountAddress`/`compute_smart_account_address` portado do Desktop pros 3 SDKs — `docs/docs/smart-account.mdx` já prometia essa função no SDK TS, mas ela nunca existiu lá~~ | **Sessão 160** |
| ~~SDK4~~ | ~~Achado real no caminho: `verify_auth_response` do SDK Ruby comparava a chave pública recuperada (`Eth::Signature.personal_recover`, 65 bytes) direto contra um endereço (20 bytes) — nunca batia, rejeitando toda assinatura válida. Corrigido com `Eth::Util.public_key_to_address`~~ | **Sessão 160** |
| ~~SDK5~~ | ~~Nenhum dos 3 SDKs tinha suíte de testes — adicionados vitest (TS), pytest (Python) e rspec (Ruby), incluindo um vetor fixo de paridade cross-linguagem pra `computeSmartAccountAddress`~~ | **Sessão 160** |
| ~~SDK6~~ | ~~Documentação (README, 3 páginas de SDK, quickstart, exemplo) reescrita — narrativa de relayer wallet removida, versões fictícias ("mobile app v14.9.5+"/"v1.1+") removidas~~ | **Sessão 160** |

### Novo SDK Dart (Sessão 161)

| ID | Item | Resolvida em |
|---|---|---|
| ~~SDK7~~ | ~~Novo `sdk/dart/` (`truthid_sdk`) — `TruthIDClient` (verificador, mesmo papel do TS/Python/Ruby: createChallenge/verifyAuthResponse/verifySession/checkDeviceStatus/computeSmartAccountAddress), pure-Dart, sem dependência de `package:flutter`~~ | **Sessão 161** |
| ~~SDK8~~ | ~~`TruthIDRequester` (papel novo, nunca implementado em nenhum idioma antes) — 3 fluxos genéricos (`signMessage`/`signRequest`/`pin`), portado do protocolo real do Mobile (ECIES, HKDF, IPNS, LAN sweep) e do requisitante de referência da extensão (`vaultEdit/*`, TypeScript)~~ | **Sessão 161** |
| ~~SDK9~~ | ~~Vetor de paridade cross-linguagem (`computeSmartAccountAddress`) validado também no Dart — 4º SDK bate byte a byte com TS/Python/Ruby na primeira tentativa~~ | **Sessão 161** |
| ~~SDK10~~ | ~~Vetor de paridade IPNS validado contra Kubo real (mesmo fixture de `mobile/test/services/ipns_key_service_test.dart`, Sessão 113) reaproveitado no SDK Dart~~ | **Sessão 161** |
| ~~SDK11~~ | ~~Achado real no caminho: conectar um container Docker à sua própria IP externa trava (hairpin NAT) — sweep LAN e dead-drop poll tornados injetáveis em `TruthIDRequester` pra testar a orquestração sem depender de rede real entre dois dispositivos físicos~~ | **Sessão 161** |
| ~~SDK12~~ | ~~52 testes novos (dart test), `dart analyze` limpo, doc nova (`docs/docs/sdk/dart.md`) + seção no `sdk/README.md`, build do Docusaurus validado~~ | **Sessão 161** |

### P25 — hang em `cargo test --lib pin::` (Sessão 162)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P25~~ | ~~Causa raiz: descasamento de casing entre o `app_name` normalizado (minúsculo) que `try_consume_quota` usa pra buscar e o `app_name` capitalizado que 3 testes semeavam direto no arquivo de autorizações — o app "não era encontrado", caía no caminho de aprovação e ficava parqueado esperando um `resolve()` que esses testes nunca chamavam. Corrigido semeando os testes já normalizados e endurecendo `revoke_authorization`/`set_daily_limit` pra também normalizar o `app_name` recebido (mesma consistência que o resto do módulo já tinha)~~ | **Sessão 162** |

### P10 — Fase 2 do passkey na extensão + senha nova via extensão (Sessão 164)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P10~~ | ~~Documentação corrigida: a fatia Mobile já tinha fechado 100% em hardware real nas Sessões 135-136 (`PENDING.md`/`ROADMAP.md` nunca foram atualizados depois disso). **Escopo real que faltava — senha nova via extensão — implementado nesta sessão**: novo `extension/src/autofill/newCredentialCapture.ts` detecta submit de formulário com usuário+senha e propõe a credencial pro Device aprovar (mesma heurística do "Salvar senha?" nativo dos navegadores — propõe quando não há entrada existente com esse username exato pro hostname, evita distinguir estruturalmente cadastro de login). Toda a infra downstream (`pendingEdits.ts`, `cipher.ts`, `mobileDelivery.ts`/`desktopDelivery.ts`, `vault_edit_approval_screen.dart`/`VaultEditApprovalModal.tsx`, `vault_edit.rs`) já era genérica — zero mudança fora da extensão. Batch sync (sub-item 2.1 do roadmap) segue de fora, ver P29~~ | **Sessão 164** |

### P27 — `vault-edit` no SDK Dart (Sessão 165)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P27~~ | ~~Novo método `TruthIDRequester.vaultEdit(...)` — LAN + dead-drop cross-network completo, paridade com o requisitante de referência em TypeScript (`extension/src/vaultEdit/*`). Sem fase de resposta (estrutura diferente de `signMessage`/`signRequest`/`pin`), retorna `VaultEditPendingRequest` (`delivered: Future<bool>`) em vez de `PendingRequest<T>`. Novo `internal/vault_edit_content_cipher.dart` (cifra AES-GCM/HKDF, salt domain-separado), `internal/vault_edit_dead_drop_key.dart` (deriva o par Ed25519 completo, não só o nome IPNS público — vetor cross-language TS↔Dart validado byte a byte), `internal/kubo_publish_client.dart` (cliente HTTP Kubo novo via `dart:io`, add/key-import/name-publish/key-rm, best-effort — primeiro cliente Kubo do SDK Dart). `marshalKeyProtobuf`/`computeIpnsName` de `ipns_key.dart` promovidos a públicos e reaproveitados. `dart test` 69/69 (17 novos), `dart analyze` limpo, build do Docusaurus validado~~ | **Sessão 165** |

### P12/P13 — documentação corrigida, já estavam fechados (achado ao levantar pendências, Sessão 166)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P12~~ | ~~Dead-drop IPFS/IPNS pra `/sign-message`/`/sign-request` — já implementado nos dois (`_deadDropIpnsName`/`_deadDropError` em `sign_message_approval_screen.dart` e `sign_request_approval_screen.dart`, e no SDK Dart via `DeadDropPollClient` em `_awaitResult`/`_raceForBlob`, requester.dart). Nunca foi removido do PENDING.md depois de implementado~~ | já implementado antes, doc corrigida na Sessão 166 |
| ~~P13~~ | ~~Callback opcional no login — duplicata: já fechado como **F15** ("Funcionalidades Implementadas", Sessão 142), `callbackUrl` já é `String?` opcional em `approval_screen.dart` desde então. P13 nunca foi removido quando F15 fechou~~ | já implementado (F15, Sessão 142), doc corrigida na Sessão 166 |

### P29 — Sync em lote (batch sync) pro vault-edit (Sessão 166)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P29~~ | ~~Extensão acumula 1+ credenciais propostas (`pendingEdits.ts` já era lista) e manda tudo numa sessão só; Device (Mobile/Desktop) mostra uma lista, aprova de uma vez — N `addEntry`/`vault_upsert_entry` locais + **1 publish() só** no fim. **Achado que reduziu bastante o escopo**: `executeBatch` não era necessário — `VaultRegistry.updateVault` sempre grava um único `(cid, contentHash)` por publish, não importa quantas entradas mudaram no blob (confirmado no contrato); `executeBatch` nunca foi usado por nenhum UserOperation via device key/bundler, só por transações diretas do owner (Ledger) em pareamento de device, sem relação com Vault. `desktop/src-tauri/src/vault_edit.rs`/`local_signer_server.rs`: corpo HTTP virou `Vec<VaultEditRequestBody>` (só a extensão fala esse endpoint, sem quebra de compatibilidade). `mobile/lib/screens/vault_edit_approval_screen.dart`: aceita tanto lista quanto objeto único no conteúdo cifrado — o SDK Dart (`TruthIDRequester.vaultEdit`, P27) ainda manda uma proposta por sessão, sem precisar de nenhuma mudança nele. `extension/src/vaultEdit/{mobileDelivery,desktopDelivery}.ts`/`popup/main.ts`: usam a lista `pending` inteira em vez de `pending[0]`; novo `removePendingEdits` (lote) substitui `removePendingEdit` (item único, ficou órfão, removido). `cargo test --lib` 97/97 (4 novos), `flutter test` 407/407 (4 novos), `npx vitest run` (extensão) 88/88 (2 novos), `tsc --noEmit`/`npm run build`/`flutter analyze` limpos~~ | **Sessão 166** |

### P36 — decifra do Vault dentro da extensão iOS (Sessão 176)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P36~~ | ~~15.6, fatia 2 — `CredentialProviderViewController` só mostrava uma tela informativa e cancelava~~. `CredentialProviderViewController.swift` agora decifra AES-256-GCM (`CryptoKit.AES.GCM.SealedBox(combined:)`, o layout do blob `nonce\|\|ciphertext\|\|tag` bate byte a byte com o formato `combined`, sem slicing manual) usando chave/blob compartilhados via Keychain Access Group + App Group (`SharedVaultAccess.swift`, duplicado nos 2 targets — contrato próprio, não depende do schema interno do `flutter_secure_storage`). Novo canal `truthid/ios_autofill_vault_sync` (`AppDelegate.swift`, 2 métodos: `syncVaultKey`/`syncVaultBlob`) + `IosAutofillVaultSyncService` (Dart, novo) espelham chave+blob toda vez que `vault_screen.dart::_load()` recarrega (reaproveita `VaultRepository.readRawBlob()`/`VaultKeyService.deriveVaultKey()`, já existentes). `prepareInterfaceToProvideCredential` preenche direto pelo `recordIdentifier`; `prepareCredentialList` mostra um picker simples (`UITableView`) de todas as entradas tipo credencial; `provideCredentialWithoutUserInteraction` continua cancelando de propósito, forçando o gate de Face ID/passcode que o próprio iOS já exige antes de invocar a extensão. `Security.framework`/`CryptoKit.framework` linkados no target (mesma referência `SDKROOT`-relativa da fatia 1). Checklist de validação em hardware **fundido em P35** — nada disso builda neste ambiente Linux, sem Xcode/macOS/simulador, mesma limitação já registrada na fatia 1. `flutter analyze`/`flutter test` (lado Dart) verificados nesta sessão.~~ | **Sessão 176** |

### P8 — Fase 15 (Digital Identity Vault) concluída por completo, 15.1-15.8 (Sessão 177)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P8~~ | ~~Fase 15 — Digital Identity Vault (documentos, endereços, cartões de crédito), 8 etapas~~. Última etapa, 15.8 (revisão de segurança): auditoria confirmou zero logging real (grep completo, nenhum print/console.log/dbg!/tracing toca card_number/cvv em produção ou testes), fechou preventivamente um `impl Debug` customizado redigindo `card_number`/`cvv` na `CreditCardData` (Rust) contra vazamento futuro por `dbg!`/`tracing`. Implementada a cifra individual de `card_number`/`cvv` (tentativa+fallback, mesmo padrão de migração de chave que `vault::load()` já usa — sem flag de schema nova), sempre em claro em memória e sempre cifrada em disco/export, reusando a mesma vault key (sem sub-chave derivada). Achado crítico corrigido: o reparse inline de `vault_publish`/`markPublished` (otimização pré-existente que evita `load()` duplicado) não normalizava os campos de cartão, o que geraria "pendência fantasma" pra sempre em qualquer vault com cartão — corrigido com teste de regressão nos 2 lados. Achado real de exposição mínima: `toJsonForExtension()` só removia `totp_secret`, não `document`/`address`/`credit_card` — uma entrada de cartão sincronizada pelo canal QR mais antigo (13.9) vazava em texto pleno pro `chrome.storage.session` da extensão; corrigido. `_MaskedInfoRow` (Mobile) trocou máscara proporcional ao tamanho por máscara fixa (vazava comprimento do PAN/CVV). Polish: campos de cartão no formulário Desktop ganharam reveal toggle igual senha. `cargo test --lib` 135/135, `flutter test` 496/496, `flutter analyze`/`npx vitest run` 101/101/`tsc --noEmit`/`cargo clippy`/`cargo fmt --check` limpos.~~ | **Sessão 177** |

### Bugs do `/code-review max` (Desktop) — 52/52

| ID | Grupo | Resolvida em |
|---|---|---|
| ~~B1-B52~~ | ~~Todos os 52 bugs de alta/média/baixa severidade~~ | **Sessão 149** |

### Backlog (Sessão 130)

| ID | Item | Resolvida em |
|---|---|---|
| ~~BL1~~ | ~~Ler QR code do 2FA (TOTP) no celular e desktop~~ | **Sessão 132** |
| ~~BL2~~ | ~~Passkey na extensão — Fase 1 (login)~~ | **Sessão 133** |
| ~~BL3~~ | ~~Gerador de senha do Desktop como popup~~ | **Sessão 135** |
| ~~BL4~~ | ~~Bug de "pending changes" falso no Mobile~~ | **Sessão 131** |

### Funcionalidades Implementadas

| ID | Funcionalidade | Concluída em |
|---|---|---|
| ~~F1~~ | ~~Fase 1 — Smart Contracts~~ | ✅ |
| ~~F2~~ | ~~Fase 2 — Comunicação (WebRTC — retirado)~~ | ✅ |
| ~~F3~~ | ~~Fase 3 — Desktop App (Tauri)~~ | ✅ |
| ~~F4~~ | ~~Fase 4 — Mobile App (Flutter)~~ | ✅ |
| ~~F5~~ | ~~Fase 5 — SDKs (TS, Python, Ruby)~~ | ✅ |
| ~~F6~~ | ~~Fase 6 — Integração & Testes E2E~~ | ✅ |
| ~~F7~~ | ~~Fase 7 — Mainnet & Lançamento~~ | ✅ |
| ~~F8~~ | ~~Fase 8 — Documentação Web (Docusaurus)~~ | ✅ |
| ~~F9~~ | ~~Fase 9 — Identidade Visual~~ | ✅ |
| ~~F10~~ | ~~Fase 10 — Ledger via USB direto (Desktop)~~ | ✅ |
| ~~F11~~ | ~~Fase 11 — Teste E2E Prático~~ | ✅ |
| ~~F12~~ | ~~Fase 12 — Publicação & Release (v1.0.0)~~ | ✅ |
| ~~F13~~ | ~~Fase 13 — TruthID Vault (senhas)~~ | ✅ |
| ~~F14~~ | ~~Fase 14 — Smart Account (ERC-4337)~~ | ✅ |
| ~~F15~~ | ~~Callback opcional no login~~ | **Sessão 142** |
| ~~F16~~ | ~~Vault genérico — Desktop assina via device key (fatia 1)~~ | **Sessão 102** |
| ~~F17~~ | ~~Vault genérico — canal de comunicação local (fatia 2a)~~ | **Sessão 103** |
| ~~F18~~ | ~~Vault genérico — sign-request + modal (fatia 2b)~~ | **Sessão 103** |
| ~~F19~~ | ~~Vault genérico — Practice Valuation cliente (fatia 3)~~ | **Sessão 103** |
| ~~F20~~ | ~~`/sign-message` implementado~~ | **Sessão 107** |
| ~~F21~~ | ~~Cross-device /sign-message — LAN (fatia 1)~~ | **Sessão 108** |
| ~~F22~~ | ~~Cross-device /sign-message — dead-drop (fatia 2)~~ | **Sessão 109** |
| ~~F23~~ | ~~Cross-device /sign-request — LAN (fatia 1)~~ | **Sessão 110** |
| ~~F24~~ | ~~Backup criptografado exportável~~ | **Sessão 126** |
| ~~F25~~ | ~~`/truthid/v1/pin` — núcleo Rust (fatia 1)~~ | **Sessão 119** |
| ~~F26~~ | ~~`/truthid/v1/pin` — rota HTTP + tela (fatia 2)~~ | **Sessão 120** |
| ~~F27~~ | ~~`/truthid/v1/pin` — Settings (fatia 3)~~ | **Sessão 121** |

### P17 — Social Recovery UI (Sessão 178)

| ID | Item | Resolvida em |
|---|---|---|
| ~~P17~~ | ~~**Social Recovery (UI)** — N-de-M guardiões com multisig/timelock. Contrato `RecoveryManager` já existia e estava deployado, mas sem interface de usuário. Implementado: `GuardianManagement.tsx` (Desktop) com configurar/propor/aprovar/executar/cancelar recovery + `GuardianStatusScreen.dart` (Mobile, só leitura). Aba "Recovery" adicionada ao Desktop App.tsx. ABI completo do RecoveryManager adicionado aos dois lados. `tsc --noEmit` limpo, 101/101 testes passando. Redeploy em cascata (P1/P2) não é pré-requisito — a UI funciona contra o contrato atual~~ | **Sessão 178** |
| ~~P5~~ | ~~**Validação E2E real — assinatura via device key no Mainnet** — device key do Desktop e bundler já estavam prontos (herdado do P4, mesma sessão). Clicado "Publicar via device key (sem Ledger)" na aba Vault: primeira tentativa falhou com "todos os providers Kubo falharam" (`ipfs.service` systemd --user instalado mas inativo nesta máquina) — dono do projeto autorizou `systemctl --user start ipfs`, segunda tentativa funcionou. Vault foi de versão 8 pra 9. **Confirmado on-chain via `cast call getVault(1)` num RPC público, independente do app**: `version=9`, `updatedAt=1785599131` (01/08/2026 15:45 UTC, bate com o horário real da sessão). Prova real do pipeline UserOp+bundler assinando só com device key, sem toque físico no Ledger, contra a Mainnet.~~ | **Sessão 181** |
| ~~P45~~ | ~~**`queryClient.invalidateQueries()` sem filtro** (`GuardianManagement.tsx`, configureGuardians/propose/approve/execute) — invalida o cache do app inteiro (vault, sessões, devices, saldos) a cada ação de guardião, não só as queries de recovery. **Fechado como não-bug**: é o mesmo padrão usado em `App.tsx` (botão "Refresh" manual), `ManageDevices.tsx`, `WithdrawModal.tsx`, `DesktopDevice.tsx` e `CreateIdentity.tsx` — convenção deliberada do projeto (refetch amplo após qualquer escrita), não um descuido isolado do Recovery. Corrigir só aqui quebraria a consistência e arriscaria parar de atualizar saldo/devices depois de uma ação de guardião, que hoje dependem desse invalidate global. Dono do projeto escolheu não mexer.~~ | **Sessão 181** |
| ~~P44~~ | ~~**Timelock de 7 dias hardcoded 2x** (`timeRemaining`/`canExecute`) em vez de reusar o `timelock` já lido do `TIMELOCK()` do contrato. Se o contrato fosse redeployado com timelock diferente, o contador regressivo e o gate do botão Execute ficariam dessincronizados do texto. Corrigido: `timeRemaining` ganhou um 2º parâmetro `timelockSecs` (default `7n * 86400n` só como fallback pra janela breve antes do `TIMELOCK()` carregar — default de parâmetro JS já cobre `timelock` chegando `undefined`), os 2 call sites (identidade própria e a seção do P39) passam o valor real; `canExecute` ganhou a mesma guarda `timelock !== undefined` que `canTargetExecute` (P39/P41) já tinha desde que nasceu.~~ | **Sessão 181** |
| ~~P43~~ | ~~**No-op silencioso quando `smartAccountAddress` é `null`** (`handleConfigure`/`handleCancel`) — diferente das outras validações nas mesmas funções, que chamam `setConfigError(...)`, esses branches só davam `return` sem nenhum feedback visual. Corrigido: `handleConfigure` chama `setConfigError(...)` (reusa o estado/exibição já existentes); `handleCancel` ganhou um `cancelError` local novo, exibido junto do erro de transação já existente.~~ | **Sessão 181** |
| ~~P42~~ | ~~**`hasApproved` indefinido mostrava "Approve" pra quem já tinha aprovado** — mesmo padrão do P41 (query separada carregando à parte, estado de loading tratado como negativo). Contrato já protegia via revert (`AlreadyApproved`), só gerava um prompt de transação falho confuso. Corrigido com `isHasApprovedLoading`/`isTargetHasApprovedLoading` gateando os dois pontos (identidade própria e a nova seção "Act as Guardian" do P39, que nasceu com o mesmo padrão) — mostra "Checking approval status…" em vez do botão/badge até a leitura resolver.~~ | **Sessão 181** |
| ~~P41~~ | ~~**`canExecute` corria à frente do `threshold` real** — `getProposal` e `getGuardianConfig` carregam de forma independente; se a proposta resolvesse antes do threshold (que ficava `0n` por padrão), "Execute Recovery Now" aparecia disponível antes da hora. Contrato já protegia via revert (`ThresholdNotReached`), então era só UX ruim, não risco de segurança. Corrigido reusando o mesmo `isGuardianConfigLoading` do P40 como guarda extra em `canExecute`.~~ | **Sessão 181** |
| ~~P40~~ | ~~**Risco de sobrescrita silenciosa de guardiões reais** — `guardianConfig` ficava `undefined` enquanto `getGuardianConfig` carregava, então `isConfigured` caía no default `false` e mostrava "No guardians configured" mesmo com guardiões já configurados; num RPC lento, clicar em "Configure Guardians" nesse estado sobrescreveria a lista real. Corrigido: novo `isGuardianConfigLoading` (de `useReadContract`) gateia o aviso — enquanto carrega, mostra "Loading guardian configuration…" em vez do aviso; o aviso "No guardians configured" (e o botão que abre o form) só renderiza depois que a leitura resolve de verdade. `tsc --noEmit`/`vitest run` (101/101) limpos.~~ | **Sessão 181** |
| ~~P39~~ | ~~**Fluxo de guardião (Propose/Approve) inalcançável na prática** — `GuardianManagement.tsx` usava `username` do `IdentityContext`, que sempre resolve pra identidade de quem está conectado, sem campo de busca. Corrigido: nova seção "Act as Guardian" com input de username + botão "Look up", reads (`getGuardianConfig`/`getProposal`/`hasGuardianApproved`) e handlers (`proposeRecovery`/`approveRecovery`/`executeRecovery`) próprios, escopados ao username buscado (`guardianTarget`), totalmente separados dos da identidade própria — nenhum comportamento existente (configurar/cancelar a própria identidade) foi tocado. `canTargetExecute` já nasce sem a corrida do P41 (guarda `targetThreshold > 0n`). `tsc --noEmit`/`vitest run` (101/101) limpos. Validado com clique real no Desktop nativo: buscar o próprio username mostra corretamente "You are not a guardian for @masterlxz" (nenhum guardião configurado ainda) — confirma que o lookup lê o contrato pelo username buscado, não mais pelo da identidade logada.~~ | **Sessão 181** |
| ~~P4~~ | ~~Validação E2E real — delegação de assinatura (Desktop + Practice Valuation). Colisão de porta 1420 já não existia mais (Practice Valuation fixou 1430/1431 permanentemente antes desta sessão). Achado real: primeira tentativa de Approve com o Ledger conectado na conta errada ("Account 0" do modal) devolveu `AA20 account not deployed` — `smartAccountAddress` é derivado do endereço conectado via `computeSmartAccountAddressSync`, então conectar a conta errada aponta pra um CREATE2 nunca implantado. Achado o endereço certo lendo `owner()` da smart account on-chain via `cast call` (bate com "Account 1" do Ledger). Reconectado, novo sign-request, Approve → `Status: executed`, `userOpHash`/`transactionHash` reais. **Confirmado on-chain via `cast receipt` num RPC público, independente do app**: bloco 49402374, `status: 1 (success)`, `to` é o EntryPoint v0.7. Primeira vez que o caminho completo (app terceiro → loopback → aprovação → Ledger → UserOp → bundler Pimlico → EntryPoint) roda de ponta a ponta contra a Mainnet de verdade~~ | **Sessão 179** |