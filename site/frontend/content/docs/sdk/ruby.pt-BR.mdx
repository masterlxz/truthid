---
title: SDK Ruby
description: "Referência completa da API para truthid-sdk no RubyGems."
---

Referência completa da API para [`truthid-sdk`](https://rubygems.org/gems/truthid-sdk) no RubyGems. Novo no TruthID? Comece pelo [Guia rápido](/docs/quickstart) — esta página é a referência detalhada de cada método e tipo, para quando você estiver integrando de verdade.

## Instalação

```bash
gem install truthid-sdk
```

Requer Ruby 3.0+.

## `TruthID::Client`

```ruby
require "truthid"

truthid = TruthID::Client.new  # usa network: "base-mainnet" por padrão
```

Também existe uma função factory, se você preferir:

```ruby
truthid = TruthID.new_client  # equivalente a TruthID::Client.new
```

### Construtor

`TruthID::Client.new(network: "base-mainnet", rpc_url: nil)`

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|----------|--------------|
| `network` | `"base-sepolia"` ou `"base-mainnet"` | Não — padrão `"base-mainnet"` | De qual rede ler os contratos |
| `rpc_url` | `String` ou `nil` | Não | Endpoint de RPC customizado. Usa por padrão o RPC público da Base para a rede escolhida |

Assim como no Python, `network` tem um padrão aqui — você só precisa passá-lo para sobrescrever ou para usar a testnet. (O SDK TypeScript exige explicitamente, sem padrão.)

## Métodos

### `create_challenge(origin)`

Cria um desafio de uso único para incorporar no QR code mostrado ao usuário.

**Parâmetros**

| Nome | Tipo | Descrição |
|------|------|-------------|
| `origin` | `String` | O domínio do seu site, ex.: `"yoursite.com"` |

**Retorna** [`AuthChallenge`](#authchallenge)

```ruby
challenge = truthid.create_challenge("yoursite.com")
challenge.nonce      #=> "3f2e1a4b-..."
challenge.issued_at  #=> 1718000000000
```

<Callout title="Armazene, depois apague" type="idea">
Mantenha o desafio no servidor, indexado por `nonce`, até `verify_auth_response` rodar — depois apague-o imediatamente. Veja [Invalidação de nonce](#nonce-invalidation) abaixo.
</Callout>

**Construindo o QR code** — o app mobile espera exatamente este formato (`challenge.to_h` já produz as chaves camelCase corretas):

```json
{
  "action": "truthid-auth",
  "challenge": { "type": "challenge", "nonce": "...", "issuedAt": 1718000000000, "origin": "yoursite.com" },
  "callbackUrl": "https://yoursite.com/auth/verify"
}
```

`callbackUrl` **precisa** usar `https://` — o app mobile se recusa a enviar a resposta assinada para uma URL `http://` simples.

---

### `verify_auth_response(challenge, response, ttl_ms: 30_000)`

Verifica a resposta assinada recebida do celular do usuário. Executa seis verificações em sequência e para na primeira falha:

1. O usuário aprovou (não rejeitou)
2. O desafio está dentro do TTL (padrão: 30 segundos)
3. O nonce corresponde ao desafio original
4. A assinatura criptográfica é válida
5. O dispositivo está registrado e ativo na blockchain
6. Recupera o ID da identidade vinculada a esse dispositivo

**Parâmetros**

| Nome | Tipo | Descrição |
|------|------|-------------|
| `challenge` | [`AuthChallenge`](#authchallenge) | O desafio que você criou |
| `response` | [`AuthResponse`](#authresponse) | A resposta recebida do celular |
| `ttl_ms:` *(opcional)* | `Integer` | Idade máxima do desafio em ms. Padrão: `30_000` |

**Retorna** [`VerifyAuthResult`](#verifyauthresult)

`AuthResponse.from_hash` o monta diretamente a partir do corpo JSON já parseado — diferente do SDK Python, sem necessidade de mapeamento manual de campos:

```ruby
data = JSON.parse(request.body.read)  # { "approved" => ..., "nonce" => ..., "signature" => ..., "deviceAddress" => ... }

response = TruthID::AuthResponse.from_hash(data)
result = truthid.verify_auth_response(challenge, response)

if result.valid
  puts "Authenticated! Identity ID: #{result.identity_id}"
else
  puts "Failed: #{result.reason}"
end
```

**Motivos de falha**

| `reason` | Causa |
|----------|-------|
| `"User rejected the login request"` | Usuário tocou em "Rejeitar" no celular |
| `"Challenge expired"` | Mais de `ttl_ms` ms se passaram desde `issued_at` |
| `"Nonce mismatch"` | O nonce da resposta não corresponde ao do desafio |
| `"Invalid signature format"` | A assinatura está malformada |
| `"Signature does not match device address"` | A assinatura não foi feita por `device_address` |
| `"Device is not active or has been revoked"` | O dispositivo foi revogado pelo dono da identidade |

---

### `verify_session(session_hash)` [#verify_sessionsession_hash]

Verifica se um hash de sessão ainda é válido (não revogado). Chame isso nas requisições subsequentes após o login para confirmar que a sessão não foi revogada de outro dispositivo.

**Parâmetros**

| Nome | Tipo | Descrição |
|------|------|-------------|
| `session_hash` | `String` | String hex `bytes32` (`0x...`) |

**Retorna** [`SessionInfo`](#sessioninfo)

```ruby
session = truthid.verify_session(session_hash)
if session.exists && !session.revoked
  # ainda logado
end
```

---

### `check_device_status(device_pub_key)`

Consulta o status atual de um dispositivo na blockchain.

**Parâmetros**

| Nome | Tipo | Descrição |
|------|------|-------------|
| `device_pub_key` | `String` | Endereço Ethereum do dispositivo (`0x...`) |

**Retorna** [`DeviceStatus`](#devicestatus)

```ruby
status = truthid.check_device_status(device_pub_key)
```

---

### `compute_smart_account_address(ledger_address, index: 0)` [#compute_smart_account_addressledger_address-index-0]

Prevê o endereço de smart account (controladora) de um usuário via `CREATE2` — cálculo puramente local, sem chamada de RPC e sem servidor envolvido. Útil para mostrar um endereço de depósito, ou verificar um saldo, antes que a conta jamais tenha sido usada on-chain.

**Parâmetros**

| Nome | Tipo | Descrição |
|------|------|-------------|
| `ledger_address` | `String` | O endereço da carteira de hardware / EOA dona |
| `index:` *(opcional)* | `Integer` | Índice da conta, para múltiplas smart accounts por dono. Padrão: `0` |

**Retorna** `String` — o endereço checksummed da smart account.

```ruby
smart_account = truthid.compute_smart_account_address(ledger_address)
```

Uma função em nível de módulo também está disponível para quando você não precisa de uma instância completa de cliente:

```ruby
smart_account = TruthID.compute_smart_account_address(ledger_address, network: "base-mainnet")
```

## Tipos

Todos os tipos a seguir estão no módulo `TruthID`. `AuthChallenge` e `AuthResponse` são classes simples — seus atributos seguem o snake_case normal do Ruby (`issued_at`, `device_address`), e cada um tem um método de conversão (`to_h` / `from_hash`) no ponto de contato com o JSON camelCase que o app mobile de fato envia e assina. `VerifyAuthResult`, `SessionInfo` e `DeviceStatus` são `Struct`s, já que nunca cruzam a rede.

#### `AuthChallenge` [#authchallenge]

```ruby
class AuthChallenge
  attr_reader :type, :nonce, :issued_at, :origin

  # to_h => { "type" => ..., "nonce" => ..., "issuedAt" => ..., "origin" => ... }
end
```

#### `AuthResponse` [#authresponse]

```ruby
class AuthResponse
  attr_reader :approved, :nonce, :signature, :device_address, :session_signature

  # self.from_hash(h) lê h["deviceAddress"] em device_address, etc.
  # session_signature: personal_sign sobre keccak256(nonce) — sempre enviado junto com signature
end
```

#### `VerifyAuthResult` [#verifyauthresult]

```ruby
VerifyAuthResult = Struct.new(:valid, :identity_id, :device_address, :reason, keyword_init: true)
```

#### `SessionInfo` [#sessioninfo]

```ruby
SessionInfo = Struct.new(:exists, :revoked, :identity_id, :device_pub_key, :created_at, keyword_init: true)
```

#### `DeviceStatus` [#devicestatus]

```ruby
DeviceStatus = Struct.new(:exists, :active, :label, :identity_id, :added_at, keyword_init: true)
```

## Notas de segurança

### Invalidação de nonce [#nonce-invalidation]

Apague o desafio do seu armazenamento **antes** de chamar `verify_auth_response`, não depois — apagar depois deixa uma condição de corrida em que a mesma resposta assinada pode ser submetida duas vezes dentro da janela do TTL.

```ruby
# Ordem correta
pending_challenges.delete(response.nonce)            # apague primeiro
result = truthid.verify_auth_response(challenge, response)  # depois verifique
```

### TTL

O padrão é 30 segundos, correspondendo à própria expiração de desafio do app mobile. Diminuí-lo é aceitável; aumentá-lo além de 30 segundos não tem efeito, já que o celular já se recusará a aceitar um desafio mais antigo.

### Somente HTTPS

O celular faz um `POST` da resposta assinada diretamente para a sua URL de callback — o app mobile recusa URLs que não sejam `https://`, e seu endpoint ainda precisa de um certificado TLS válido para a conexão ter sucesso.

## Redes

| Rede | Chain ID | Descrição |
|---------|----------|--------------|
| `"base-sepolia"` | 84532 | Testnet — para desenvolvimento |
| `"base-mainnet"` | 8453 | Produção (padrão) |

```ruby
# Testnet durante o desenvolvimento
truthid = TruthID::Client.new(network: "base-sepolia")

# RPC customizado em vez do endpoint público
truthid = TruthID::Client.new(network: "base-mainnet", rpc_url: "https://your-private-rpc.example.com")
```

Os endereços dos contratos para ambas as redes estão em [Contratos inteligentes](/docs/intro#smart-contracts-base-mainnet-chain-8453).

## Visibilidade da sessão — nada a fazer

Logins concluídos já aparecem nos apps mobile/desktop do TruthID do usuário e podem ser revogados individualmente por lá. O celular registra a sessão on-chain por conta própria (via uma UserOperation) antes mesmo de chamar sua URL de callback — não há etapa de registro separada para você chamar.

Se quiser confirmar que uma sessão chegou on-chain, derive seu hash a partir do nonce e chame `verify_session`:

```ruby
session_hash = "0x" + Eth::Util.keccak256(response.nonce).unpack1("H*")
session = truthid.verify_session(session_hash)
if session.exists && !session.revoked
  # confirmado on-chain
end
```

## Próximos passos

- [Guia rápido](/docs/quickstart) — passo a passo completo, da instalação ao primeiro login
- [Exemplo completo com Sinatra](https://github.com/masterlxz/truthid/blob/main/sdk/README.md#full-examples) com tokens de sessão e uma rota protegida
- [Referência do SDK TypeScript](/docs/sdk/typescript)
- [Referência do SDK Python](/docs/sdk/python)
